# 多轮创作与旧进度保留：实际验收

本记录承接[完整城市收集与起降验收](ACTUAL_CITY_FLIGHT_PLAY_2026-09-14.md)。使用原 PI Desktop 的同一世界、同一创作会话和真实 `gpt-6-astra / xhigh`；未给模型增加 token、调用次数或整轮时长上限。

## 第三轮：白天轻雨

前两轮已做出完整城市、博美、三个一次性收集目标、登机锁、实体跑道和可驾驶飞机。实际游玩完成三件收集、起飞、降落和离机后，通过普通创作入口要求只修改天气，并保留所有已有进度。Agent 检索了已有控雨素材；因其包含定点控雨技能，没有安装，而是制作独立轻雨显示脚本。

- 世界：`world-7a2bad903a26`；会话：`da57da5e-063e-46a0-99a7-7b3ba27fd35e`。
- 原构建：`gbd-345efa60a42214b1c97d5bfb9d18d46151c066b7db7e365fe1350cc44cfad758`。
- 新构建：`gbd-8c52f3d3c74b77d80bb42250d8226e42df9e77d293101c977e0280e58681d98c`。
- 实际模型轮次：`f34a92ff-a0f1-4e12-a356-83ce1649f693`，451.31 秒，约 7 分 31 秒。
- 真实本轮用量：未缓存输入 323,123、缓存读取 5,940,608、输出 10,228，总计 6,273,959 token；推理明细 4,605 已包含在报告总量中，不重复相加。调用数、成本和纯生成 TPS 未提供。

原生检查通过后自动采用。随后总控通过普通保存、快照读取、真实画面捕获和完全退出重开验证：**所有旧存档字段与值均保持一致，冷启动恢复的完整状态也完全一致。** 包括五项库存、飞机实例及停放位置、已离机状态、一次降落与约 4.6 秒飞行记录、博美实例与位置、等待状态和一次抚摸记录。雨丝和新的白天天空已实际目视核对。

只读核验脚本为 `tests/product-continuation-evidence.mjs`，证据汇总见 [weather-preservation.json](evidence/product-continuation-20260914/weather-preservation.json)。实际输入为短路径原生目录 `D:/cm-product-agent/test-results/desktop-native-product-5N6HZ2/responses/` 下的 `play-102/103/106/107/108.json`。`play-109/110/111` 再次保留重开后的冻结状态与画面。所有回执、截图和三轮用量来源保留在该独立目录，不修改原文件。

该轮使用源码模式应用构建 `ad6bd63f`、Core 二进制 `2e0a34c7`。最终 Windows 包需按其自身身份另行验收，不能把这组源码模式结果当作已测试发行包。

## 真实体验限制与恢复记录

天气修改后沿机场道路实际步行，城市、库存、飞机和等待中的博美仍在。直接与博美身体相撞会减速或被推向侧面；之前的完整路线在正对身体时曾连续停止，侧移可绕行。这是需要处理的实际体验反馈，不能宣称所有接触角度都会完全卡死。

后续观察阶段，验收脚本写 `session.json` 时出现一次 Windows `EPERM` 原子替换失败。应用正常保存并退出，随后从同一档案恢复；故障属于测试证据写入。脚本增加对短暂文件共享冲突的重试，保持旧完整文件直到原子替换成功，未删除目标文件。两条带旧实例身份的待执行输入 `play-115/116` 在恢复后被正确拒绝，未改变世界；后续使用新身份继续普通步行。旧失败报告和回执原样保留。

本记录证明多轮修改的旧状态兼容和保存重开；飞行在轻雨下的完整再起降、长时间连续手感以及后续反馈修复，需要对应的独立实际记录，不能由存档比较推断。
