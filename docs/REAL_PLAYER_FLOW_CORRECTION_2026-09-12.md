# 按真实玩家配置纠正宣传愿望验收

用户指出额外额度偏离正常使用。此前 `deepseek-flash/high/16384`、
40 次请求和 10 分钟截止的试验只能保留为受限诊断，不能据此判断用户
实际模型的创作能力。新增输出上限对照代码已撤回，普通入口不启用
`CRAFTMINE_CREATION_EVAL`，也不创建或使用评测账本。

只读核对用户本机最近会话，实际选中 `deepseek-v4.1-flash-expires-on-0910`、
`max` 思考，模型配置为 1000000 上下文及 384000 输出容量。完整模型能力
条目和 `inherit` 权限在隔离档案内按正常 API 配置，累计 token 为不限。
用户自己的数据库没有修改，凭据未写入公开记录。

## 正常流程中实际遇到的情况

这些尝试使用同一测试世界和会话，旧报告保留：

| 阶段 | 结果 | 模型生成请求 |
| --- | --- | --- |
| 普通入口初次连接 | 漏解包桌面 API 的 `ok/data` 返回值；已修正 | 0 |
| 普通聊天继续旧任务 | 产品要求先显式恢复旧草稿 | 0 |
| 从导航入口尝试恢复 | 入口的调用来源不适用；改用独立后台世界中正常工作台按钮所用的 API | 0 |
| 正常工作台恢复 | 旧任务仍带已经过期的 30 分钟截止；模型启动前被拒绝 | 0 |
| 正常结束过期任务并重新输入 | 保留草稿、世界和会话；真实模型开始输出，却被产品 120 秒总计时中断 | 1 |

实际模型尝试证据：
`D:/cm-promo-flight-clarified-0912/test-results/desktop-native-complete-BKI6gF/player-8119b35a-21ab-48fe-9f84-d37fd59d70bb.json`。
输入为「继续完成刚才的歼20，我要能实际驾驶它飞起来。」；正常任务
墙钟 120617 毫秒，保存了 90508 字符的思考，正文为空、状态 aborted。
应用的估计输出字段为 22627；供应商最终 usage 未收到，不能当作准确
计费 token。未生成飞机，不能算驾驶或视觉通过，也不能把它当供应商空响应。

`craftmineGuardedStream` 原计时器在请求准备前启动，收到持续增量也不
续期。因此该中断来自应用自身整次请求的硬截止。修复方向是仅对无进展
执行超时，真实增量续期，保留用户立即取消，不另加整次响应上限。

恢复使用独立测试成品自身的本机调试接口调用原工作台桥，先核实该世界
和 headless 输入保护；没有操控用户浏览器，没有发送鼠标键盘事件或请求
Pointer Lock。采用和探索验证也已支持普通报告，不依赖评测账本。

## 修正后的实际结果与限制口径

固定成品 `32cd879d` 下，原会话中的白博美改形、城市、飞机分别经 13、11、8 次真实调用结束，新检查、正规采用及保存冷重开通过。城市经历一次模型自行修复编译错误，任务约 605 秒，没有被旧十分钟评测上限中止。白狗名字与短距离跟随保持；城市实际主街步行及登坡通过；飞机目前只确认停放原型，驾驶尚未验收。外观均粗糙，不以技术检查替代玩家目标验收。

“无上限”须准确指累计 token：当前普通驱动未额外添加 token、调用次数、整轮时间限制，但原产品仍在 `host-requests.cjs` 首次请求设置三十分钟绝对截止，并保留请求数和压缩次数策略。既有预算测试与界面文案明确表达该设计。暂停、离线后恢复不会顺延截止，下一请求可能过期；这不是此次测试额外添加的限制，也不能宣传为整个产品任务完全不限时。本轮没有暗改产品预算或旧账本。

[最新原始实机画廊](D:/Craftmine-Promo-Preview-20260912-player/index.html) 保留各组配置来源，旧受限样本与本次普通玩家结果明确区分。
