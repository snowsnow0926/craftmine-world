# DeepSeek 玩家连续造物流程测试

基线：preview26 / `19a5e153`。本轮使用用户授权附件中的 Flash 服务，在新建 Godot 3D 空白世界内连续创作，保留上一轮已经产生的内容。

## 实际配置与边界

- 通过应用普通配置和会话入口调用 `deepseek-flash`，上下文 1,000,000、最大输出 384,000、最高思考 `max`、有效权限 `auto`；启动和发消息前核对真实配置。
- 不额外设置模型调用次数、token 或整轮时长上限。模型自己选择素材复用、建模与实现方式。
- 独立后台应用、独立档案；不抢占鼠标、键盘、焦点或 Pointer Lock。
- 操作驱动只提交玩家话语、按玩家原始目标回答澄清、观察并通过现有运行接口试玩。不会写数据库、人物位置或存档来伪造成功。
- 原 preview26 ZIP 和原玩家档案保持原样。修复在本次独立工作树实现并重新验收；原失败记录保留。

## 连续请求

| 步骤 | 提交的玩家需求 | 重点观察 |
| --- | --- | --- |
| 1 | 我想要一棵树。 | 实际出现在合理位置，正常游走观察。 |
| 2 | 我想地上有些花草。 | 花草可见，保留树，走动连续，不遮挡整个视野。 |
| 3 | 我想生成一些怪物。 | 合理数量、可见且有对应行为；后续用于战斗。 |
| 4 | 给我一把剑。 | 玩家能使用，能打到怪物，有伤害和反馈，保留已有环境。 |
| 5 | 我想玩怪猎，给我生成一个大怪物。 | 大型敌人和可实际游玩的战斗，不机械缩成静态摆设；按原意回答澄清。 |
| 6 | 给我一把 AK47。 | 能装备、射击并作用于敌人，既有剑/怪物/环境不被意外覆盖。 |

每步区分模型结束、源码检查、实际采用、画面与行为验证。记录真实耗时、模型统计、错误、澄清与恢复；必要时保存和冷开验证内容及进度。编译通过或模型自述完成不能单独判为玩法通过。

## 实测记录（进行中）

独立世界为 `world-71246e2e610e`，会话为 `3a87794a-478a-4cd6-847b-6a8faa90cdf2`。运行的是已交付的 preview26，后续开发中的修复尚未注入本次会话。

| 步骤 | 模型耗时 | 模型请求数 | 累计 token | 实际结果 |
| --- | ---: | ---: | ---: | --- |
| 树 | 118.6 秒 | 7 | 480,088 | 源码检查和自动应用完成；截图可见简单树，普通前进被树干碰撞挡住，保存成功。 |
| 花草 | 193.3 秒 | 8 | 1,203,761 | 4 丛草和 4 丛红花实际可见，树保留；普通后退和保存成功。 |
| 怪物（原包失败记录） | 925.7 秒 | 24 次已观测 / 23 次有用量报告 | 5,221,792（部分统计） | 战斗包安装声明冲突，反复失败；定位确定性包数据问题后，通过普通停止按钮结束本轮，保存原世界，等待修复版继续。尚未新增怪物。 |
| 剑 / 大怪物 / AK47 | 未开始 | — | — | 等待前一步完成或修复后继续。 |

token 为本轮所有请求累计值，包含缓存读取，不代表同时占用的上下文长度；输出/推理 token 和各请求细节保留在原始报告。

怪物原包失败轮包含 51 个工具调用、11 次工具错误，已报告用量中缓存读取 4,316,672、输入 743,181、输出 161,939（其中推理 153,792）。停止原因是已复现的不可安装声明及重复无效参数探索，不是 token、请求数或时长阈值。修复后继续将单独记录，不能算作原包自然一次完成。

已确认的问题：

1. 树已自动应用后，世界页仍显示先前 `GODOT_CANDIDATE_ACTIVE` 错误。实际截图和碰撞正常，普通重新打开同一世界可恢复；这是过期展示错误残留，不能误写成世界运行已黑屏。
2. `cw.module.sandbox-combat` v1 声明了 3 个实体，但只定义一个怪物节点的自动安装方式，安装器正确拒绝 `PACKAGE_SINGLE_ENTITY_DECLARATION_REQUIRED`。公开接口没有“选择包内实体”参数，改位置或分组安装不能修复包数据。模型自行推断并尝试未知参数，又产生参数无效等错误。
3. 模型向玩家输出大量内部标识，已自动应用的流程仍反复建议刷新。已开展提示与工具说明改进，实际效果需更新包后再验证。

原始报告目录：`D:/cm-deepseek-monster-hunter/test-results/desktop-native-product-vJtlTm`。结果按实际发生补充，不预填成功，不把开发者诊断修复混作自然 AI 创作。
