# Ordinary player sample: preserve and edit imported module instances

This is one GU3/GU6/GU7 sample, not a replacement for GA01–GA28. Use the same
sealed `2a584796` client throughout. Bootstrap a fresh protected background
profile through ordinary world creation, two original Kenney building source
imports, actual checks, preview/adoption and `sessionCreate`. Bootstrap is
explicit developer setup with no model calls or direct source edits.

The user-confirmed DeepSeek Flash model and max thinking remain authoritative.
Refresh the existing saved binding read-only, then verify the actual session's
configured result. Do not add model token, request-count or whole-turn limits.
Use ordinary permissions and answer clarifications for the original wish. Each
approval, clarification, failure and any manual intervention must be recorded.

## First ordinary wish

> 把这两栋重叠的房子分开放到我面前，左右各一栋，留出走路和以后放大的空间；保留原来的房子，不要重新做两个替代品，也不要改变玩家位置。

Do not dictate a tool, write the patch from the harness, or fabricate a capture.
After the model stops, inspect its actual changes and real check. Use ordinary
candidate preview/adoption and save. Confirm two independent source entity IDs,
separate positions, preserved shared resources and unchanged player progress.
Record screenshots and actual current scene-object references.

## Second ordinary wish

Use existing bounded semantic look controls to aim at the left building; require
the actual ray/mesh reference to confirm that source instance. The ordinary
`playerPrompt` must obtain its own fresh host capture. Never substitute the
earlier diagnostic reference into its request.

> 把左边这栋房子放大为原来的2.5倍，转90度，变成可以穿过的；只改这一栋，另一栋保持原样，保存后重新打开也要保留。

The model chooses its own tools. Record whether it uses the new typed query or
ordinary source inspection; a successful raw patch does not prove typed-tool
usage. This wish covers three parameters. The wrapper's metadata-only `label`
is not a visible rename and is not silently added as a fourth player requirement.

## Independent acceptance

Require an actual new source check and normal adoption. Read the exact source
values and compare the peer/shared files with their prior hashes. Inspect actual
screenshots, renewed runtime identities and visible geometry. Through ordinary
physics movement, cross a solid wall location on A and show that B's equivalent
wall still blocks. A missed ray alone does not prove absence of collision.

Save and cleanly close, reopen the same sealed client/profile, and repeat source,
identity, image and local traversal observations. Preserve source-only evidence,
visual judgments and local collision results as separate claims. The current
observer does not expose exact runtime Visual scale/rotation matrices; source
values plus screenshots do not become a numeric native-transform measurement.
If that exact measurement remains necessary, record the interface gap and add a
finite independently pinned observer rather than an arbitrary getter/eval route.

No real mouse/keyboard, Pointer Lock, focus changes, user browser or personal
profile may be used. Cancellation must stop the model normally and close the
independent client with the existing shutdown audit.
