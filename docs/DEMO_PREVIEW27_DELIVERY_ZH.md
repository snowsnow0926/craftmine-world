# Demo preview27 验收记录

交付包：[Demo-preview27-5073803f.zip](</D:/Craftmine Releases/Demo-preview27-5073803f.zip>)，1,172,801,235 字节（约 1.17 GB）。完整解压后双击 `START-PLAYER-PREVIEW.cmd`。包内含中文说明、素材清单和 7 份世界模板 ZIP。

SHA-256：`939af0a0128551403522a991f312a491c262eb3996733d6d70c146e679c4f3de`。解压后 8,476 个文件、2,257,773,199 字节逐项一致；所有交付文件及 9,028 个源码/示例 ZIP 成员完成已知附件密钥扫描，没有命中，也没有打包测试用户资料。

应用封包源码为 `5073803f06894f096934a45b8908baff4782eff3`。本报告在封包完成后补充到仓库，不改动已封存的成品。开发分支为 `codex/deepseek-monster-hunter-demo-20260915`，工作树为 `D:/Craftmine Worktrees/deepseek-monster-hunter-demo-20260915`；按项目流程合入本地 main，未推送远端。完整提交清单、测试证据与清理记录位于 `D:/Craftmine Archives/deepseek-reuse-demo-20260915/`，原始玩家测试目录 `D:/cm-*` 保留。未创建 GitHub issue 或 PR。

本次重点验证：让 DeepSeek 在一个真正新建的空白 Godot 3D 世界中，自行检索、读取、复用已有素材，并自动检查、应用，随后实际游玩。全部运行在独立后台资料目录，未占用用户鼠标、键盘或浏览器，没有给模型追加整轮时长、调用次数或 Token 上限。

## 六步连续创作

实际配置：PI 后端、DeepSeek Flash（API 名称 `deepseek-flash`）、1M 上下文、384K 最大输出、最高思考、全自动。六步在同一个世界、同一个会话完成。以下为每轮创作耗时，另有逐步游玩、截图、保存与重开验收时间。

| 玩家指令 | 实际复用内容 | 创作耗时 | 累计 Token（其中缓存） |
| --- | --- | ---: | ---: |
| 我想要一棵树 | Kenney 橡树 | 2 分 16 秒 | 436,507（323,328） |
| 我想地上有些花草 | 4 丛草、4 丛花 | 1 分 36 秒 | 605,923（534,912） |
| 我想生成一些怪物 | 宣传片六只角怪，适配当前地面范围 | 5 分 28 秒 | 2,331,346（2,136,064） |
| 给我一把剑 | 宣传片重刃 | 1 分 36 秒 | 1,426,351（1,371,648） |
| 我想玩怪猎，给我生成一个大怪物 | 宣传片裂岳兽试炼 | 4 分 59 秒 | 827,623（545,792） |
| 给我一把 AK47 | 宣传片 AK47 | 1 分 36 秒 | 716,823（655,104） |

创作累计约 **17 分 31 秒、6,344,573 Token**，其中 **5,566,848 是缓存读取**；这是 44 次模型请求的累计量，不是单次上下文长度，也不是全部新增内容。69 次工具调用没有工具错误。每一步均有真实检查及自动应用记录。

实际游玩确认：

- 树、花草和原宣传片小怪、重剑、巨兽、AK47 均能在实际画面看到。
- 小怪追击并攻击，玩家生命 100→76。
- 重剑实际挥砍，一只角怪生命 60→0，画面计数变为击败 1/6。
- H 进入巨兽试炼；巨兽攻击使生命 100→55；B 返回开战前的准确位置。
- AK47 瞄准连射 4 发，弹匣 30→26、巨兽生命 1600→1504；R 换弹恢复 30 发；3/2 切换重剑和步枪。
- 保存并关闭应用，重新打开后，构建身份、组件身份、生命/击败进度、武器选择、弹药、射击计数和试炼记录保留。

关闭后的只读应用记录还确认：六次正式应用均逐项保留之前的组件状态和玩家状态。移动中的小怪在应用后或冷开后会继续走动；本报告不把稍后采样的位置差异当作存档重置，也不声称抓到了零帧冷恢复时的精确姿态。

## 本轮修复与核对

- 素材库现在自动生成 34 类素材、41 个版本的清单，另保留 4 个完整宣传片世界。默认让 Agent 先查询已有内容与用法，再复用合适资源。
- 宣传片里的树、整片草甸、六只角怪、重剑、巨兽和 AK47 已拆成独立组件。模型文件保持原字节，旧的 35 个版本记录也保留。
- 修复自动应用后的世界显示恢复，以及历史无效安装声明的提示和按钮状态。
- 修复战斗血条、耐力及试炼提示跑到视口外的问题，检查了多种实际视口尺寸与暂停时缩放。
- 修复初始化中断后，已选中的世界没有有效继续入口、恢复意图没有传递、官方加载脚本被误判为修改等问题。对原来失败的同一个空白世界继续准备，最终构建、确认、显示、保存、冷开全部通过，源码和初始化身份未改写。
- 修复已知隔离检查环境日志被再次投影成“未知错误”的问题。只接受绑定到真实执行器证据和日志哈希的分类，原始日志和真正未知/脚本/断言错误保留。
- 试炼开战短提示不再写死重剑操作，实际按键以当前武器的常驻提示为准。
- 修复角怪和巨兽保存朝向时矩阵与角度往返转换造成的微小误差。保留严格状态检查，不放宽比较容差；原失败源码及完整存档复现后，最小修复通过严格比较、两个独立冷启动及 384 项 Godot 检查。
- 保存运行中的世界模板时，先通过应用确认暂停并取得稳定存档；保存、失败或取消后，只释放本次保存产生的暂停，保留原有玩家暂停及退出、候选应用等保护。严格源码和存档身份检查不变，旧失败操作仍按同一次操作重试；需要重新读取时明确提示先取消旧操作。

## 上下文与服务故障

此前一轮请求在官方返回 HTTP 200 后等待约 13 分 44 秒，只收到推理前心跳，没有实质内容或工具调用；玩家报告服务故障后已停止并保存。该次用量未知，不能记作零消耗，也不算素材复用成功或失败。

玩家报告服务恢复后，重新开始以上完整六步。第五步发生一次正常完成的整理，没有重复压缩循环。当时 1M/384K 配置正常；旧版本在新任务首请求丢失了上一轮的测量缓存，退回保守估算 557,257，超过 521,859 的整理阈值。上一轮最后真实输入是 299,425；557,257 并非实际输入量。

最终包已将测量缓存与任务授权区分：同一会话和世界下，仍须核对实际请求前缀、模型、系统、工具、权限和新增内容；预算和工具授权继续使用当前任务完整身份。无法核实时仍回退，不移除真实上下文容量保护。离线同一重建请求估算由 551,029 降到 316,842；原请求正文未完整留存，离线重建不冒充原 API 实测。

额外实测保留了完整的失败与修复过程：第 7 轮仅改开战提示，却在严格检查中发现角怪恢复朝向有约 1.49e-8 弧度差异。该检查失败、模型重试与随后主动停止的记录保留。第 8 轮由实际 DeepSeek 在同一个旧世界中按最小修复修改两个恢复函数，检查通过并自动应用为修订 11；实际文件哈希与已验证的最小修复一致，原来的生命、击败记录和弹药保留。

随后第 9、10 轮分别只读询问换弹和重剑操作，均完成。第 9 轮与修复轮的任务代次不同，系统按设计回退保守估算；第 10 轮在新的任务身份下成功复用已核对的测量前缀，首请求估算 123,681，采用 `measured-whole-prompt-exact-prefix-plus-utf8-half-tail/1`。该次实际总量 117,941，减去输出 1,236 后输入含缓存为 116,705。这证明跨轮优化已在真实服务调用中生效。本次正例使用同一个世界构建，没有据此声称任意构建变化都能复用测量；1M/384K 配置与真实容量保护不变。

## 可直接复现的存档与限制

`examples/deepseek-six-step-playtested.zip` 由应用自身的保存模板、导出 ZIP 流程生成。导入并创建副本即可继续试玩，无需重新调用模型。存档保留试玩进度，不是空白世界，也不保证任意新创意都能一次成功。

已通过普通模板入口创建另一个世界 `world-a256ef707e46`，原世界保留。副本的原生画面、全部 11 个组件、玩家状态、生命、击败和弹药进度核对通过；活跃小怪的后续移动姿态不作精确一致声明。测试驱动曾在切换期间过早读取旧会话，保留原 097 失败记录；改为等待世界真实绑定与界面一致后，普通重开确认了新空会话 `8575e46b-0857-42e5-958a-4ebebb736b80`，没有再次创建世界或调用模型。

最终收尾的 `3cda9d17` 候选包暴露了另一条必须修复的竞争：模板保存成功，但周期自动保存清除了 checkpoint 缓存，导致发布结束时没有释放自己产生的暂停。直接从运行世界打开素材库、保存并关闭后，物理帧数在 23:43:56 至 23:44:20 UTC 保持 8921；该失败保留，不能写成恢复运行通过。修复以实际暂停意图和实例归属判断，后续最终包复测另列。

最终 `5073803f` 封包复测通过：保持世界视口，普通打开素材库、保存模板 `player.world.31f0786c54ce4b7999e4b2365ed9f6eb`，约 9.1 秒完成，再通过普通关闭表单返回。没有切换完整工作台、调用额外恢复接口或发送模型请求。关闭后在 23:50:35 与 23:51:03 UTC 连续读取同一实例，物理帧数从 3064 增至 4797，证明世界在关闭后持续运行。随后普通保存并退出。修复保留手动暂停、候选应用、切换实例和退出保护；46 项相关回归通过。

六步完整实测使用源码提交 `beb55787` 的封包；后续旧世界修复与两条只读问答使用 `ad50fb66` 封包，旧世界里的朝向修复由 DeepSeek 实际完成。最终交付包源码提交为 `5073803f`，内置素材也包含已验证的朝向恢复修复，以及诊断提示、跨轮估算、开战短提示和模板保存修复。后续任务单独记录，不与原六步用量混算。

测试资料目录为独立的 `desktop-native-product-NUWiAc`。真实创作窗口及重开窗口没有页面错误或输入/焦点违规；测试驱动最初配置服务后立即退出的启动阶段，保留一条 `host-core is unavailable` 的关闭竞争记录。它不计为模型或素材错误。原故障、测试驱动误用捕获入口等记录全部保留。

这是未签名的 Windows x64 免安装预览包；未声称完成独立干净 Windows 或连续真人操控验收。界面操作手感、录制效果和不同新创意仍欢迎玩家实测。
