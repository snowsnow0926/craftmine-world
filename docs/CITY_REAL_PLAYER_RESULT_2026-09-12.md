# CITY 普通玩家实测结果（2026-09-12）

结果：**已生成、检查、正常采用并重开一个可步行的奥格瑞玛风格城市原型，取得真实截图。** 主街、要塞斜坡及一段侧向路线经过实际物理移动验证。画面是低细节体块搭建，亮青色底座地面仍在，不能宣称完整原作地图或 1:1 复刻达标。

## 本次真实输入与产品配置

- 输入原文：**我想复刻完整的奥格瑞玛，进入城市里走动探索。** 没有技术提示、实现答案或机械首项选择。模型确认用此目标覆盖旧受限诊断中“只做入口”的模拟选择；本轮没有新澄清题。
- 复用 CITY 原隔离世界 `world-fa9771a1b259` 与会话 `1d24450f-6955-4444-abd4-6e8bb56af9c8`。通过正常世界界面结束已过期旧任务、保留草稿后提交新玩家输入，没有重放旧模型任务。
- 固定成品：`D:/cm-promo-loop-0912/desktop/build/releases/32cd879d364d-c0acf0e3-b793-467e-9dcb-1b8572953512/output/win-unpacked`。
- 包清单 SHA-256：`db710608e91eed54274bd66341482cba3bcf1518d0d822f6f10e80052e113d25`。
- 实际玩家配置：`deepseek-v4.1-flash-expires-on-0910`、思考 `max`、上下文 1,000,000、模型输出配置 384,000，完整图像/文档能力与 `inherit` 权限配置。走普通会话/工具流程，不启用 CREATION_EVAL，不加测试用 token、模型次数或整轮时限，不读取旧评测账本计费。普通产品既有保护保持原样。

## 实际完成过程

1. 模型生成 `scripts/orgrimmar.gd`，挂载到主场景。包括主街、正门、要塞、四谷区域体块、城墙、旗帜火把及飞龙塔结构。首次工具调用在顶层多传 `expectedHash`，模型自行纠正。
2. 第一次真实检查因两处 GDScript 类型推断错误失败；模型读取错误后自行修复，第二次检查通过，六个固定运行时断言通过，`sourceStale=false`。测试 agent 没有修改生成源码。
3. 共审阅并单次批准 4 个 medium 操作：两次本世界源码 patch、两次对应 revision 的 check；均通过正规权限桥返回 `allow-once`，没有改 auto 或全批。
4. 模型正常结束后，用现有采用脚本正常预览、采用、保存、冷重开。同世界、同正式构建、新实例检查通过；采用和后续探索额外模型调用为 0。
5. 第一轮普通 look/walk 共 244 个动作 physics ticks，从 `(0,0.90,6)` 沿主街到约 `(-8.99,2.60,-8.66)`，水平净位移约 17 米，实际爬上要塞斜坡。第二轮 184 ticks 从平台回到主街再走向侧边建筑。两轮只用真实控制/物理，不改玩家位置，不注入场景。

正式构建：`gbd-811a429f5f2e8e871119f369b7f61263563acd649096d494173f3ee66a686c4c`。检查任务：`gjob-55415346ac57c63aaca4147380a19f2505f7942ee8a2df3179442b72f3ea9862`。

## 截图与证据

证据根目录：`D:/cm-promo-city-retest-0912/test-results/desktop-native-complete-eokghq/`。以下路径相对该目录；原始产物保留在本地，不纳入源码提交。

| 文件 | 含义 |
| --- | --- |
| `player-90ea3139-93bd-45da-a371-38ad1f69879a.json` | 普通玩家本轮原报告，配置、输入、权限回执、检查与真实用量 |
| `adoption-77fc4da5-0396-4c5d-bb18-27ad8fa6a076/report.json` | 正常预览采用、保存和冷重开证据 |
| `adoption-77fc4da5-0396-4c5d-bb18-27ad8fa6a076/reopened.png` | 已采用城市冷重开画面 |
| `exploration-5cf27218-4473-4a2d-8348-7a87272ec12b/view-1.png` | 推荐展示：主街面向要塞，能看到区域标签与建筑 |
| `exploration-5cf27218-4473-4a2d-8348-7a87272ec12b/report.json` | 主街行走与登坡前后真实位置 |
| `exploration-60dda98b-c61a-4cba-acf9-6b56090988c4/view-1.png` | 推荐展示：从要塞平台回看主街和正门 |
| `exploration-60dda98b-c61a-4cba-acf9-6b56090988c4/view-3.png` | 侧向建筑体块、旗帜与火把 |
| `exploration-60dda98b-c61a-4cba-acf9-6b56090988c4/report.json` | 下坡和侧向行走证据 |

已目视上述实际截图。两次探索最后还保留了贴近建筑表面的画面；这不是专门碰撞测试，不能据此判断全部室内是否可进入。尚未逐一走完各谷、飞龙塔及所有边界。模型最后也提到暗影裂口等区域仍可后续补充，因此“完整城市”的原始愿望还存在内容、布局和美术差距，不能只靠区域标签或检查通过抹平。

## 用量与隔离

北京时间 2026-09-12 03:48:10 开始，03:58:30 结束；普通模型任务 metrics 用时 605,152 毫秒，11 次请求均返回，`status=completed`、0 pending。输入 257,239、缓存读取 1,502,208、输出 103,463（其中 reasoning 74,406），聚合 totalTokens=1,862,910；总数含缓存读取，不是新增付费输入或费用结论。

普通报告 `stateIntegrityVerified=true`；采用及两轮探索均 `ok=true`。各次退出审计 `violations`、`pageErrors`、`shutdownFailures` 均为空。没有真实鼠标键盘、前台焦点或 Pointer Lock，没有裸引擎运行或编辑受测成品/模型作品，没有操作其他 profile。本次提交只有中文结果记录。
