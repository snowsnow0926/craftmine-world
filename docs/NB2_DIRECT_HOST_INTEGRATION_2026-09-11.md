# NB2 生产直接编辑集成记录

日期：2026-09-11。本工作树：`D:/cm-nb-host`；分支：`codex/creation-next-host-20260911`。

## 实现

接通三个正式 RPC：直接编辑、读取操作状态、读取可撤销正式历史。宿主创建真实持久任务，
固定目标和编辑意图，调用原有工具/check/候选采用后结束任务，不启动模型。
正式历史依据导出源码 contentOid 读取并复核字节/hash，拒绝工作稿冒充；采用边界复核
当前查看会话、世界、捕获、任务、候选、要求哈希和最新源码。

集成 NB2 的操作阶段持久化、真实目标属性透传和 UI 丢回复修复。重启读取需当前会话/世界
授权；中断进展若实际已被 core 采用，只读确认原 job/candidate/formal build 后恢复为已采用，
不重做操作。语音/任务浮层仍使用既有世界通知补读。

评测器 ground 导航修为多个固定角度逐一实际命中地面才成功；砍树交互前校验真实射线
确实选中树，避免固定视角在造树后一直命中旧物体。

## 验证

首轮生产集成 13 项通过：真实 React 属性组件 → 主 frame RPC → 实际源码检查/采用 →
删除 → 撤销同 ID → 保存退出 → 新进程继续，完整位置、背包、领奖账本和尺寸保持。
两个 Electron 进程退出码均为 0；退出审计无输入、窗口、页面或关闭失败。
真实用量账本最后直接编辑任务 `calls.observed=0`，不是用假模型返回值模拟零请求。

首次测试在删除采用的正常切换阶段读取运行快照遇到 `WORLD_BUSY`，测试程序错误地立即
退出并在任务仍关闭中强停。已只修测试等待逻辑，对此暂态有界重试；保留失败来源，不把
这一轮失败当成功。第二轮 13 项完整通过，记录见
`docs/evidence/next-batch-20260911/direct-host/production-ui-report.json`。

加入持久进展后，扩展的 17 项真实宿主/UI 验收全部通过，两个进程正常退出：再次验证原流程，
新增刷新真实属性仍为 2 倍及指定颜色、模拟阶段记录停留 applying 后跨进程只读核对已采用、
错误世界拒绝和核对过程零模型调用。正式源码、core 与存档未做故障修改，只有已关闭进程的
私有操作阶段记录被设置为 applying，原操作编号保持不变。
完整报告与属性面板截图见 `docs/evidence/next-batch-20260911/direct-host/persistent-production-ui-report.json`
及同目录 `persistent-production-ui.png`。截图已目视确认实际属性和选中对象可见。
纯服务与正式历史/候选 guard 共 13 项通过，桌面 TypeScript 与 production build 通过。
所有窗口独立、headless/offscreen、不可聚焦；无个人麦克风或浏览器操作。
