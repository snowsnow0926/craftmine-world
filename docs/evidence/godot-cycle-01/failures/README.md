# 第 1 轮 Godot 失败记录

这些目录保存了成功前的原始运行结果，未将失败从本轮记录中删除。

- `godot-headless-CxzgYV`：首次编辑器导入检查失败。该次日志收集器还存在覆盖问题，因此导入日志仅剩断言错误；不能把它当作完整引擎输出。
- `godot-headless-CA969d`：修复日志留存后再次运行，保留了独立用户目录下系统文件夹查询失败的原始输出。
- `godot-headless-jTLCEW`：导入通过，3D 运行时退出报告一个未挂载网格节点泄漏。
- `godot-headless-a3rLNP`：3D 通过，2D 完整重启后首次物理重叠读取为 false。

修复完成后的独立运行结果为上级目录 `godot-headless.json`，共 13 项检查通过。所有样本均为固定作者工程，没有调用产品 LLM，也没有显示或激活测试窗口。
