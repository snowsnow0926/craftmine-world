# 持续输出不再被单次 120 秒硬截止中断

真实普通玩家会话记录：`D:/cm-promo-flight-clarified-0912/test-results/desktop-native-complete-BKI6gF/player-8119b35a-21ab-48fe-9f84-d37fd59d70bb.json`。使用玩家实际的 v4.1 / max 配置，累计 token 不限。该次运行墙钟 120617 毫秒，assistant 状态 aborted；思考长度 90508 字符，输出 token 估计 22627，响应耗时 119630 毫秒。此处仅保留统计，不复制思考原文；运行没有产出飞机。

原因是 `craftmineGuardedStream` 从准备开始设置一次 120 秒计时器，即使持续收到思考或源码参数也不续期。

现改为 120 秒无进展超时。非空 text/thinking/toolcall delta 必须伴随相应累计内容增长或工具参数变化才续期。text/thinking 只比较对应块的字符串长度；当前锁定 PI 的工具流比较 `partialJson` 长度，不逐 token 序列化整条消息。没有该工具流字段的适配器仅比较当前工具参数。连续输出相同字符只要累计长度增长仍算进展；空增量、启动事件和重复未变的内容不续期。

准备阶段、等待首次增量和后续真正停滞仍能超时。用户取消仍立即生效。不增加整轮时长、模型请求或 token 限制，不更换模型。无进展超时作为 `PROVIDER_IDLE_TIMEOUT` 错误展示并记为未知用量，不再伪装成用户取消，也不自动重发同一请求。用户取消仍为 `TURN_ABORTED`。迟到响应不能转为成功或派发工具。

验证使用假时钟：思考、正文和工具参数各持续 300 秒仍成功；同字符追加正常续期；空/重复事件不能维持停滞流；准备及首响应停滞超时；取消立即结束；正常请求只预留和结算一次，停滞不伪造已知用量或自动增加模型请求。没有运行新产品或付费模型请求。
