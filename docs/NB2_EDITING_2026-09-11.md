# NB2 对象编辑开发记录

日期：2026-09-11；分支 codex/creation-next-edit-20260911；独立工作树 D:/cm-nb-edit。

已实现：真实 3D 选中边框与对象中文名称；真实节点位置/尺寸采样；直接尺寸和颜色编辑面板；明确删除；不回滚存档的操作级撤销；宿主有限编排服务，真实调用现有操作/检查工具并只在通过后采用。

删除/撤销沿用源码 CAS、幂等回执、实体 ID 预留和占位保护。旧日志保留，新日志增加最多八个实体的 before/after。存在任何规则脚本的删除保守拒绝并说明依赖，环境/规则/旧无 inverse 操作不声称可撤销。

## 本分支证据

- 28 项操作编译、宿主编排、目标解析测试通过。
- 真实 Godot 规则检查通过，新增金色选框/名称和实际节点尺寸断言；证据 test-results/guidance-rule-sTOBAL，原生与导出运行仍保留普通自定义规则状态。
- 真实 React 组件六项 headless 检查通过，取消不写、尺寸颜色请求与 opaque capture、撤销原回执、无输入/焦点请求；证据 test-results/creation-edit-ui-LbjgEp。
- 真实原生引擎“宝箱领奖→删除→重开→撤销→重开再次交互”通过，背包、玩家位置、领奖历史保留，恢复同一 ID 后返回 already-opened 且无重复奖励；证据 test-results/edit-progress-GLNC7N。
- 五个 workspace 包构建与 desktop TypeScript 检查通过。

本分支服务通过依赖注入复用生产工具；main/index.ts 的正式 IPC、durable turn 与要求冻结接线由总控负责，与 NB1 一并集成。上述 fixture 服务和 UI 结果不冒充完整生产链；合入后仍需真实 candidate/application/进度重开验收。测试未操作用户浏览器、鼠标或麦克风。


## 非预置对象行为接口补充

根据 NB1 真实需求验收发现，旧 tree/rock 不可互动且规则声明强制关联门。新增通用 entity-behavior 声明、已有实体的唯一所有权、E 事件分发、实际 visible/solid 观察与受状态投影保护的恢复。具体掉落/计时/重生逻辑未写入底座或指导模板，仍必须由模型普通源码实现。

23 项编译器/静态契约检查通过；旧普通双按规则经真实引擎/PCK回归通过（guidance-rule-MicSZu）。新增纯 presence 切换夹具通过真实 E 射线、隐藏碰撞观测、站在已隐藏对象位置恢复、拒绝实体重现时与玩家重叠、拒绝 restore 与投影不一致并回滚、新进程完整恢复，证据 test-results/entity-behavior-M42t6L。首轮夹具对作者0.9与Godot float32实际位置直接相等而失败，保留原日志；改为比较真正操作前capture，未放松运行时判定。


## 独立集成审查修正

发现真实 target service 丢弃 runtime 的名称、尺寸和颜色，导致UI夹具虽通过、真实参数却可能回到默认。已改为从同次采样中唯一匹配目标 ID 的实体提供编辑值，拒绝错ID、重复实体和非法参数；真实service反例通过。

发现UI状态仅按session保存、请求回包前不忙碌，以及宿主状态只有内存。已补session/world共同分区、提交前原ID锁定、丢回复查询同ID、晚回复不倒退、持久阶段和重启中断状态；原操作结果不明不再次写入。新owner读取要有主host已验证的session/world，状态落盘失败标待核对而不是已采用。8项服务检查（含重启、重新绑定和落盘故障）、9项真实React检查（含同world切换、同帧双提交和丢回复）通过，UI证据 test-results/creation-edit-ui-fljEQu。主index授权及持久目录由NB3集成。

删除依赖拒绝仍保守：普通脚本虽然声明entityIds，但持有world引用可访问其他对象，不能仅凭声明宣称删除无依赖。另已向NB1报告普通放置判据需要校验真实visible/solid，以及受测world.observe可修改的信任边界，交其要求验证实现处理。


## 可编辑观察接口的真实反例

根据总控要求，单独生成测试候选：creation.json和真实Node3D的树保持1倍尺寸，真实Body碰撞关闭；仅修改可编辑的creation_world.observe，报告2倍与有碰撞。实际Godot Web完成导入、导出、load、resume后，生产readGodotCreationObservation及godotCreationMatches在loaded/running两阶段均通过“放大2倍”的冻结要求，同一helper收到实际节点值则拒绝。

证据 test-results/observation-forgery-GtbD5G/report.json 包含实际引擎打印、原始观察、源码SHA及导出日志。探针 tests/creation-observation-forgery-probe.mjs 默认确认旧缺口；修复后可设置CRAFTMINE_EXPECT_FORGERY_REJECTED=1反向验收。此证据覆盖生产语义helper路径，不把它扩大为完整GodotBuildVerifier/候选采用已通过；未调用模型或触碰个人数据。

修复建议已交总控：将需求实体事实采样放在受保护并锁定哈希的adapter中，直接读取真实节点、碰撞与材质；不委托可编辑world.observe提供“证明”。普通entity-behavior源码继续可开发，不能靠禁止所有玩法脚本规避此问题。


## 导出后保护脚本与入口独立核验

在真实固定引擎中加入仅测试用EditorPlugin，导出前保护源码均等于原PIN，插件在导出期间分别修改base_adapter源码、修改project.godot的adapter入口。两种导出命令本身均正常完成，但实际PCK不再等于受信源码/入口；新增纯Node读取器分别以CREATION_PACK_PROTECTED_MISMATCH和CREATION_PACK_SELECTOR_MISMATCH拒绝，正常真实导出通过。证据 test-results/creation-pack-export-wb26gb，8项检查。

实现已接入生产godot-executor的stageArtifacts之前，预期值来自core claim；不改模型源码、不注入额外文件、不改core结果schema。6项解析器边界/别名/伪造checksum反例通过；执行器协议41项通过，1项宿主不允许文件symlink的历史测试保持skip。范围为creation-sandbox Web候选，其他底座不受新格式门禁影响。

对NB1的新保护adapter进行独立Web复验，实际错误节点仍是1倍且无碰撞，但可编辑world.observe伪造的2倍/有碰撞已被忽略，生产愿望helper拒绝；5项通过，证据 test-results/observation-forgery-Ropelo。第一次反向探针仍要求收到旧伪造值，因新adapter返回真实值而失败；随后仅修正测试的反向模式预期，保留失败报告，未修改产品。
