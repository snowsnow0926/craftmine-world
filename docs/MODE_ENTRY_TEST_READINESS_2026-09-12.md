# 模式入口自动验收的就绪状态（2026-09-12）

成组提案预览驱动发现：入口 DOM 出现时，世界列表的异步读取未必完成。此时正常点击沉浸模式会进入选世界页，不能把它当成已进入游玩；仅等待 `.craftmine-play` 会卡住。该次没有调用包安装，源码仍为原 revision 3，不能归因于成组安装失败。

私有 headless `primaryMode` 的只读结果新增 `playWorldId`，直接读取已有按钮的 `data-active-world`。世界选择页的原“进入世界”按钮补相同 `data-mode/data-active-world`，没有修改 `canEnter`、禁用状态或 `onClick`。后续驱动可等待所需世界确已就绪，再调用真实入口；不会更改私有 store 或强设布局。

旧冻结包的补验可通过限定于独立主 renderer 的 DOM 读取等待同一属性，再调用其正常 React handler。测试不向用户浏览器或 OS 发送输入。失败运行和强制关闭后的非干净退出证据保留，不写成成功；新的驱动还需逐调用记录和可取消等待。
