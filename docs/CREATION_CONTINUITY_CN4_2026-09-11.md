# CN4 检查与采用交接开发记录

基线 `073e3364` 的真实读取 helper 在 core 已通过、宿主仍采用时立即返回：等待时间 0，未读取采用状态。证据 `test-results/continuity/completion-baseline.json`。这是受控时序复现，不据此改写此前 HOLDOUT01 失败原因。

执行器现复用原 ledger 记录同作业的采用阶段，先登记 pending 再提交检查结果；读取工具使用既有最多 30 秒总等待时间等待该实际采用结果。检查通过与采用结果独立返回，超时仍是 pending，未知记录不猜成功。记录不是授权，不延长回合，不放松取消、世界、源码、候选和进度检查。

独立审查补充了空 context 恢复、落盘期间取消及 finish 回复丢失三项边界：恢复检查可正常生成候选但不申请采用；采用前复查取消和停止；丢回复仅读取原作业并记录真实原因，不自动重放。重启未完成记录保留为中断。UI 根据同作业真实宿主采用状态显示正在采用，以正式构建身份判断已采用。

初始 helper 与记录测试 32 项通过；宿主及实际执行器协议/原生验证在本轮集成中补齐。最终报告会列出实际执行结果，不把本文件的待验收项视为通过。
