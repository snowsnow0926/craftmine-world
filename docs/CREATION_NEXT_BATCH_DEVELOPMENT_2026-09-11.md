# 下一批持续开发记录

Goal计划：CREATION_NEXT_BATCH_PLAN_2026-09-11.md；基线c1660f12/preview.10。

## 已集成与当前验证

NB1冻结造物要求并接入真实候选检查、Rust证据匹配和自动采用；对象/尺度/数量/颜色/时间、顺序门与有限采伐规则分别验证。NB2通用entity-behavior接口支持普通脚本，直接编辑、删除及内容撤销已接入主机任务。NB3语音取消任意语言回退，任务卡读取持久状态。NB0已补齐旧反馈与preview.10真实交付映射，原始记录保留。

首次真实模型使用项目已授权DeepSeek凭据。原默认deepseek-v4.1-flash-expires-on-0910已不在服务models列表，测试选用实际可用的deepseek-flash，不修改个人设置。CA01由模型实际生成一棵树，生产检查和正式采用均通过；用了16次请求，其中多数为编译期间反复读取。最初评测runner错误地用worldNavigation保存，引发PERMISSION_DENIED，保留原始失败：test-results/desktop-native-complete-73fuxT。修复为既有worldPanel保存后再验，不将该脚本失败冒充模型内容错误或抹去。

根据实测，模型godot_build_read改为固定上限30秒的宿主等待（上下文、取消、真实身份仍校验），原始结果与后续对照分开。首轮基线16次；后续修复对照采用同一40次上限的独立轮次，开始前冻结输入、模型与检查版本，旧失败与修正原因保留。费用未知不记录为零。

独立审查发现并修复：capture未传真实编辑属性导致属性可能被默认值重置；编辑UI状态未按世界隔离；提交丢回复后可能生成新operationId；编辑状态需持久化并在重启时标中断而不自动重放；最新任务需要按session过滤。真实Model任务、无模型direct编辑与引擎作者夹具分别记账。

外部验收：真实麦克风/玩家手感未由自动测试替代；未发现可用的干净Windows虚拟机入口，保留该环境验证，不在个人安装上执行安装/升级/卸载。

## 后续实测与修复

CA01已在原测试档案的独立副本中完成零新增模型补验：保存、完整退出、重开后原模型产物、碰撞与所有进度一致。前后模型用量仍为16次，预算请求ID与原始档案文件哈希不变；原始runner失败仍保留。详见CA01_ZERO_MODEL_RECOVERY_2026-09-11.md，不将补验计成新模型成功样本。

实际引擎反例证明可编辑world.observe能够谎报尺寸与碰撞。现在受保护的适配器直接读取节点、网格材质和物理形状，core校验三个检查与传输脚本及固定入口；导出后再独立解析PCK，核验其中的实际脚本与project.binary入口。正常导出通过，导入期@tool替换脚本或入口的两个反例均拒绝。源码和导出包两层分别验证。

较密集的顺序门夹具揭示两个检查时序问题：第一个候选站位可能与另一标记重合，以及碰撞体的set_deferred更新需要物理帧。检查现在对明确的站位重叠尝试下一合法方向，并在互动后等待实际物理帧再判断。采伐检查同步采用相同的落点恢复处理；不放宽错误顺序或无条件开门的断言。

真实导入与导出的attempt账本继续保留起止时间，新增运行检查/自动采用阶段计时并在重启后保留；源码操作记录捕获、索引、读取、存档身份检查和原子修改阶段。scripts/report-creation-model-comparison.mjs只从已有报告与账本生成比较，不发模型请求，也不覆写原报告。修复轮源码与检查器不同，不能声称同条件整体提速。

统一入口为node scripts/verify-creation-next.mjs；--quick执行逻辑、宿主与运行时测试，完整入口还包括独立UI、真实引擎、PCK篡改、生产执行器及直接编辑测试。模型测试独立运行并另记预算。现有276项基础测试已通过；最终集成结果见下文收尾记录。

本轮真实模型在重开后调用creation_operation和godot_build_start时碰到“工具不存在”，经ToolSearch重新激活后才成功；这属于每轮延迟工具目录重置造成的多余请求。已把宿主实际注册的基础造物、检查、事实、能力和指导工具保持在世界任务基础目录。两轮请求与两次实例重建的实际运行时测试通过，未注册工具不加入，工具执行权限没有放宽。旧的通用project_inspect/capabilities_read误读Godot数据也已按真实运行时分流，并通过实际Rust只读接口验证。

## 最终工程验收与有限模型结果

最终快测249项通过、1项跳过；原14阶段真实集成证据链通过，复制会话UI9项及真实生产链19项补验通过，已知preview.10底座迁移19项通过。保存恢复故事33项流程检查、232项原生断言、13个原生进程和5个底座物化通过。源码、编译产物和原始报告身份记录在docs/evidence/creation-next-final-20260911/。原聚合失败、重试和测试同步修正均保留，没有重写原报告。

最终模型轮原25次加续测15次达到共同40次上限：CA01/02/03/06首次通过；CA05已生成、修复和采用普通采伐脚本，但runner冻结后未恢复，原结果失败，独立零请求补验16/16。CA07发现产品复制世界未建立新会话，已修复并真实验证；模型在副本采用新树后，runner因会话自动改名重开失败，改用稳定ID和真实配置核对后零请求补验15/15。HOLDOUT01最后3次请求耗尽剩余预算，未完成改色，保留失败。

首次16次、第一修复轮40次和最终共同40次共96次实际模型请求、13次案例执行。补验不计新样本，已知表达变体不当作未见保留集；不据此宣布普通玩家成功率或同条件整体提速。真实麦克风、玩家与干净Windows生命周期仍单列待外部验收。

中文交付总表为CREATION_NEXT_BATCH_DELIVERY_2026-09-11.md。所有模型进程已结束，已归档本轮7份test-results目录、合并本地master并清理8个自有工作树；其他任务、个人配置和既有Windows成品保留。本轮不包含远端推送或新安装包。

最终独立文档复核纠正CA04名称为设置时间（原话“把时间设为18点”）。机器状态中的旧cycle 5 Godot记录标为历史快照，历史用量数字保留，当前完成状态统一指向CREATION_NEXT_BATCH_STATUS.json。
