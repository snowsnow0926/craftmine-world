# FB02 全部闭环目标执行记录

用户明确要求把九项反馈与逐项审计作为 goal，全部完成后提供复盘报告。目标已在本任务建立；此前审计和 preview.16 成品作为基线保留。

## 工作分工

- 主控：集成、地面维护恢复、原生输入与退出验证、实际玩家配置/普通模型流程、最终包及复盘。
- `goal_workspace_entry`：工作台视图保持、设置返回、世界二级选择/创建去工作台/纯对话生成入口。
- `goal_auto_creation`：可信全自动策略、异步检查与持久采用、失败恢复；保留身份、候选检查和存档事务保护。
- `goal_result_loading`：所有进入路径的加载/错误恢复、对话结果卡、预览采用与返回、原列表预览入口。

## 工程与成品验收清单（8e8ec534 已完成）

当前产品 `0.14.4-preview.17`／`8e8ec5348d94e0323f5135518e76ade750eb44cb` 的九项工程修复、修订 ZIP 验收和验收后字节复核已完成，详见 [正式复盘报告](FB02_PREVIEW17_FINAL_RETROSPECTIVE_2026-09-12.md)。下列勾选表示工程完成，各用例的逻辑、页面或成品层级以正式报告为准；没有物理键鼠或玩家前台焦点亲测，新版真人确认仍待复测。报告与证据随本次文档提交归档；goal 完成状态以本任务系统记录为准。

- [x] FB02-001：首次/切换/新建加载可见；失败后重试或返回可继续。
- [x] FB02-002：系统全屏、Esc 暂停/设置/返回同世界、正常退出及保存失败恢复。
- [x] FB02-003：主界面/插件/Godot快捷键、可见对话、焦点owner交接及返回；严格标注无物理输入的证据边界。
- [x] FB02-004：稳定二级选择；创建后工作台；纯对话生成；取消/返回/最终进入世界。
- [x] FB02-005：首条发送、附件/粘贴、流式输出和结果出现时世界保持可见，历史会话隔离仍成立。
- [x] FB02-006：旧stock世界实际升级；被创作打断后同实例空闲恢复；旧草稿/历史/存档保留。
- [x] FB02-007：按真实global/session auto授权贯通普通创作、检查、正式采用和结果反馈；无需额外开关/权限/手动采用，失败可安全继续。
- [x] FB02-008：对话中可见检查结果、准确候选动作、预览/采用/返回/切目标世界；重进会话可恢复。
- [x] FB02-009：原列表按钮到预览、退出不改变正式世界、采用后保存重开、失败及过期候选恢复。
- [x] 干净提交构建最新 Windows 包；完整校验 ZIP 解压载荷；实际运行解压后的程序验证。
- [x] 逐项复盘与证据完成并随本次文档提交推送 GitHub；任务系统的 goal 在推送确认后由总控完成。

## 验证约束与模型基线

所有验证在独立隐藏/不可聚焦进程、独立profile进行，禁真实鼠标键盘、Pointer Lock、置前和操作用户浏览器。原玩家profile只读备份，不能以删除旧世界或重置设置消除问题。

只读核对用户截图所示“生成博美犬”会话 `7eed797a-61a3-4a01-9e9b-94d950d8fa7b`：模型 `deepseek-v4.1-flash-expires-on-0910`，思考 `max`，权限 `auto`，产品模型绑定 contextWindow 1,000,000、maxTokens 384,000。这是玩家现有产品配置，不额外削减；不使用旧 `CRAFTMINE_CREATION_EVAL` 的次数/时长限制。凭据仅由已授权本地秘密配置读取，不打印或写入报告。

准备证据：`test-results/fb02-goal-player-baseline/`。当前用户程序未运行，未声称读取实时前台选择；模型来自与反馈一致的保存会话。

## 中间集成记录（历史保留，不是最终交付验收）

下列文字保留各中间阶段的当时判断，包括“此时仍在补齐”“尚未完成”等；修订包已完成的最新状态以上方清单和正式复盘为准。

2026-09-12 后续兼容修复：原生接触证明正常落地误差约 1.029 毫米，已修复胶囊底部支撑的数值边界，墙体／斜坡／天花板穿透保护保留。`docs/evidence/fb02-floor-contact-20260912` 保存当前 10 项新物理回归、17 项原碰撞回归、三个真实 Web 导出及完整存档重建证据。加载失败不再因轮询无限重建窗口；含候选取消恢复的 50 项 host 生命周期检查通过。

`desktop-native-collision-migration-4tYIRe/collision-migration-report.json` 用 `TVYVZj` 故障存档副本完成真实冷恢复、正式文件仅替换精确 guard、主草稿和历史保留、完整存档原值保持、加载／错误界面恢复，以及两次正常启动退出。服务／事务 67 项、页面 41 项亦通过。`4FIDrT` 中测试误把合法 applied 引用更新当成历史丢失的记录保留；修正为验证旧 applied 是新 applied 的祖先，其余引用原值保持。

此时仍在补齐“从另一个健康世界切换到旧 guard 故障世界”的受控兼容迁移入口，以及 selected／failed／无实例状态的只读查询。上述冷恢复通过不能替代跨世界路径或最终修订 ZIP 的验收，goal 尚未完成。

后续跨世界补齐：`desktop-native-collision-migration-7bzM6e/collision-migration-report.json` 六项通过，两个原生启动正常退出。从普通入口新建健康世界后，再选择原旧存档，先保存健康世界完整进度，自动完成目标兼容迁移并进入；目标源文件仅 guard 改变，原存档、主草稿和历史保留，再次保存重开通过。相关协调器 39 项和类型检查通过；selected／failed／无实例仅开放准确状态的只读查询，未放宽应用权限。此轮产品源码冻结后重新构建 Windows 包，最终 ZIP 验收与总报告仍由总控完成。

`188e65eb` 已提交并推送，版本号预留为 `0.14.4-preview.17`。该提交保存本轮集成开发，不表示 Windows 成品或全部真实模型流程已经通过。最终检查结果须在全部交付后另行记录。

- `desktop-native-candidate-FOdVEY`：原候选列表→预览→返回→采用→保存重开→地面维护→再重开，正式后继正确；其余 20 个文件、存档和 main 草稿保留。
- `desktop-native-candidate-J8xoAB`：维护被预览打断→返回同一实例→空闲自动恢复。
- `adoption-lineage-core-gB9jO0`：实际 JS 私有路由→Rust 的 session-only latest 查询及身份边界；实际 Git 后继、历史候选采用状态。
- `desktop-native-fb02-EFYHkk`（开发构建 4）：旧世界副本加载进度条截图、实际绿色地面、系统全屏；四条软件输入路径 F2/Esc、暂停恢复、保存退出。无真实物理键盘测试。
- `fb02-goal-root-recovery-final.log`：41 项退出、取消、调度及捕获恢复检查；后续修复续作专项已扩展为 8 项。
- `fb02-goal-precommit-tests.log`：52 项相关修复、取消、队列、旧观察器升级检查通过。

本轮连续运行找出的额外断点及处理：

1. 创建 ACK 遗失真实初始化状态，前端过早进入聊天。已透传完整状态，并从真实世界列表再次核对、等待实际 runtime。
2. `godotBuild.latest` Rust 支持 session-only，但私有 JS 路由仍强制 worldId。已跨层修正并跑实际 core 集成测试。
3. 新固定控制器模板的 `scene_mesh_picker_v2.gd` 重复父类常量，真实 Godot 编译失败。已使用 v2 独立常量名，保留原数值；更新精确资源哈希。`fb02-creation-base-compile-t3zPbs` 六组配置、共 12 次实际编译/场景运行通过。
4. 自动修复只完成构建、未发起检查时会停在 failed/manual。已让同一可信意图继续普通修复，保留已完成消息；不把 build 当作 check，不跨越玩家的新要求或取消。
5. 新纯对话入口主界面读取 runtimeState 被导航白名单拒绝。已补窄只读路由，继续验证实际新建流程。

失败记录同样保留：`desktop-native-ordinary-player-9FOJ9Z` 是模型调用前的新模板编译错误；`7Kp9Fo` 是模型调用前的主界面路由拒绝。它们不代表玩家模型能力失败，也没有换模型或用脚本生成作品冒充模型结果。工作台测试曾把未附着的后台 verifier 页面当成正式世界，`9NGC1W` 中已保存反例并修正测试为核对实际 native owner。

## 843 候选工程验收与新阻断（历史保留）

本节记录 843 候选被拦下时的状态，不是当前产品状态；失败未被改写，后续修复和最终验收见末节。

- 当前候选提交：`843bdd15a8b2ac09da8ccc2c08326b5724d74c6c`；版本：`0.14.4-preview.17`。真实门玩法保存重开的 `PLAYER_PENETRATION` 待修复，不能将该身份标为最终完成包；修复后的最终身份由总控提供。
- 该候选 ZIP 已真实解压运行。独立目录 `test-results/desktop-native-fb02-ttPt5L` 的 [工作台报告](../test-results/desktop-native-fb02-ttPt5L/workspace-chain-package-report.json) 与 [新建／取消／继续报告](../test-results/desktop-native-fb02-ttPt5L/world-create-chain-package-report.json) 均通过、正常退出 0，三组审计错误数组为空；两条链串行执行，测试进程均已退出，原玩家 profile 未改动。
- 成品核对了二级入口、F2/Esc 软件路由及可见反馈、同一正式实例的设置返回、真实附件会话物化后世界保持、纯对话黑色覆盖和真实 formal 截图、取消后原世界恢复与草稿保留、明确继续后首次加载完成、进入及保存退出。软件事件／原生回调验证没有模拟物理键盘、抢占真实输入或激活测试窗口，因此新版真人确认仍未取得。
- 开发中发现的初始化 ACK、资源编译、主界面只读路由、占位取消、结果历史自动弹开、会话 ACK 暂态和未选中草稿恢复问题均纳入工程修复；原失败记录保留。最终成品测试另保留了取消完成等待条件过早的反例，修正的是测试同步点，成品内容未改。
- 逐项工程和真实模型证据、最终 ZIP 校验值、交付路径与整体复盘由总控汇总至 [最终复盘报告](FB02_PREVIEW17_FINAL_RETROSPECTIVE_2026-09-12.md)。本追加只保留已通过的局部工程证据，不宣称保存重开、整体成品或 goal 完成；最终报告／交付项继续待总控收尾。


## preview.17／8e8ec534 最终工程完成追加

- 产品提交 `8e8ec5348d94e0323f5135518e76ade750eb44cb`，版本 `0.14.4-preview.17`，产品已推送 GitHub；正式报告已发布为 [FB02 最终复盘](FB02_PREVIEW17_FINAL_RETROSPECTIVE_2026-09-12.md)。
- [最终验收摘要](assets/fb02-preview17-acceptance/final-acceptance.json) 归档 11 份实际解压包原生报告、16 次启动，均正常退出且三组退出审计错误数组为空；[字节复核](assets/fb02-preview17-acceptance/final-package-byte-recheck.json) 确认验收后 1,656 个载荷文件与包内源码、版本、产品提交一致。
- 最终包完成旧存档冷恢复、健康世界到旧故障世界的兼容切换、原候选预览采用及维护恢复。原模型 max/auto 普通创作实际 16 次调用，无人工权限决定或人工采用；随后实际引擎走动、门开闭与保存重开完成，整份保存快照保持原值。旧 843 失败、此前测试同步误判和 preview.16 缺口继续留作历史。
- 四路软件 F2/Esc、设置前后同一正式实例、附件会话世界保持、纯对话真实截图和新建／取消／继续整链均由同一最终解压程序通过；没有真实物理键鼠、置前、焦点抢占或 Pointer Lock，不能登记为新版真人已确认。
- 工程、成品验证与正式报告均已完成；本次收尾提交包含完整文档和证据，goal 状态以任务系统记录为准。
