# FB02 修复进度（2026-09-12）

本文件记录 FB02 真人反馈进入开发后的代码映射和当前状态；原始反馈与截图保持在 `USER_FEEDBACK_BATCH_002_2026-09-12.md`。

**最新结论：全部要求尚未闭环。** 本轮逐项审计又确认首条发送丢失世界面板、设置返回改变游玩偏好、地面升级中断后不重试等断点；完整全自动和对话结果入口也未贯通。当前逐项状态与必须补齐的验收见 [全部要求闭环审计](FB02_REQUIREMENTS_CLOSURE_AUDIT_2026-09-12.md)，下文保留历史修复记录。

## 真人复测纠正

用户确认使用 `2dc7748e` ZIP 后，加载画面、全屏、F2/Esc 和地面颜色仍未改善。以下早期记录描述的是工程改动，不代表玩家验收通过。FB02-001/002/003/006 重新打开；当前实测与交付结果以后续运行审计记录为准。

## 早期工程改动（保留历史，不作为结案证据）

### FB02-009：预览副本 `INVALID_GODOT_CANDIDATE_ACTION`

根因是 Godot 候选列表 UI 使用 `candidate.id`，而候选协调器要求稳定的 `candidateId`。列表可能同时带有旧的行 ID 和候选 ID，点击按钮时传错身份就会在 IPC 严格校验处失败。

修复：新增 `godot-candidate-view.mjs`，优先使用 `candidateId`，仅对旧记录回退到 `id`；按钮、DOM 数据属性和预览调用统一使用同一个身份。新增 `tests/godot-candidate-view.test.mjs` 覆盖新旧记录和非法身份。

### FB02-001：Godot 载入黑屏缺少状态

修复了 Godot Web shell 的加载页视觉反馈：加载期间显示高对比度背景、旋转指示器和“正在准备场景和运行资源，请稍候”文字；运行时错误仍会替换成错误信息。世界面板同时增加了加载状态层，便于在原生视图尚未接管前显示状态。

### FB02-003：重启后 F2 快捷键可能未激活

发现沉浸快捷键的激活条件依赖本次渲染会话的 `modeChosen`，已保存为游玩模式时重启仍可能保持关闭。现在持久化的游玩模式会恢复快捷键监听；原有焦点、输入法、重复按键和原生子视图边界保护保持不变。

### FB02-008：工作台内显示生成结果操作

预览控制条现在也会挂载在工作台世界视图中，提供“应用到世界”和“返回原世界”操作。沉浸模式仍会自动打开轻量预览层，工作台模式不会因为轮询到候选而强制弹出沉浸层。

### FB02-007：全自动模式下的内置项目补丁

全局权限模式为“全自动”时，内置 `plugin_craftmine_world_*` 工具族现在会忽略旧会话残留的 `ask` 覆盖，直接按全自动执行，不再弹出玩家权限卡。放行范围只包含保留的内置 Craftmine 插件 ID；其他插件、Shell 和外部工具仍按原权限策略处理。候选检查、事务校验、取消、回滚和正式应用的数据一致性边界仍然保留。

### FB02-006：第一个世界的地面亮度

已通过附件截图识别到具体对象：左侧当前选中的“第一个世界”，副标题为“Godot · 规划中”，对应 `creation-sandbox` 底座。截图中的浅青色地面来自 `creation_world.gd` 的地面材质与环境光叠加。

已将地面颜色从 `#6e9580` 调整为较深的 `#4f6b5a`，环境光能量从 `0.7` 降到 `0.55`，并同步更新指导目录与源文件哈希。需要重新打包后由真人确认亮度是否舒适。

## 早期验证记录（历史版本）

- Godot 候选身份回归：1 项通过。
- F2、Esc、F11、原生 Godot/插件快捷键测试：24 项通过。
- 桌面端 typecheck 通过。
- 根目录 `npm test`：304 项通过，0 失败。
- 插件重新构建成功。

## 仍待确认或开发

- FB02-002：preview.16 系统全屏、暂停/恢复和正常保存退出有成品证据；新审计确认设置入口会持久切到 create，设置返回不会恢复原游玩模式。必须修复该返回闭环，不能仅按按钮存在结案。
- FB02-005：已定位首条发送创建会话时切到空工作面板，世界只保留在 HOME 上下文；附件/部分粘贴同样会创建会话。已用现有纯函数复现，不再要求用户补充信息作为修复前提。普通逐字符输入与首条发送应区分。
- FB02-007：用户已明确要求内置 Craftmine 创作流程全自动，授权问题已澄清。最新审计确认仍有独立的每世界自动应用开关默认关闭；目前只解决工具免权限卡，尚不能称完整零操作。后续应贯通并验证实际完整流程，无需再次询问同一权限期望。
- FB02-004：二级选择只在没有可玩当前世界时出现，创建后跳工作台和仅对话生成没有完整实现；仍是用户既有要求，不能以“后续产品建议”视作完成。
- FB02-006：指定旧世界正常迁移、绿色地面与存档保留有成品证据；新审计确认维护被玩家工作打断后同一实例不再重试，恢复逻辑必须补齐。
- FB02-008：控制条只在已经打开预览后出现；生成检查完成本身不会给工作对话提供应用入口。必须补齐候选就绪到结果卡的前半段。
- FB02-009：候选身份修正存在；原列表按钮到预览、采用/返回、保存重开仍需成品连续验收。

最新成品证据与尚未完成事项统一见 [preview.16 保留世界与 Windows 交付审计](FB02_RETAINED_PACKAGE_AUDIT_2026-09-12.md)。

## 当前包边界

早期改动已进入 `09e152aa`、`2dc7748e` preview.15 便携包，但用户复测未通过，不能继续据包内文本存在而宣称问题解决。最新修复需有实际旧世界启动、可见加载层、快捷键反馈、全屏和迁移证据，并使用独立版本号发布。
