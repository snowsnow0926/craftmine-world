# 首次自主创作：首轮实现与验收记录

日期：2026-09-13。对应[中文方案](FIRST_INDEPENDENT_CREATION_PLAN_2026-09-13.md)与[首次使用指南](FIRST_CREATION_QUICKSTART_2026-09-13.md)。

## 已实现

- 在原 PI Desktop 素材详情中加入“直接使用”：读取要求 → 检查并准备加入 → 玩家确认加入世界。保留原有 AI 加入／修改和世界模板入口。
- 使用确定的素材版本与内容哈希，支持素材默认位置及受支持的三维坐标调整。原始模型不会被冒充为可直接安装的玩法组件。
- 复用原生源码安装、构建检查、进度保护和正式采用，不连接模型，不创建 AI 对话，不改写对话输入框。
- 操作可跨面板关闭和应用重开恢复。前端只保存操作定位信息，结果向主进程核对，不自动重放。明确区分已采用、取消、失败与保留草稿。
- 补齐过早状态查询、同编号重试、早期取消、并发互斥、旧世界／旧源码校验和公开错误脱敏。
- 更新当前版本入口，准备新玩家评估包和逐人记录模板。

## 验证结果

桌面 TypeScript 检查与生产构建通过；33 项后端／协议测试、19 项真实 React 表单验证通过。最终原生验收完整通过，使用独立离屏进程和新测试档案，没有真实鼠标键盘、窗口置前或 Pointer Lock。

原生记录：`D:/Craftmine Archives/first-creation-20260913/integration/test-results/desktop-native-direct-library-QMl18W/report.json`。
界面记录：`D:/Craftmine Archives/first-creation-20260913/integration/test-results/direct-library-ui-XLrn75/report.json`。
报告保留原始工作树路径，迁移映射见归档目录的 `LOCATION.json`。

实测经过真实 PI 新建世界表单和素材详情操作，连续安装同一博美两次，取得不同实例；同编号重试没有重复安装。关闭面板并返回、完全退出并重新打开应用后，正式构建、两个对象、操作历史和玩家物品均保留。原始博美模型被明确拒绝直接安装，原 AI 入口仍存在。另一个世界中刚提交就取消的树木操作保持取消状态，没有进入正式世界；随后可返回原来的双博美世界。

| 操作 | 准备及检查 | 确认后采用 | 两阶段合计 | 模型调用 |
| --- | --- | --- | --- | --- |
| 第一次加入博美 | 34.9 秒 | 17.7 秒 | 52.5 秒 | 0 |
| 第二次加入博美 | 38.9 秒 | 15.6 秒 | 54.5 秒 | 0 |

这些数字只代表本机此次运行的两个组件操作，不是整个首次使用过程或所有内容的速度保证；没有加入玩家理解界面、思考和决定的时间，不能与从零生成完整世界直接等同比较。

模型文件 SHA-256 保持 `1ab9f354598df75504b061fb06e1e5e386bd59c878afcec3bad8388832dadd0b`。玩家控制脚本在该轮所有构建中的 SHA-256 均为 `557e0e834c726f1b54d59eb260d6147c8b4a8400fdc5386a447c964cc4063955`。
主进程构建 SHA-256：`778599b88a7f2ca6ffc4907c441fabccf537d047fb46af33da82b816574a7302`。

早期诊断全部保留：`3JrkLb` 发现界面历史只存于会话；`nG10P0` 发现过早查询尚未持久化的操作；`VTokzn` 是主动取消的旧构建诊断；`yeZIgK` 与 `4Z99LD` 分别因测试脚本的字段名和空组件字段假设失败。已修复相应产品问题和测试脚本，最终 `QMl18W` 独立从新档案完整通过。没有修改失败报告、伪造存档或把局部结果改记为整轮通过。

## 交付与后续

开发分支：`codex/first-creation-20260913`；工作树：`D:/Craftmine Worktrees/first-creation-20260913`。方案翻译提交 `ce92789b`，主进程集成提交 `9b55378e`，最终验收脚本修正提交 `45e70353`；子任务原始提交已通过合并保留完整历史。按项目流程合入本地 `main` 后清理本轮工作树，原始验收材料迁至上述归档目录，不推送 GitHub。

已交付的 Windows preview.21／`b8900d63` 原样保留，本轮是源码开发验收，尚未制作包含新功能的 Windows 成品。源码测试使用本机 Core 二进制，并非运行旧成品来冒充新功能验证。演示世界与已认可的素材归档不变。

约 5–8 名新玩家和干净外部 Windows 电脑的测试仍待真实执行；本轮没有招募、代填结果或对外发送消息。参数编辑、模板跨玩家分享和自然语言创作继续使用已有流程，本轮没有重新运行所有历史功能或宣称真人体验已通过。下一步是打包这个版本，让真实新用户按评估包完成首次创作，再据阻碍与阶段耗时优化摆放交互和等待体验。GitHub 默认分支仍为 `master` 的发行入口问题已记录，未修改远程设置。

