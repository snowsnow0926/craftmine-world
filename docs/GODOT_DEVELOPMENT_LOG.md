# Godot 多底座循环开发进度

目标来源：[开发计划书](GODOT_MULTIBASE_DEVELOPMENT_PLAN.md)。用户于 2026 年 9 月 9 日明确授权设置 Goal 并开始循环开发。当前 Goal 为 active，没有 token 总预算，也没有恢复旧定时任务。

## 当前阶段

| 阶段 | 当前状态 | 已有结果及仍需完成的工作 |
| --- | --- | --- |
| GD0 接入选型 | 进行中 | 固定 Godot 4.7.2，原生逻辑与实际多线程 Web 导出/中央 React 呈现通过；操作系统隔离初始化失败，原生 Electron 接线及完整测量仍待完成 |
| GD1 界面 | 核心布局已实现并验证 | 世界居中、右侧对话、独立宽度保存、窄窗和游玩切换已接入；世界列表的底座标签、辅助入口整理和最终新引擎画面验收后续完成 |
| GD2 工程创作接线 | 待实施 | 工程文件、受管理作业、PI 工具、Rust 版本与应用事务 |
| GD3 真实模型武器创作 | 待实施 | 本轮 3D 工程是固定作者样本，不能替代这一阶段 |
| GD4 俯视底座 | 待实施 | 本轮物理与商店探针不等于完整底座或小镇交付 |
| GD5 横版与玩法分支 | 待实施 | 横版基础、类银河城、可挖掘沙盒分别推进 |
| GD6 作品与连续创作 | 待实施 | 兼容复用、转换副本、长任务、历史草稿接续 |
| GD7 Windows 交付 | 待实施 | 新引擎尚未进入发行包；现有第 7 批交付保留 |
| GD8 高阶能力 | 待实施 | L3 部件与 L4 策略按独立评测推进 |

## 第 1 轮：界面与原生引擎验证

日期：2026 年 9 月 9 日。实现起点为 `65daadd`，来自已同步的本地 `master`。专用分支为 `codex/godot-cycle-01-20260909`，专用工作树为 `D:/cw-gd-20260909-01`。没有操作历史工作树、玩家存档或正在使用的程序。

实现提交：

- `213bba7`：中央世界与右侧完整对话，原生边界同步、独立宽度偏好、窄窗排列及规格/ADR/验收场景。
- `409e9ab`：固定版本引擎、下载与完整性核对、对应许可文件、3D/2D 固定工程与实际 Godot 进程验证。

本轮通过 `76c192a` 快进合并到本地 `master`，没有推送远程。合并后逐项核对了证据清单中的哈希，并将已验证引擎缓存保存在 `desktop/build/godot/4.7.2-stable`。未创建新发行包，玩家正在使用的程序没有被替换或重启。

本轮工作树登记及分支已移除。Git 清理返回 `Directory not empty`；随后针对 `D:/cw-gd-20260909-01` 的 PowerShell 递归清理被自动审批以 `blocked by policy` 拒绝，未给出更具体原因。保留该残留目录，不重试清理、不复用。下一轮在新的独立工作树中继续。

默认对话宽度为 400 像素，可在 360 至 640 像素之间调整，双击分隔线恢复默认。进入 1100 像素以下的世界工作区时自动收起导航，并保留重新展开入口；760 像素以下上下排列。游玩模式使用同一个世界视图，返回创作时恢复对话。其他 PI 工作面板继续使用原有布局。当前左侧仍保留完整项目/会话导航，尚未替换成按底座组织的世界列表。

Godot 使用官方 `4.7.2.stable.official.ed1daf0bf`。编辑器压缩包为 86,013,866 字节，主程序为 180,858,888 字节。下载体积不是最终客户端包体增量。工具链核对压缩包 SHA256、解压文件以及版本匹配的 LICENSE/COPYRIGHT；实际运行前将主程序复制到独立测试目录，使用独立编辑器数据、游戏存档与临时目录。

验证结果分别记录，避免将不同证据相加后当作完整产品验收：

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| `pnpm build:js` | 通过 | 完整 JavaScript 构建；保留原有大分块提示 |
| 桌面 `tsc --noEmit` | 通过 | TypeScript 检查 |
| 面板状态、宽度、呈现单元检查 | 22/22 | 原有组件与状态逻辑回归 |
| [实际 React 布局](evidence/godot-cycle-01/react-layout.json) | 15/15 | 会话数据和原生视图传输为夹具，包含实例保持、边界和深浅主题 |
| [原有游玩布局回归](evidence/godot-cycle-01/legacy-layout.json) | 10/10 | 保留普通资源面板、会话、偏好与错误数据处理 |
| [原生 Electron 生命周期](evidence/godot-cycle-01/native-lifecycle.json) | 17/17 | 真正的 Electron、PI/Rust、旧世界导入、保存失败重试及完整重启，全部离屏 |
| [实际 Godot 引擎](evidence/godot-cycle-01/godot-headless.json) | 13/13 | 固定作者工程的导入、运行、物理交互和重启保存；没有调用产品 LLM |

Godot 3D 样本检查相机挂点、装备 UI 状态、真正的物理射线命中、弹药和目标血量。2D 样本通过 CharacterBody2D 移动到 Area2D 商店范围后购买，范围外拒绝购买，并核对金币和物品。两个工程均在完整结束并重启 Godot 后保留进度。

无窗口 Godot 使用 64 × 64 的虚拟渲染表面。本轮准星测量仅证明 Control 覆盖当前虚拟视口，不能当作屏幕画面或 GPU 性能验收。单次样本中，3D 和 2D 导入分别约 2.23 秒、1.94 秒；这是当前开发机已有系统文件缓存的观测，不能作为新机器冷启动或最终创作耗时保证。

已保留的失败与修复：

1. Windows PowerShell 下载路径改用无 HTML 解析的下载模式并增加超时，避免后台准备挂起。
2. 首轮日志收集器在断言失败时覆盖原始输出，已改为追加错误。后续保留了具体引擎日志。
3. 独立 USERPROFILE 缺少 Desktop/Documents 等目录，导致编辑器目录查询报错；补齐测试目录后导入通过。
4. 3D 示例预先创建后被替换的网格节点未释放，导致引擎退出报泄漏；改成单一挂载节点后通过。
5. 2D 重启后的物理重叠状态尚未稳定，首次读取为 false；保留正常物理更新并等待初始化后，恢复检查通过。

失败记录见 [Godot 失败记录目录](evidence/godot-cycle-01/failures/README.md)。没有通过放宽数值或忽略引擎错误取得通过结果。

截图：[桌面深色布局](evidence/godot-cycle-01/desktop-dark.png)、[桌面浅色布局](evidence/godot-cycle-01/desktop-light.png)、[窄窗口布局](evidence/godot-cycle-01/desktop-stacked.png)。截图来自独立测试实例，未控制用户正在使用的浏览器。

## 第 1 轮后的入口（历史）

继续 GD0：准备匹配的导出模板，将同一批固定工程导出到 Web，在独立 headless 页面中验证载入、渲染、状态桥接和再次构建。对照原生呈现的实际接入条件，记录功能、等待时间和包体开销；普通独立窗口不能替代中央嵌入。

同时确定导入、工具脚本、构建和运行的操作系统隔离边界。在完成这一层之前，固定作者探针不能升级为允许产品 LLM 任意生成工程并自动运行的入口。之后再推进 GD2 的 PI/Rust 工程事务与真实模型创作。

继续执行时先核对本地最新提交、Goal 状态和工作树，不复用已结束轮次的工作目录，也不重复执行历史禁止的输入测试。尚未完成的 GD0–GD8 工作继续由当前 Goal 推进，不将本轮检查通过视为整份计划完成。


## 第 2 轮：Web 实际呈现、世界状态与隔离原型

日期：2026 年 9 月 9 日。起点为 `e56ff66`，专用分支为 `codex/godot-cycle-02-20260909`。权限切换后，经精确作用域工具审批，将本轮工作树从 `D:/cw-gd-20260909-02` 移入 `D:/Craftmine World/test-results/worktrees/godot-cycle02`。旧的第 1 轮残留目录未操作。迁移导致 pnpm 的绝对依赖链接失效，离线重建后继续。

实现提交：

- `15124da`：固定模板提取、实际 Web 导出、世界/构建/会话通信、分区存档、完整恢复校验、保存后退出，以及真实 React/Godot 验收。
- `0e282b5`：独立 Rust AppContainer 可行性原型及失败记录。编译通过不等于隔离通过。

本轮通过实际渲染定位并解决了测试采集时序：逻辑尺寸正确时，Godot 的旧绘制帧仍可能出现在截图中。最终等待实际画布尺寸与绘制更新，并在无文字的左侧导航区域采样 30 点，保留了之前逻辑通过但图像不正确的报告。

| 验证 | 结果 | 适用范围 |
| --- | --- | --- |
| `pnpm build:js` | 通过 | 实际桌面构建；本轮未改桌面产品组件 |
| [真实 Web 与 React](evidence/godot-cycle-02/web-preview.json) | 33/33 | 真实引擎、场景、物理与画面；会话及原生视图传输是夹具 |
| [MessageChannel 回归](evidence/godot-cycle-02/transport.log) | 21/21 | 原生端口传输、世界身份、畸形数据、超时、退出和并发边界 |
| [原生 Godot 回归](evidence/godot-cycle-02/native-headless.json) | 13/13 | 原有实际物理与完整 Godot 进程重启保存 |
| [AppContainer 编译](evidence/godot-cycle-02/appcontainer-build.log) | 通过 | 独立 Rust 原型，锁定离线依赖 |
| [AppContainer 运行](evidence/godot-cycle-02/failures/appcontainer.json) | 未通过 | 两次子进程均在 main 前以 0xC0000142 退出，尚未执行边界断言或 Godot |

3D 验收看到真正的中心准星，枪挂在相机下，切剑后图像与攻击状态同步；实际射线将目标血量从 50 改为 38、弹药从 6 改为 5。重新构建 GDScript，将伤害 12 改为 7后，继续从原进度攻击得到血量 31、弹药 4。俯视样本通过物理移动进入商店，并检查金币/物品变化。坏状态在任何赋值前被拒绝，同一运行器仍能继续返回快照。

持久化验收包括关闭并重启整个独立浏览器进程；同一来源、同一底座的不同 worldId 也使用不同存档路径。并发购买、保存、退出按顺序完成。生产中稳定的逐世界来源、主机进度事务、崩溃/配额/写入失败仍未验收。这里没有把“FileAccess 写入成功”单独当作耐久提交证明。

选型改为同版本 **多线程 Web 候选**。单线程模板的退出任务组错误被保留；没有屏蔽它来取得通过。多线程页面补齐跨源隔离与资源策略后，在同一套验收中正常退出。详细原因、测量和边界见[选型记录](GODOT_INTEGRATION_DECISION.md)。

本轮独立审查发现并修复了存档没有包含世界身份、异常响应可能悬挂、保存与退出竞态及坏存档可能部分赋值的问题。该审查与 Rust 隔离原型由一个子 Agent 在明确的文件范围内协作；主线程负责实际 Web 验证、审查及集成。

[最终截图](evidence/godot-cycle-02/desktop-godot.png)、[俯视截图](evidence/godot-cycle-02/desktop-top-down.png)、[失败记录](evidence/godot-cycle-02/failures/README.md)、[证据哈希](evidence/godot-cycle-02/manifest.json)。本轮产品模型调用为 0，没有新发行包，没有替换玩家当前客户端，没有真实输入或焦点请求。

## 下一轮入口

继续定位 AppContainer 子进程初始化失败，并验证文件、网络、子进程、非交互界面和 Godot 导入/导出边界；不把固定可信 probe runner 接成任意模型工程运行器。可并行推进 GD2 的工程/构建/进度数据模型和 GD1 尚未完成的世界导航。

随后接入真正的 PI/Rust 工程工具和原生 Electron 世界视图，再让真实产品模型完成准星、持枪和装备需求。GD3–GD8、旧世界兼容、作品复用、长任务、完整性能测量和 Windows 发行仍属于原目标，不因这轮通过而缩减或标为完成。

合并状态：本轮通过 `e8d903e` 快进合并到本地 `master`，合并后核对了全部 27 份证据文件的哈希。没有推送远程。本轮工作树登记和分支已移除；Git 首次清理留下依赖及仓库副本，经核对精确目录后，仅删除 2,504 个链接本身，再清理剩余目录，未跟随链接目标。本轮目录已不存在，历史残留目录均未操作。


## 第 3 轮：真实工程源码管理和受限启动诊断

日期：2026 年 9 月 9 日。起点 `4d787a2`。主工作树 `test-results/worktrees/godot-cycle03`（分支 `codex/godot-cycle-03-20260909`）；独立 Rust 工作树 `test-results/worktrees/godot-cycle03-core`（分支 `codex/godot-cycle-03-core-20260909`）。本轮继续当前 Goal，没有新增自动任务或替代原计划。

Rust 实现提交 `7a926fb`：复用 tasks.sqlite、世界/会话绑定和写租约，新增 Godot 源码工程头、不可变版本和操作回执。UTF-8 源码按 SHA-256 独立落盘，物理世界目录同样按 worldId 哈希，避免 Windows 保留名称及大小写碰撞。原子补丁验证版本、清单和文件哈希；失败不推进工程头。超过旧 2 MB JSON 限制的源码可保存。当前单文件 4 MiB、单清单 64 MiB，模型工具仍限制总输入 180,000 UTF-8 字节；累计历史空间另需配额和清理。

PI 插件接入 godot_project_create、godot_project_index、godot_file_read、godot_project_patch。世界、会话、回执身份来自主机。源码只保存为 source-only，不能调用构建、执行、候选应用；新工具和模型提示均明确此状态，旧 DSL 检查不能证明 Godot 编译成功。源码中“first-person/top-down/side-view”是工程意图信息，不代表三个可玩底座已发布。

| 验证 | 结果 | 适用范围 |
| --- | --- | --- |
| 完整 Rust 领域回归 | 69 通过，1 未验收 | 含 12 项 Godot 源码检查；Windows junction 夹具未能创建，明确 ignored |
| 新源码工具实际 stdio 联测 | 6 组通过 | 真实插件源码与新 Rust 二进制、独立数据库；未调用产品模型 |
| 现有 Agent 上下文测试 | 17 通过 | 模型/账本夹具，非真实推理 |
| 实际插件构建、pnpm build:js | 通过 | 新工具目录、Agent 与桌面源码构建；非发行安装包 |
| Rust 隔离探针编译 | 通过 | 私有桌面和有界新进程加载诊断 |
| 隔离运行 | 未通过 | 仍在 main 前 0xC0000142；边界断言和 Godot 都未运行 |

联测确认完整进程重启后先拒绝自动继续（EXPLICIT_RECOVERY_REQUIRED），再由既有主机 task.resume 恢复，随后读到原 Godot 工程版本；界面选择 beta 时原 alpha 任务仍写入 alpha。历史源码不被后续补丁覆盖。Rust 回归还覆盖真实 SQLite 回滚、坏 blob、坏清单/头版本、世界 a/A 独立目录、旧 turn 与基线冲突。

隔离诊断的五次新尝试均保留原始日志：命名窗口站被 OS 拒绝、独占系统命名窗口站已存在、目录 Low 标签写入被拒，以及私有桌面成功后的相同初始化失败。最终实现只读取 Medium 标签，不再依赖不必要的标签改写。加载事件仅缩小观察范围，根因未证明；没有关闭隔离限制来取得成功。临时 AppContainer profile 与桌面句柄全部清理，没有操作用户输入、焦点或默认桌面权限。

Windows junction 测试的环境失败、Unicode 测试起点错误、恢复流程遗漏和构建日志路径错误均有明确记录，没有把准备失败当作通过。[证据与范围](evidence/godot-cycle-03/validation.json)、[原始失败记录](evidence/godot-cycle-03/failures/README.md)。本轮没有重跑第 2 轮 Web/渲染检查，没有新增发行包，没有替换玩家客户端，也没有推送远程。

后续继续 GD0 的普通用户执行隔离与真实导入/导出，GD2 的构建作业、二进制素材、源码备份、进度与应用事务、原生 Electron 视图和真实模型创作。GD1 世界导航、GD3–GD8 的底座/内容/复用/长任务/发行与高阶能力仍在原 Goal 内。


独立审查还修正了两处问题：新增 Godot 指令不能顺便改变旧审查读取的 craftmine.request/2 数据格式标记；原审查夹具 2 项回归已通过。新增源码操作遇传输错误时，只查询原 binding/调用/请求的回执；提交后丢弃响应并结束 turn 的故障注入确认不重复写入。提交前失败且没有回执时保留原错误，不声称成功。这两项加入后，最终工具验收为 6 组，使用生产 CoreClient 默认的 5 秒请求超时；不等同于全部原生 PI 生命周期或任意网络故障验收。


本轮实现提交：`7a926fb`（Rust 源码事务）、`94ae19c`（PI 源码工具）、`68c8bb2`（未知结果回执恢复与协议兼容）、`d9ce808`（隔离加载诊断及失败证据）。本轮实现已通过 `5e5f756` 快进合并到本地 `master`；合并后逐项复核了 17 份证据的大小与哈希，没有推送远程。


第3轮清理：独立 core 工作树/分支已安全移除。root 工作树 `D:/Craftmine World/test-results/worktrees/godot-cycle03` 的清理在进程创建前被自动审批拒绝；审核理由是包含约 2,418 个链接和约 5 万个文件的整树/分支清理，需要用户明确授权。没有执行该删除，没有重试或改用间接方法，目录及已合并分支保留。该目录不复用，历史残留同样不动。此清理事项不阻断当前 Goal 的后续开发，下一轮使用新独立工作树。


## 第 4 轮：并行开发接手、全面审计与集成修复

日期：2026-09-09。起点 46739d2。用户要求查看子 Agent 进度、审计并继续开发。
独立分支 codex/godot-integration-20260909 保留合入 A–H 原作者历史；
随后由三个独立审查/修复树与 root 完成构建/快照、运行生命周期、底座存档和预检修复。

当前实际新增入口为左侧真实世界列表、体素空白创建和保存后切换；原生验证还发现
接口成功而实际行未刷新，已补主机通知并保留原截图作为验收缺口证据。
核心限制修正覆盖约39.5MB的实际WASM，仍保持源码限额；完整正式进度与候选新鲜度
在应用时校验。页面自证Godot应用入口关闭，回环网络与真实执行器未完成前不开放执行。

完整Rust102通过/1历史忽略、真实PI broker12、原生客户端25、导航23、
C宿主夹具17、A库9、E/F/G作者工程52/40/80、存档专项5组、F一致性2项通过。
JS构建和严格TS通过。预检自测35通过，但真实发行资产清单有5项失败；
导出/安装包未提供，不能计为通过。无真实模型调用，没有新安装发行包。

详见[GODOT_PARALLEL_AUDIT.md](GODOT_PARALLEL_AUDIT.md)、
[验收摘要](evidence/godot-cycle-04/validation.json)和[证据哈希](evidence/godot-cycle-04/manifest.json)。
后续继续原GD0–GD8目标：OS回环边界、A/B适配、完整状态与C真实接线、
模型创作、复用/长任务、权利清单和独立Windows交付。Goal保持active。
主目录另一个任务的许可方案、README和总计划改动保持原样；未重试历史清理拒绝。


本轮已快进合入本地 master a8b920d；逐项复核27份证据的大小和SHA256，
A–H八份原HEAD及各修复提交均为master祖先。README、总计划及许可策略三个无关工作区文件
合并前后哈希完全一致，没有推送。底座修复工作树/分支已清理；core审计清理返回
Invalid argument，目录/登记保留；C审计实体已删除，Git元数据清理返回相同错误，
登记/分支保留，均未强删或换方式重试。根集成树在最终文档合并后执行普通Git清理。

## 第 5 轮：完整底座进度、真实宿主接线与隔离证据修正

日期：2026-09-09。起点 `2f84da9`。root 在 `D:/cm-gd5-core` 的
`codex/godot-cycle05-core` 继续当前 Goal，并集成 host、bases、sandbox 三个独立子任务。

Rust 新增完整原生进度、正式产物解析、候选描述符和耐久回执；真实 Electron 宿主接线，
三个作者底座实现加载/暂停/保存/恢复。root 完整产品回归发现私有 broker 漏了 Godot
方法，已修复。E 时间戳漂移、F/G 状态恢复和 G 编辑器退出泄漏也完成修复。

本轮 Rust 111 通过/1 历史忽略，完整客户端 25、第一人称真实 Godot/Electron/Rust 9、
三底座进度 20 组、E/F/G 作者工程 52/40/80 通过；完整 TS 与 JS 构建通过。
客户端 25 项使用旧世界，F/G 当前是 Godot headless 验证，不能宣称产品三底座创建已验收。

修正前一轮网络误判：普通 AppContainer 的 TimedOut 且无 OS 错误号不代表成功。
新 LPAC+registryRead 原生 TCP 探针明确返回 10013，同策略固定 Godot 导入/导出退出 0。
编辑器脚本网络只给出通用错误，因此完整门禁仍退出 1；产品执行器尚未开启。
真实发行预检 8 项失败，发行导出和安装包跳过。真实模型调用为 0。

见[完整报告](GODOT_CYCLE_05.md)、[验收范围](evidence/godot-cycle-05/validation.json)、
[证据清单](evidence/godot-cycle-05/manifest.json)。所有失败保留，原始字节已归档。
下一轮继续可信进程/策略凭证、真实执行器、候选协调和产品三底座创建，再做真实模型验收。
GD0–GD8 范围不变，Goal 保持 active；无真实输入、焦点或 Pointer Lock，没有推送和安装。


本轮实现与验收已通过 `adda86c` 快进合入本地 master；在主目录逐项复核 128 份证据的
大小与 SHA-256，三个子任务及 root 修复均为 master 祖先。README、总计划和许可策略
合并前后哈希一致，没有推送。本轮 bases/sandbox 树及分支已移除；host 普通 Git 清理
返回 Filename too long，目录/分支保留，未强删或换方式重试。root 在本记录合并后执行
普通 Git 清理，结果以最终回复为准；历史树均未操作。清理记录另行纳入证据清单。
