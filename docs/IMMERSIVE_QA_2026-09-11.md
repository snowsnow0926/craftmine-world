# 沉浸式造物首版验证记录

记录日期：2026-09-11。验证在专属 `D:/cm-iw-check-0911` 工作树进行，未操作用户正在使用的窗口、鼠标或键盘。

## 已通过的真实运行验证

固定引擎为 `4.7.2.stable.official.ed1daf0bf`，来自已校验缓存 `D:/Craftmine World/desktop/build/godot/4.7.2-stable`。之前“没有 Godot”的结论不准确：环境中实际存在固定版本缓存，只是未配置 PATH。

造物故事最新扩展：`test-results/creation-story-ip20np/report.json`。25 个流程检查与 195 个原生断言全部通过，23 次引擎调用无错误；原生断言包含各阶段共同的初始化、回应与身份检查，数量不表示 195 个独立功能。

- 空白世界的真实物理射线选中地面，源码操作在此放树。
- 新进程实际渲染节点保留树身份，修改源码后高度为原来的两倍。
- 准星瞄准与距离判定控制宝箱、标记交互；宝箱仅奖励一次。
- 错误顺序保持门关闭，正确顺序触发生成的规则源码开门并关闭实体碰撞。
- 普通角色控制器实际移动；保存时间、位置、背包、宝箱、门和规则进度。
- 未知字段、非法背包数量、缺少门进度、缺少规则进度均拒绝恢复，原状态不变。
- 真实 Web 导出件完成测试专用 executor 检查与候选采用，真实 Godot 回执交给 Rust 保存。
- Rust 退出重启后完整状态相同，再由新 Godot 进程恢复；重新互动宝箱不重复奖励。

- 重开后调用生产 `creation-source-service`，真实 Rust 分页读取、源码补丁和持久回执完成新物件放置；相同请求重试读回原回执，不重复放置。捕获提供者属于测试构造，位置与采样来自另一个实际 headless 进程，不冒充桌面 IPC 验收。
- 新增门与规则，修改源码默认时间为 18 点。真实候选默认值经 JS 与 Rust 分别推导迁移，第二次正式采用保留已完成规则、背包、宝箱和玩家位置，新门与规则保持初始状态。
- 创建第二个世界，仅替换世界身份；原世界保持不变。实际完整备份经过内容校验、激活恢复与服务重启，两个世界的完整进度一致。

本次 Rust 可执行文件 SHA-256：`76e1652cc7de7808e9bdfc736619362c70ad7f58430a7b50d854b11a92d3e2e7`。它复制进本次测试目录，运行不依赖共享构建目录在测试期间保持不变。

旧四底座：`test-results/audit-base-save-FTDdlv/report.json`，第一人称、俯视、采矿、横版全部经过真实引擎导入及状态验证；还包括横版拒绝损坏存档的启动保护。

## 已准备的独立依赖环境

`scripts/prepare-isolated-dependencies.mjs` 复用已安装的第三方包，将所有 `@pi-desktop` 工作区包明确绑定到当前工作树。不会把旧工作树的共享类型或业务源码误当成当前版本。验证工作树中已成功编译 shared、plugin-sdk、i18n、plugin-devkit、agent-runtime 五个包。

## 尚不能据此宣称的结果

本记录不声称模型首次成功率、实际语音转写、画面质量、玩家手感、桌面完整创作流程或生产沙箱执行器验收已通过。上述故事使用固定作者输入，executor 登记属于测试前置。最终桌面构建与综合接线验证由总控在合并后继续执行并记录。
