# 首次自主创作：完整交付与收尾

2026-09-14 完成。对应用户“把后面的都做完”，承接[中文方案](FIRST_INDEPENDENT_CREATION_PLAN_2026-09-13.md)与[剩余工作计划](FIRST_CREATION_COMPLETION_PLAN_2026-09-13.md)。开发基线为 `cdcd0813`。

## 本轮交付

| 工作包 | 实际结果 |
| --- | --- |
| PI Desktop 内的完整流程 | 侧边栏“首次创作指引”连接既有世界、素材、创作、历史和模板入口；只保存阅读位置。新建世界在没有消息的情况下也能恢复同一真实创作会话；旧的无绑定世界可明确“开始创作”。 |
| 素材与编辑 | 兼容素材使用固定版本直接检查并加入；保留 AI 操作。实际完成博美加入、树木放置、尺寸和颜色修改。大型博美导致无关空地无法选择的问题已修复，仍保留相关未知几何的保守遮挡判断。 |
| 等待与恢复 | 显示真实导入、构建、检查阶段和准备／采用耗时；仅合并同时发生的重复读取，不缓存后续内容校验。操作支持重试、取消、关闭面板和应用重开恢复；素材建议面板的主进程入口拒绝问题已修复。 |
| 保存与复用 | 新用户档案完成保存、冷启动、模板导出、第二个空档案导入游玩和再次冷启动。另一个保留的旧世界完成正常观察组件升级；38 个源码文件仅改动 1 个，随后完整闭环通过。 |
| Windows 与材料 | 完整文件夹、ZIP、独立试玩启动入口、三个世界模板、中文操作说明、评估包和逐人记录表。源码、Blender 对应源码、第三方说明与实际文件清单随应用提供。 |

## 成品身份

版本：**0.14.4-preview.22**。固定源码提交：`20aad1fb7ec3fef14e1dbbc9fae0ab120b0195b8`。

- 文件夹：`D:/Craftmine Releases/FirstCreation-preview22-20aad1fb`。
- 启动：上述目录内 `START-PLAYER-PREVIEW.cmd`；档案位于 `%LOCALAPPDATA%\CraftmineWorld-FirstCreationPreview22`。
- ZIP：`D:/Craftmine Releases/FirstCreation-preview22-20aad1fb.zip`，**1,127,972,077 字节**（约 1.13 GB）。
- ZIP SHA-256：`2325dde3a4be7a39d47a82309397987f52e9c3075187cf92a28378183e78ef10`。
- 程序本体：2,188,090,237 字节；含模板与说明的完整目录：2,211,591,468 字节。
- 实际解压逐文件对比：**8,451 个文件一致**；验证工具也按固定 SHA-256 核对。

实际应用 main、Core、验收脚本及文件清单哈希见[成品原生验收](FIRST_CREATION_PACKAGED_ACCEPTANCE_2026-09-14.md)。最后的测试驱动修正和交付文档提交位于成品源码提交之后；未将文档 HEAD 冒充为重新构建的程序。发行 ZIP 生成后保持不变，后续补充证据记录在仓库与归档中。

三个附带模板为 `matched-courtyard.zip`、`companion-and-rain.zip`、`first-creation-pom-tree.zip`。第三个模板来自本次实际成品创作，7,468,740 字节，SHA-256 为 `3dc8148969767183471476a7839b1f894a32662945a180a87050908c0848be87`。模板通过原有素材库导入后创建独立世界，需要完整客户端。

## 验证与测量

实际成品的全新创作闭环与旧世界升级分别完成 5 次和 4 次正常启动，模型调用均为 **0**，原生退出审计未发现输入、焦点、页面或清理异常。原生完整树木与博美画面已目视核对。旧世界升级仅通过精确旧版哈希、正常检查和正式采用完成，未编辑数据库或制造对象来替代玩家操作。

随后从最终 ZIP 的实际解压副本运行三组额外验收，也全部通过：

- 素材直接操作：五步引导、连续两只博美、同编号重试不重复、跨面板及冷启动恢复、原始资源拒绝、即时取消、新旧世界分别保留。
- 前端操作流：四个真实预览、正常创建示例副本、确切素材引用进入原 Composer。实际 CLI `0.154.0-alpha.6.2` 返回连接就绪，选中 `gpt-6-astra / xhigh`；只验证连接，没有生成内容。
- 四世界流畅性：全部创建、原生状态采样、保存、切换、F2／Esc 对话布局、进入对话后停止移动输入，以及完全退出后恢复上次世界。两次应用均正常退出。

五份最终原始报告与汇总见[交付证据 JSON](evidence/first-creation-completion-20260914/delivery.json)，共 14 次正常启动，退出审计均无异常；实际运行前后的成品文件清单一致。解压副本完成验收后可清理，发行目录与 ZIP 保留。

| 演示世界 | 首次创建至可读取原生世界 | 已有世界切换 |
| --- | --- | --- |
| 博美主线 | 33.2 秒 | 13.1 秒 |
| 飞行 | 27.9 秒 | 15.2 秒 |
| 控雨 | 21.9 秒 | 11.7 秒 |
| 城市 | 25.2 秒 | 10.5 秒 |

完整退出后恢复上次世界约 12.1 秒。四次约 2 秒的 PI 界面帧采样 P95 均约 16.7 ms，未出现超过 50 ms 的间隔；四次经过宿主身份核对的引擎 FPS 监视样本均返回 60。它们是短时后台样本，不能替代长时间实际游玩或保证其他机器的表现；GPU 耗时为未知。

完整的新档案自动闭环从首次启动到最后一次冷启动内容核对约 **4 分 8 秒**。其中直接加入博美的准备约 30.0 秒，确认后采用约 5.4 秒。该自动样本包含多次应用启动，不包含真人理解界面和考虑需求的时间；也不能与从零生成完整世界的 AI 用量直接比较。

解压副本的两次直接加入，客户端观测总耗时约 36.1 秒和 41.3 秒。应用持久化的准备／采用阶段为 29.9／5.2 秒和 35.2／5.1 秒；客户端总耗时包含状态查询及界面返回等待，因此不会与阶段相加完全一致。

针对性 TypeScript 检查、桌面生产构建、直接素材服务、导航入口、素材库、指南／恢复界面及真实 ZIP 解压测试通过。新拾取器的原生超预算场景和旧版迁移兼容性检查通过，Rust 调试测试配置下的 3 项探针测试通过。

构建诊断原样保留：一次发布测试编译发生 `STATUS_ILLEGAL_INSTRUCTION`，不能记为发布模式单元测试通过；正式打包第一次编译发生 `STATUS_HEAP_CORRUPTION`，使用同一源码、相同发布优化配置和单任务构建后通过。最终验收使用重新封装后的真实 Core，而非旧的已交付二进制。

## 收尾与边界

总控分支为 `codex/first-creation-complete-20260913`，工作树为 `D:/Craftmine Worktrees/first-creation-complete-20260913`。指南、原生闭环和打包分别使用独立 Codex 子 agent 工作树，原始提交经合并保留。源码合入本地 `main`，不推送 GitHub，也不修改默认分支或发布远程版本。

合并后由每个 agent 清理自己的已合并工作树。清理前先保留测试证据，再解除目录链接，避免沿依赖链接删除其他工作目录。归档基址为 `D:/Craftmine Archives/first-creation-complete-20260913`，各任务 `LOCATION.json` 记录旧路径映射，最终实际收尾回执为 `CLOSEOUT.json`。外部短路径原生证据目录继续保留；原有 preview.21、四个认可世界及可复用构建缓存保留。

支持编辑的对象仍以界面实际能力为准，任意 GLB 不自动获得尺寸／颜色参数。已有素材来源与许可警示继续保留，文件完整性检查不会把未知来源变成正式授权。

**待外部实际执行：** 5–8 位真实新玩家、干净外部 Windows 电脑及长期连续游玩。测试包未签名，没有声称已通过安装器安装／升级／卸载。相关中文材料已备齐，没有代填真人结果。社区分发和多人功能继续遵循原方案的用户与内容供给前置条件。
