# 第二轮并行派工与剩余工作判断｜2026-09-10

**剩余工作量仍然大，值得继续并行。C 暂时不用强停，继续由它收尾当前执行和运行检查问题。** 上一轮已产生大量模块代码，但完整客户端接线、真正独立可恢复的备份、素材界面、真实 AI 创作与固定安装包仍有实质缺口。现在还不能可靠地说只剩某个百分比或几天。

本目录提供 **10 份当前收尾提示词、2 份后续阶段提示词，以及 1 份给 C 的继续工作提示**。每份都能独立复制给 agent。当前只准备派工文件，没有替用户启动新开发任务，也没有停止 C。

## 工作还差在哪里

| 用户最终能做的事 | 本次核对结果 | 剩余工作判断 |
| --- | --- | --- |
| 新建世界，用 AI 修改，检查后预览和应用 | 核心、宿主、界面、工具各有实现；尚未证明同版本正式产品全链路成立 | 最大优先项，需要 R1/R2/R7 与 C 联调 |
| 在多种底座中持续玩和创作 | 三底座已有内容和验证；最新来源需要重验，挖掘分支还未正式接入 | R3 仍有玩法和保存功能要补 |
| 做出一个门或宝箱，跨世界复用、升级 | 包与实例管理已有代码，部分安装仍只是声明，未真实写入场景 | R4 是实质开发工作 |
| 搜素材、看预览、查看和恢复创作历史 | Git 模块未在交付核心正式登记；素材 UI 缺失；工具与入口未完整连接 | R1/R2/R6/R7 联合完成 |
| 换电脑后恢复完整工程和进度 | 原备份包含外部正文清单，仍依赖来源目录，不是完整可迁移备份 | R5 需要补归档与真实恢复 |
| 安装后直接使用，AI 确实能完成需求 | I 尚无真实模型创作通过记录；K 未交新的固定完整包，许可和性能证据仍缺 | R8/R9 必须在集成版本上收尾 |
| 高阶 L3/L4、分享与社区 | 高阶目前主要为框架；新创作包和社区计划包含更多后续阶段 | 独立后续工作，不因首版可用而消失 |

上一轮很多通过数字证明的是单个模块或当时源码。它们不能累加为产品完成率。I 的 14 类、28 轮、128 条冻结断言应保留；“产品接口当时未配置”与“真实模型执行失败”也要分别记账。

## C 现在怎么处理

最新已读取结果为 2026-09-10 01:56:34（本机时间）。C 仍有未提交改动；上一轮通过真实预检、注册与 10 个产物暂存后报 `GODOT_CHECK_TIMEOUT`。下一轮 `DUSUoL` 已通过实际导入、导出和独立 Electron 检查，随后在画面断言失败：3 帧只有 1 个不同哈希。需要继续确认画面采集与实际游戏呈现。日志说明任务在推进，但没有证明全链路成功。

建议把 [C 的补充提示词](C-continue-current-task.md) 发回原 C 任务，让它定位这一个问题并尽早提交可消费成果。不要同时新开一个执行器负责人。只有完成当前逻辑阶段并交接，或证据确认遇到本轮无法解决的外部障碍时，才安排有序停止/转交；其余 agent 已停止不是停止 C 的理由。

本报告是文件与原始日志的审计快照，不是 C 调度器实时状态。分发前如果 C 已有更新，以它的新提交和最终结果为准。

## 本轮怎样分发

原任务可以继续用，但应从最新本地 master 建自己的新分支/工作树，保留历史地合入旧成果。**A/M、D/E、F/G 每组只选一个负责人恢复**，不要两位都接同一份新任务。没有列为本轮负责人的原 agent 保持停止。

| 提示词 | 建议负责人 | 可立即做的工作 / 最后依赖 |
| --- | --- | --- |
| [R1 · 核心与 Git 真正接通](R1-core-and-git.md) | 原 A 或 M，选一位 | 优先开工；统一引用、正式登记、进度与恢复。先交稳定核心给 C/R2/R7 |
| [R2 · 客户端、预览、历史和恢复界面](R2-client-and-preview.md) | 原 D 或 E，选一位 | 优先开工；合并入口与界面；新建/应用最终依赖 R1/C/R3 |
| [R3 · 多底座和挖掘正式接入](R3-bases-and-mining.md) | 原 F 或 G，选一位 | 修底座和组件；产品创建与保存依赖 R1/R2/C，安装对接 R4 |
| [R4 · 创作包与真实安装复用](R4-packages-and-installation.md) | 原 H | 静态包与安装器先做；实际场景安装依赖 R1/R3/C/R2 |
| [R5 · 完整备份与恢复](R5-portable-backups-and-reclaim.md) | 新专责 agent | 新目录归档恢复先做；一致性和引用保护对接 R1/R6 |
| [R6 · 素材界面、预览和证据](R6-assets-ui-and-evidence.md) | 原 N | 界面与解码先做；R1 接 RPC，R2 接宿主和导航，R4 接复用 |
| [R7 · 模型工具、实时观察与上下文恢复](R7-model-runtime-and-recovery.md) | 原 L | 优先接真实服务；最终模型创作依赖 R1/R2/R3/C，与 R8 联测 |
| [R8 · 正式产品与真实 AI 验收](R8-real-product-model-acceptance.md) | 原 I | 现在接产品适配器；先一条真实需求，再扩大到已集成范围 |
| [R9 · 固定包、许可和性能交付](R9-fixed-package-and-rights.md) | 原 K | 现在补来源与测量接入；最后依赖集成代码和同包 R8 结果 |
| [R10 · 隔离剩余边界与最终联测](R10-isolation-final-gates.md) | 原 B | 小范围补协议/恢复/资源边界；不重做已通过的隔离器，不覆盖 C 的稳定二进制 |

人力充足时，上述任务可以并行做各自模块；它们的最终验收有依赖，不能都停在接口文件上等待别人。如果只有 4 个开发位置，先 **C + R1 + R2 + R7**，先打通最关键的一条创作流程，再轮入 R3/R4/R5/R6。R8/R9 可以利用接口等待时间先做适配和材料。不要让一个位置只重复等待没有变化的结果。

以下两份保留全部后续范围，优先级在当前可用版之后；多余的独立人力可以做前置准备。

| 后续提示词 | 建议负责人 | 启动条件 |
| --- | --- | --- |
| [R11 · 真正的 L3 部件与 L4 策略对照](R11-l3-l4-after-baseline.md) | 原 J | 现在读取已冻结评测、确定问题；完整实验等 R9 的可用基线 |
| [R12 · 社区目录与本地上传下载阶段](R12-community-local-milestone.md) | 新社区 agent | CP 格式冻结后，本地 CC0/CC1 可独立推进；运行包安装依赖本地产品能力 |

普通包目录、上传下载不依赖完整 Git 远程托管。社区公开部署、外部上传和商业运营仍按当前计划和具体授权推进；本轮派工不自动公开用户内容。

## 明确共享文件的唯一负责人

| 共享范围 | 本轮负责人 | 协作方式 |
| --- | --- | --- |
| Rust main.rs/lib.rs、数据库登记、godot_*、content_history | R1 | 其他模块交已提交实现，R1 分批正式登记并交稳定构建 |
| 唯一共享引用契约和总回收计划，包括 godot_storage | R1 | R4/R6 参与冻结向量；R6 修改自己的消费者；R5 提供备份保护引用 |
| Electron main/index.ts、view.mjs、App/导航、历史与备份 UI | R2 | R4 提供作品 UI，R6 提供素材 UI；R2 实际接入口和事件 |
| 执行器、私有插件后台、独立检查器、相关构建入口 | C | 其他人消费提交与协议；不拷贝 C 正在变化的源码 |
| 模型工具、上下文、manifest 的 agentTools | R7 | C 仅改后台服务贡献；双方核对 manifest 的合并结果 |
| 底座、GDScript 共享适配、组件物化 | R3 | R2 管 JS 运行桥；R4 调用物化接口，不再各写一套 |
| 创作包、安装/升级/转换、作品组件 | R4 | 完整备份交 R5，素材正文交 R6 |
| 完整归档、恢复与备份保护引用 | R5 | 不再另写总回收器；R1 管 Git 和总事务，R6 执行合格素材回收 |
| asset_catalog、素材 worker/预览和素材 UI | R6 | 不把素材 UI 再转给 R2；R2 只接宿主和导航 |
| broker 源码与稳定二进制 | R10 | 不覆盖 C 正在运行的副本；新版本按哈希交接 |
| 全局依赖/锁文件、最终合并与验收总判定 | 主任务 | 专责任务提交必要差异；不碰主目录其他任务的未提交文档 |

共享入口的所有权是为了减少冲突，不允许形成“所有模块先完成才能登记、所有模块又等登记才能完成”的循环。负责人应先交最小可用逻辑提交，再分批合入消费者并联合验证。只有片段、schema 或模拟适配器时，明确写“待接入”。

## 上一轮成果定位

本轮开始审计的主线为 `e462147`。下列提交已存在，尚未合入该主线；启动时必须核对是否有更新。除 C 外，13 份上一轮工作树在核对时干净；这不代表 13 个产品功能已完成。

| 原任务 | 最后核对提交 | 第二轮承接 |
| --- | --- | --- |
| A | `7906c5b` | R1 |
| B | `5cf65eb` | R10 |
| C | 主线基础上仍有未提交改动 | 原 C 保持独占，等待自己的逻辑提交 |
| D | `b903269` | R2 |
| E | `6bc9ec3` | R2 |
| F | `d7db302` | R3，保留既有底座历史 |
| G | `c906f3e` | R3 |
| H | `da41621` | R4/R5 按新文件边界拆分 |
| I | `9df9801` | R8 |
| J | `68e34d9` | R11 |
| K | `2c8b895` | R9 |
| L | `e0b952d` | R7 |
| M | `de9f4b0` | R1 |
| N | `b646b2e` | R6 |

旧树通常在 `D:/Craftmine World-worktrees/godot-remaining-<小写字母>-20260910`；B 在 `D:/Craftmine World/test-results/godot-remaining-b`。报告位于各自 `docs/dispatch-reports/godot-remaining/<大写字母>/`，具体文件名见每份提示词。旧树仅只读交接，不作为新 agent 的改写或清理目标。

审计还发现 N 的部分原始测试证据与 K 引用的测量文件无法找到，M 的较大测试数字来自临时登记版本。相应任务要补实际交付树证据，不能补造历史输出。A 已解决的部分来源问题也不能继续当成 D 的永久阻塞。

## 完成标准与后续范围

每个任务都要分别交代：模块实现、正式入口接通、真实模型、固定包、故障恢复，以及可见呈现/手感的证明范围。自动验证只用独立后台环境，不抢鼠标，不运行历史输入测试。

| 计划范围 | 主要承接 |
| --- | --- |
| GD0–GD2 引擎、布局与完整创作链 | C、R1/R2/R7/R9/R10，R8 验收 |
| GD3–GD5 武器、俯视和横版 | R3/R7，C/R1/R2 支持，R8 真模型验收 |
| GD6 复用、迁移、长任务 | R1/R4/R5/R6/R7，R2 接界面 |
| GD7 Windows 交付 | R9，R8 同包验收，主任务最终集成 |
| GD8 L3/L4 | R11，R8 保持独立评测 |
| VM0–VM4 / AL0–AL5 本地版本、素材、复用与导出 | R1/R2/R4/R5/R6/R7/R9 联合推进 |
| CP0–CP6 创作包、玩家底座、导出与兼容治理 | R4 主责，R1/R3/R5/R6/R9 联合；阶段性未验项保留 |
| VM5–VM6 / AL6–AL7 / CC0–CC6 远程与社区 | R12 分阶段承接，R1/R4/R6 支持；本轮优先本地社区样本 |

Godot A01–A17、版本 V01–V16、素材 AL-A01–AL-A17、创作包 CP-A01–CP-A20、社区 CC-A01–CC-A22 分表追踪，不混用编号。计划原文以主目录当前文件为准；新增社区或包计划不意味着已经实现，也不能删掉旧验收分母。

首个可用版可以先完成 3D/俯视及必要的版本、素材、复用和恢复能力；横版、高阶、社区等未完成项继续保留在总目标。当前这批任务结束后，主任务仍需按整份计划审计、集成并复验，不能凭 agent 停止数量宣布 Goal 完成。
