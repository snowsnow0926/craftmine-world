# 给正在工作的 C：继续定位运行检查超时并提交交接

这是一条发到原 C 任务的补充提示，不要另开一个 C，也不要因此杀掉仍运行的作业或重建原工作树。

继续完成你现有的 Godot 执行器与独立运行检查任务。其他 agent 停止不代表你也必须停止；第二轮仍由你独占自己的服务、检查器和构建入口，其他人不会接管这些文件。

主任务只读检查到：2026-09-10 01:52 的 `godot-remaining-c-full-chain-pnxiLD` 已通过实际预检和执行器注册，暂存 10 个产物，随后以 `GODOT_CHECK_TIMEOUT` 失败；01:56:34 的下一轮 `godot-remaining-c-full-chain-DUSUoL` 已通过实际导入、导出和独立 Electron 检查，但 `isolated check observed real frames` 断言失败，记录为 frames=3、distinctFrames=1。这里记录的是观察时的状态，先核对你当前实际句柄和最新结果，不重复启动同一作业，不把目录存在当作通过。

接下来请集中完成：

1. 继续定位当前画面断言与上一轮检查器超时，保留 import/export、检查进程、preload/运行桥、资源加载、ready/首帧与生命周期的宿主证据。区分采到静态正常画面、加载页、停滞帧和没有真实呈现，按样例的实际预期证明；不能仅凭哈希相同推断原因。根据证据修复，不要只增加超时、删检查或手动填写通过记录。若后续已解决，以最新原始结果更新报告。
2. 核对当前用到的核心与 broker 源码、版本、协议和二进制哈希。A 的 `7906c5b` 已有来源接续修复，B 的 `5cf65eb` 是上一轮交付；R1/R10 的更新只能消费已提交的稳定版本，不引用过时阻塞，不改他人的 Rust 或 broker 文件。
3. 尽早将已验证的逻辑单元整理成可供 R1/R2/R7 消费的提交，注明是否仍需后续修复。不要为了等所有场景通过，把全部执行器改动长期留在未提交状态；也不能把有提交等同完成。
4. 完成真实 core → broker → Godot 构建 → Electron 独立检查的正常路径，以及原任务要求的失败、取消、恢复与清理。交给 R2 的 firstLoad/候选检查契约应明确，随后用同版本联合验证产品应用。
5. 保持原文件范围。你拥有插件私有后台服务、执行器、godot-build-verifier、godot-check preload、相关构建入口；R7 拥有模型工具及 manifest 的 agentTools，R2 拥有 main/index.ts、view.mjs 和界面。manifest 只提交自己的后台服务贡献，合并时核对双方字段。
6. 遵守项目不抢鼠标要求：独立 headless/offscreen 和测试目录，禁用 Pointer Lock，不激活窗口、不模拟真实输入、不运行两个历史输入测试。共享二进制和用户数据不作清理目标。

若仍在进步，继续修复并验证。如果遇到确切的外部依赖、权限或环境障碍，且当前范围无法解决，保存完整失败、已验证提交、最新作业终态及最小交接说明，再有序交接；不要无变化地无限重跑。正在运行的测试先按实际句柄确认终态，观察工具等待超时不能当作进程已结束。

最终交付放在 `docs/dispatch-reports/godot-round2/C/`：源码提交、构建身份、实际命令与原始输出、通过和未通过项、给 R1/R2/R7 的接入方法。暂存产物和预检通过只能证明对应步骤，完整链路未通过前不要报告执行器全部完成。
