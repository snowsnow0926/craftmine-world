# 给现有 R2：继续完成正式客户端闭环

把本文件发回原 R2 任务，若刚完成则在原任务接续客户端收尾；不另开一个并发修改同范围的 agent，不中断现有测试或覆盖在途修改。你的 2fa3c7c 及其包含的957bbe4素材导航、66a8f2f验证已进入本轮交接记录，启动时以你更新的实际状态为准。

用户已安排新一轮专项收尾。请继续在现有任务中完成客户端的正式创建、预览应用、历史与恢复界面，并消费其他人的已提交修复。先读主目录 docs/audits/godot-round2-20260910/REPORT.md、REPRODUCTIONS.md 与 docs/dispatch-prompts/godot-round3-20260910/README.md。

## 你的文件与协作边界

- 你继续独占 Electron main/index.ts、godot-world-creation、candidate/panel/selection/runtime host、JS Web 宿主桥、Craftmine navigation/panel gateway、App/布局、历史与备份恢复 UI，以及插件 view.mjs。
- S2 接手 C/B 的执行器、broker、私有插件 main.cjs/host-requests.cjs/plugin-runtime/host-process、独立检查器和相关构建入口。你实际在 main/index.ts 注入检查器、采样与其他宿主服务；S2 实际接私有通道，不让双方只留下补丁。
- S1 接手核心、唯一合同、main.rs/lib.rs、部署事务、分支和总回收。S3 接手底座/作品安装与作品专属组件；S4 接手备份后端；S5 接手素材服务与素材专属组件。你的素材导航已经交付，组件内部修复交 S5，你只维护嵌入契约，避免再同时改 AssetLibraryPanel。
- S6 接模型工具/请求 hook；你提供带世界/构建/实例身份的真实运行观察桥。S7 是本轮综合基线与验收负责人，分批合入你的逻辑提交；S8 重建最终安装器。

## 必须收尾

1. 复现并修复 index.ts 向 loadMaterializer 传 file URL 后又被 pathToFileURL 二次转换的问题。统一参数含义，通过实际主进程入口加载真实物化模块，不只测试注入替身的工厂。
2. 消费 S1/S2 的实际初始化与查询路由，把物化、持久世界/工程登记、首次构建、独立检查、firstLoad、正式可玩接成完整流程。首次加载失败或检查失败保持可恢复记录；不能手工 seed 已应用状态来验收。
3. 新建世界使用稳定操作身份。初始化已提交但回包丢失时先查原回执，不能在 catch 中一律删物化目录、再创建另一个世界。重试/取消/重启按核心真实阶段展示；未知状态不伪装失败或成功。
4. 接 S1 的 Git 分支修改/应用、内容恢复与进度恢复，候选应用读取最新正式进度。预览/取消、保存失败、过期候选、回包丢失与重启都保持正确实例、会话及原世界。
5. main/index.ts 实际构造并注入 GodotBuildVerifier；采样与预算调用使用 S2/S6 的正式接口，返回 current world/build/base/instance 身份。切世界淘汰迟到结果，后台任务留在原世界；讨论模式不修改。
6. 完成历史分支/差异/恢复、素材导航、作品安装、草稿 resume 和 portable backup 导出/恢复入口。界面调用真实生产服务；素材/作品专属组件分别由 S5/S3 维护，你接正确导航与事件，备份恢复与历史 UI 仍由你实现。你新发现的 scan 结果/方法同名崩溃交 S5 修，GODOT_PROJECT_REVISION_NOT_INDEXED 备份漏表交 S4/S1 修；保留原失败，消费修复后联合重验，不在自己的客户端提交中重写其模块。
7. 消费 S3 最新四底座目录和机器可读初始进度，保留空白与短样例。此前 first-person/side-view 起始状态缺失已经部分修复，先核对最新实现，不能继续引用旧阻塞。
8. 保持世界居中/右侧输入、窄窗、中文、缩放/主题、布局记忆、游玩返回与会话绑定。原 14 项桌面失败基线对照保留，新改动运行适当回归；不得删断言或换旧世界测试证明 Godot 新建。

## 验收与交付

用 S7 同一综合版本完成：新建 3D/俯视世界 → 游玩 → 真实工具修改 → 检查 → 预览/取消 → 应用 → 退出重开，再覆盖横版/挖掘及历史/素材/安装/备份相关入口。先跑通一条，再逐项扩大；产品入口和实际核心必须都参与。

创建、应用、保存或恢复每个失败/丢回包路径都保留原世界和可查询操作；状态不得靠验收脚本直接改写。完整可见手感与 offscreen 覆盖范围分别说明。

测试仅独立 headless/offscreen、独立数据，初始化禁 Pointer Lock；不抢鼠标键盘/焦点，不运行 tests/browser.mjs、tests/modules-browser.mjs，不操作用户浏览器。不要终止其他任务、改共享运行二进制或清理未知目录。

先提交现有已验证逻辑，再分批交 S7 消费；源码/接口变化同步 spec/ADR/E2E，vendor 内容与提交用英文。补充报告放 docs/dispatch-reports/godot-round3/R2/，列实际提交、调用入口、构建身份、原始结果和未完成项。不要将“客户端到核心初始化返回成功”当成“首次创建可游玩已经完成”。主目录其他计划不改，不推送、不覆盖用户安装，不操作主任务 Goal。
