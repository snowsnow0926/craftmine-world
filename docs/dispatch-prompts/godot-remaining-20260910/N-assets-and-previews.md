# N｜素材库导入、搜索、预览与固定资源版本

你是“最中幻想 / Craftmine World”本轮并行开发的专责 agent。用户已授权开发，请完成实现、适当验证和交付，不要只给方案。你只负责下文指定范围，其余由其他 agent 和主任务处理。

先读：
- D:/Craftmine World/AGENTS.md。
- D:/Craftmine World/docs/GODOT_MULTIBASE_DEVELOPMENT_PLAN.md（主目录有尚未提交的最新内容，必须只读这里的版本）。
- D:/Craftmine World/docs/PROGRESS_REPORT_2026-09-10.md、docs/GODOT_CYCLE_05.md。
- 涉及 vendor/pi-desktop 时，再读该目录 AGENTS.md 和相关 spec/ADR。
- 再读主目录 docs/VERSION_MANAGEMENT_DEVELOPMENT_PLAN.md 与 docs/ASSET_LIBRARY_DEVELOPMENT_PLAN.md，二者已加入总计划；内容历史与素材引用按 M/N 共同契约接入。未加前缀的 A01–A17 指 Godot 总计划；素材库验收写成 AL-A01–AL-A17，版本管理写 V01–V16，避免同号混淆。
- 许可与导出有关工作另读主目录 docs/LICENSING_STRATEGY.md，不把决策记录当作已完成的许可适用。

共同执行约定：
1. 启动时核对最新本地 master、Git 状态和工作树。分发时 master 是 92c98b2，实际开发以最新已集成提交为准。新建自己唯一的 codex/ 分支及独立工作树；禁止在主目录或别人的工作树开发。不要让多个 agent 并行 pull、合并或清理主目录，统一由主任务集成。
2. 下文旧树只作为只读交接来源。已有提交保留提交历史；尚未提交的成果，确认源已停止修改后，仅在自己的新树接续被明确分给你的文件差异及新增文件，记录来源、文件哈希和基线。不要整树覆盖、遗漏未跟踪源码或把缓存/数据一并带入；不得修改、删除旧树。稳定交接未齐时先完成不依赖它的工作。
3. 主目录 README.md、docs/GODOT_MULTIBASE_DEVELOPMENT_PLAN.md 和 docs/LICENSING_STRATEGY.md 以及其他现存未提交文件（包括新增版本管理、素材库计划）属于其他任务的工作，禁止覆盖或顺手提交。不要操作历史残留工作树、用户存档、共享引擎缓存或正在使用的客户端。
4. 自动验证仅使用独立 headless/offscreen 进程及独立数据目录，初始化禁止 requestPointerLock 和抢焦点。禁止真实鼠标键盘、Playwright mouse/keyboard/click/fill、激活窗口或操控用户浏览器；禁止运行 tests/browser.mjs、tests/modules-browser.mjs。通过页面脚本、HTTP、纯逻辑或受限测试接口验证；不能直接写“预期状态”冒充真实玩法执行。
5. 每个模块只保留一位写入负责人。跨范围需求提交精确接口说明或小补丁建议给对应负责人。共享主入口 electron/main/index.ts、plugins/craftmine-world/view.mjs、全局依赖锁文件以及总 E2E 文档由主任务合并接线；你提供可合并片段和调用示例，不在自己的交付中夹带另一位 agent 的整份修改。Rust main.rs/lib.rs 由 A 管理。不得另建一套世界数据库、模型循环或宽权限执行路径。
6. 本轮任务报告放 docs/dispatch-reports/godot-remaining/<你的字母>/，专属测试优先放 tests/godot-remaining/<你的字母>/。行为变化同步独立 spec，架构/协议变化写独立 ADR；vendor 内文档、代码注释和提交信息使用英文。ADR/E2E 用唯一任务标识准备片段，主任务统一编号和汇入总表。
7. 保存首次失败、重试及最终成功原始证据；标明源代码、引擎、二进制、构建和数据身份。逻辑测试、预制样例、真实引擎、正式客户端、真实产品模型和人工手感分别记账。不能把 mock、固定样例或退出码 0 当作整个功能验收通过，不能放宽冻结断言或改通过标准掩盖失败。
8. 依赖未齐可先开发模块、契约及测试，但最终必须完成实际接线验证才可宣称该条完成。发现环境权限拒绝，保留原始错误和明确的待执行命令交主任务，不绕过限制或反复换方式；同时继续独立工作。
9. 每个完整逻辑改动单独提交，只提交自己的范围，不推送。交付分支、提交号、改动文件、接口、验证命令及原始证据、未完成项、依赖和集成顺序。本次由主任务统一审计合并；你的树交付时保持干净，合并确认后的清理只涉及你自己的新树。

## 专责任务

你的目标：实施 ASSET_LIBRARY_DEVELOPMENT_PLAN.md 的 AL0–AL5 本地素材与作品基础，向 H 的作品安装/升级及 M 的 Git 历史提供可靠的固定资源正文；保留 AL6/AL7 后续阶段及依赖。

独占新增 Rust src/asset_catalog/**、独立素材导入/扫描/索引/预览服务及专属素材 UI 子目录。A 管 godot_builds.rs 等既有资产 API 过渡和 RPC 登记，H 管 library.rs/library-service.mjs 以及作品组合、安装升级和完整备份，M 管共享引用规范/Git 锁文件历史。E 负责 App/导航接线。不要另建与 H 并行的作品库或与 M 不兼容的引用格式。

必须完成：
1. AL0 与 M 共同冻结 raw/object/creation/world-template、查询范围、不可变版本和完整锁文件契约；M 维护唯一共享定义，你提供依赖闭包/循环/路径/哈希的共同测试向量。区分已索引、可预览、某底座已检查和来源已应用，不能一个绿色标签代表全部。
2. 评估计划中的开源参考，读真实代码、固定提交/许可/依赖和移植范围，形成可复现试验；参考交互不等于复制代码授权。无需安装整个编辑器插件才能开始，不采用未经验证的容量宣传。
3. AL1 玩家文件流式导入、内容哈希、逻辑资源多版本、索引与旧格式映射；真实大于当前 512 KiB 的素材在经实测的新预算内可用。修改同一路径应通过明确版本/替换规则，不静默覆盖历史。重复操作幂等、同版不同内容拒绝、相同正文去重仍保留各自来源许可。
4. AL2 分类、标签、搜索、使用范围、缩略图及可信图片/模型/音频预览；首发 PNG/JPEG、静态 GLB、WAV/OGG，以及经检查的 Godot 场景/脚本包。实际格式检查、坏图、缺引用、超时和取消有独立状态；脚本/导入插件预览走 B/C 可信路径。自动音频验证只做后台解码/离线信号，不主动播放声音给用户。
5. 仅在玩家授权目录扫描/监听，处理中文、路径大小写、链接/外链、文件锁和磁盘满；目录变化只产生新版本提示，不隐式更新世界。切世界后的迟到搜索/预览不串到新世界。
6. 联合 H/M/F 做 AL3 自动门样例：导入贴图/模型，保存可交互物件，在第二世界放两扇，仅改一扇颜色，保持实例、局部覆盖和进度独立。锁定 v1 后库里新增 v2，旧世界仍用 v1。安装只产生草稿/锁文件变更，应用走 VM2/A/D。
7. AL4 的商店/战斗区/模板由 H 主持组合作品和升级，你提供完整传递依赖解析、版本冲突和更多格式的实际适配结果。禁止部分拼装缺素材作品假装可用；兼容/初始进度/来源由 F/H 验证。
8. AL5 使用关系、归档和受保护引用追踪。向 H 提供完整备份的正文快照，向 M 提供 Git 锁文件可达性解析，向 K 提供实际导出与许可清单。正式世界、历史、草稿、分支、命名版本、迁移和备份保留项阻止误删；用户源素材永不由缓存清理删除。
9. 来源不完整的本地用户素材可登记并标待核对；对外发行按 K 的实际权利与声明检查，不由 AI 根据文件名或生成过程声称可商用。外部作品包必须自包含，不能依赖本机缓存。
10. 实测 1 千/1 万资源、不同文件体积/解码成本下搜索预览 P50/P95、内存、取消、扫描、去重和构建增量。AL6 与 M 的 VM5 联合规划跨机器内容传输/LFS 适配，AL7 社区按独立产品计划推进；本地导入完成不代表远程共享已完成。
11. 生成素材通过受管理产物句柄登记，大正文不全量 Base64 塞进聊天。标签/收藏/备注等可变浏览元数据与影响运行的不可变版本清单分开；失败产物保留任务记录，只有玩家保存或明确开启归库规则后才成为收藏作品。空间回收由 A 统筹作业条件，M/H 提供引用保护，你仅删除通过该条件的素材正文，不自行触发 Git 或备份删除。

验收 AL-A01–AL-A17，各项与 Godot 同号 A 编号分开记录。首发必须完成 AL0–AL3 和 AL5 的备份/引用保护/声明；继续完成适用 AL4/AL5。I 的真实模型验收需覆盖检索复用自动门、基于旧版改造、第二世界复用和升级保留状态，不能用固定作者作品替代。

交付判定：逐条对照上述范围及两份配套计划，实际实现、产品接线和真实模型验收分别记录；保留远程/社区的后续依赖，不把它们记作本地版本已完成。提交逻辑改动、接口、证据和未完成项。
