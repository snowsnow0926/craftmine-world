# 可视化摆放与修改：完整原生验收记录

日期：2026-09-14。结果：**单客户端、全新数据目录的完整流程通过**。

本记录验证集成源码构建的实际 PI Desktop 应用；不是最终 Windows 分发包验收，也不是外部干净机器或真人试玩结论。应用资源来源提交为 `ad6bd63f4d4b1c2b6692f9674d8e83afa8f59781`，Core 使用包含完整 engine wrapper 校验的实际构建。

## 已实际完成

1. 通过原 PI Desktop 的 New World 表单创建世界，从素材库安装白色博美 v2。
2. 没有发送模型消息即退出、重新启动，恢复同一个空会话与创作入口。
3. 通过“在此放置”预览真实树模型并取消；原生截图检测到 38,330 个新增 cyan 像素，完整进度没有变化。
4. 正常检查并采用树；在“最近结果”选择它，预览移动 0.5 单位和旋转 45°，原生截图检测到 32,426 个 cyan 变化像素。
5. 检查并采用位置/朝向修改，撤销后恢复原位置与同一个实体 ID，再次应用最终位置、45° 朝向、1.25 倍尺寸与 `#88bb44` 颜色。
6. 作者保存、冷启动；保存世界模板并导出 ZIP；在另一个空数据目录导入、真实游玩、保存、再次冷启动。物体和最终变换均保留。

整个流程观测到的模型调用为 **0**；一次完整流程约 **339 秒**，包含五次应用启动、四次正式编辑检查、截图、模板导出与独立导入。这不是单次摆放的等待时间或性能承诺。五次启动全部正常退出，`violations`、`shutdownFailures` 和 `pageErrors` 均为空。

## 真实证据

- 原始报告：[report.json](D:/cm-product-visual/test-results/desktop-native-rt-wzajjh/report.json)
- 仓库内摘要：[summary.json](evidence/product-visual-editing-20260914/summary.json)
- 可复用世界：[player-world-template.zip](D:/cm-product-visual/test-results/desktop-native-rt-wzajjh/player-world-template.zip)
- ZIP：7,526,974 字节；SHA-256 `3313a166197b90ba422c368a399b6a2dccdd7fcf9db95f4b4d47f9cb38236b90`。
- 作者世界：`world-3335849b343a`；独立导入世界：`world-5af8b278dc87`。
- Core SHA-256：`6a1d9a8a57c6249a52d68c018e75fcc0c0a86f56b385564626a6b21533eddc56`。
- 主进程构建 SHA-256：`88d8a223708c1e65efb0338e8607121ae613278d3d4d48234f7cd7486a17600f`。

预览期间被动记录实际 runtime 请求；本次通过记录仅包含预期的 pause、preview、cancel、snapshot，没有额外观察或保存请求插入。记录器只追加监听，不改变执行和回复。最终作者与导入后的原生图片已经目视检查，均能看到完整的树和保留的博美。

## 保留的失败与边界

先前 `2refOO`、`nSOpPy`、`Dr1CWn`、`4qsx2m` 的失败报告没有覆盖或删除。它们分别帮助发现测试通道/导入问题、普通世界默认未启用预览扩展、Core 缺少完整 wrapper 校验、Recent 模式按钮文案差异，以及一次并发环境下的零像素预览。产品边界问题和测试脚本问题分别修正；原失败世界的单独诊断通过不代替本次完整验收。

零像素并发结果的根因尚未由失败当时的操作日志证明；本次串行完整流程和后续诊断没有复现，不据此宣称并发渲染问题已经修复。预览目前明确支持五种固定生成器物体；任意 GLB、修改过的生成器与自定义脚本组件不使用替代几何体伪装预览。
