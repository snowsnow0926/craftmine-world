# 沉浸改造审核修复与 Windows 重新交付

本轮修复审核发现的三个问题：原安装包缺少主世界插件和运行资源；
Godot 草稿预览的操作被全尺寸视图遮挡；完整工作台浮层不能查看文件与评审。

原 120 MB 安装包不作为可测试成品。重新交付必须从干净提交运行
`desktop/build-client.ps1 -Installer`，重建主插件、Rust 服务、执行器、
桌面界面、源码包和许可清单，准备固定 Godot 与 MinGit。

新增 electron-builder 的 Windows 打包前检查。缺失或旧提交的构建清单、
运行资源、插件与桌面输出均拒绝打包；正式交付仍需完成安装器解包、
内容哈希核对和包预检，不能以生成 EXE 代替完整性检查。

验证使用独立测试目录、headless 页面与受保护原生夹具，不操作真实鼠标
键盘，不激活窗口，不请求 Pointer Lock，不读取或迁移玩家存档。

## 已完成的修复与交付

- 完整浮层实际显示文件与评审，切换或重开保留聊天、草稿和世界。后台文件更新不主动打断收起状态的游玩。
- 草稿操作绑定世界、候选、构建和每次预览的独立标识；复用原有采用及回执恢复流程，旧预览按钮和并发操作被拒绝。首次预览呼出控制，F2 收起后不会反复自动弹出。
- 修正上一轮四个 Godot 控制器／HUD 文件遗漏的资源声明哈希。Windows beforePack 检查缺失资源和旧输出，正式安装器解包核验通过。

安装包受测产品提交：`04a2ef6d2655063168e573e3f0296c7b3c4c58bf`。之后只修正测试目录命名、异步世界列表等待和补写本记录，不改变安装包产品实现。

验证：43 项界面 headless 检查、27 项真实插件页面检查、16 项导航／预览／模式逻辑测试通过；桌面 TypeScript 通过。实际 Windows 包在独立 profile 中完成 11 项后台启动、默认世界加载、双模式、浮层几何和退出检查，无输入、焦点、页面错误和退出失败。完整包预检为 0 失败、1 条许可清单提示（45 项依赖未单独复制许可正文，保留源码查阅说明）；安装器载荷哈希与目录包对应。

固定交付目录：`D:/Craftmine-World-Immersive-Fix-20260911`。安装包名仍为 `Craftmine-World-Setup-0.14.4-preview.11.exe`，本轮为该版本修正版，请用新目录中的文件；旧 120 MB 包不作为交付。新包 362771945 字节，SHA-256：`e3b5024f360de51f24b9817a9965f69ff8bf630dcd2cee958707eb980bd56265`。完整证据在交付目录 `evidence`，含安装器解包记录、包预检和实际包启动报告。

真实玩家操作手感、既有个人 profile 升级和已有 Godot 世界脚本迁移未由上述新建测试 profile 代替验收。
