# Godot 创作说明与玩家反馈复盘

原始 Flash 首轮“我想要一棵树。”用了 7 次模型响应、18 次工具调用，工具均
成功。用时 118633 ms，总 token 480088（缓存 359936、输入 102754、输出
17398，其中推理 14673）。这是原包原流程的记录，不是本次优化后的成绩。

源码核对发现：creation_operation 实际限定 Godot creation-sandbox 并通过
godotProject.patch 修改源码，但工具描述没有直接说明 Godot 适用范围。
godotFacts 为 null 只表示请求中没有投影出的 Godot 源码事实，不代表项目
不存在。提示中还有重复“先读取”要求和笼统的玩家手动应用说法。原回复
已经知道本轮结束后会自动应用，却仍建议重新打开世界，并重复技术编号。

这次仅修改提示文本和三个工具说明：明确适用范围与 null 语义；按当前真实
事实补查需要的依赖，不固定重复检查无关初始存档和控制器内部；保留多种
实现选择。进度与最终回复面向玩家说明结果，技术细节仍在原有详情中。
自动应用以宿主确认状态为准，不凭检查通过宣称已经显示，也不例行要求
玩家刷新或重开。真实错误及未解释诊断不能被笼统称为无害。

模型、最大思考、1M/384K、权限、源码保护和运行逻辑均未调整。93 项 CPU
回归通过；没有使用模型/GPU，也没有改动当前测试资料。当前六步原始流
继续用旧包，新提示只在后续修复包生效。实际是否减少时间与 token，需要
保持同等配置和原始需求的后续对比，不能由 CPU 测试推断。

后续原包怪物轮遇到战斗包“三实体声明、单实体安装入口”的实际数据矛盾。
模型却把 PACKAGE_SINGLE_ENTITY_DECLARATION_REQUIRED 当成缺少调用参数，
反复尝试组合安装、entity、request.entity、内部资源哈希及位置/query变体。
没有发现要求“穷举”或禁止源码替代的规则；现有指南本已允许不合适素材改走
正常创作。这是包数据故障加模型错误推断，不能归因成提示硬性禁止回退。

补充仅解释错误层级：参数问题按真实 schema 修，包声明问题不能靠猜隐藏字段
修复；组合安装不会拆分包内部实体。可在有新证据、正确修正或正常瞬态/回执
恢复时重试，不设重试次数或整轮限额。素材确实不可用时仍可按普通源码实现
原目标并保留具体错误；用户若要求精确复用，必须说明差异，不能偷偷替代。
素材包本身由独立修复处理。新提示尚未在当前原始模型流程生效或证明更省时。

原包第三轮最后由主控按普通 abort 停止：已明确素材声明不可安装且模型持续
猜参，不是触发次数、时间或 token 阈值。原轮用时 15 分 25.67 秒；观察到
24 次模型请求，23 次已有用量报告，累计报告 5,221,792 token，其中缓存
4,316,672、输出 161,939（推理 153,792）。经历一次压缩后又检索了全库。
同一世界已保存，等待修复包按原玩家目标继续；未预先认定后续自然流程成功。

此轮文字补充的 63 项上下文测试和 15 项源码库/schema 测试通过，类型检查
与插件构建通过。未修改原包会话记录或补造成功结果。

原怪物轮最后三次 Tool not found 另经只读审计确认，不是注册或激活缺陷。
2026-09-14 UTC 17:46:29.686–29.722、17:53:18.957–19.009 激活的是
source_library/package_library，18:03:31.454–31.607 激活的是 asset_library。
18:07:08.572 三次 library_search 在尚未激活时调用而立即失败；
18:07:15.270–15.494 精确 ToolSearch 激活后，18:07:20.271–20.471、
20.471–20.639、20.639–20.802 的同三次查询全部成功，结果为空库。
18:07:15.494–15.684 还成功激活了 library_read。以上均在原轮 18:08:05
截止前，会话和事件记录一致。

仅将提示里的“已 advertised”改成“已在当前工具定义列表”，解释能力报告的
advertised 代表目录注册，不等于当前激活。当前列表已有就直接用，缺少才
ToolSearch 并在后续模型回合使用，不重复激活。没有新增运行逻辑或文字镜像
测试；正在运行的 preview27 续作仍不受这次未封包说明影响。
