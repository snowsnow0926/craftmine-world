# 宣传片开发第二轮：Godot 初始工具与运行时身份

日期：2026-09-12。基线：cf6f895a。

完成：Rust task.context 的绑定世界摘要直接返回 runtimeKind/baseId；已有读取不增加 RPC。每次模型请求在估算预算前，按权威运行时与真实注册工具求交，同步执行器和供应商 schema。确认 Godot 默认增加 godot_file_read、godot_project_query、godot_project_patch，legacy 两个入口按需；未知运行时不自动加源码修改。已展示指导直接调用，不再要求先搜索。

任务/世界变化会重建工具目录，清除旧延迟激活；摘要、评审、结束收尾没有工具。原有宿主权限、租约、预算与采用流程未改变。

验证：craftmine-context.test.ts 与 runtime.test.ts 共 154 项通过；TypeScript noEmit 通过；Rust 新增绑定世界 runtimeKind/baseId 测试通过，覆盖 Godot、legacy、未知且无源码回执。原生 PI 循环使用确定性供应商夹具成功执行读取→修改→检查，没有 ToolSearch；没有真实模型、浏览器、玩家数据或输入操作。

限制：验证证明目录与执行可达性，不证明宠物功能、自动采用、视觉效果或真实模型提速。第二次对照与安装包冻结由总控继续。
