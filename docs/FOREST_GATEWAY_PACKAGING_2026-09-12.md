# 林间入口整景封装（2026-09-12）

在原 17 个内置源码包基础上加入 `cw.scene.forest-gateway`。它是单根、依赖自包含的 `scene`，沿用 `craftmine.package/1`，没有新增安装格式或另一个资源库。18 包压缩总量为 305300 字节。

资源声明提供风格、尺寸、几何数量、通道和推荐相机位置；坐标转成毫米整数后进入既有 canonical 声明，原场景的浮点变换及布局文件保持原值。自然光照仍是独立的可选模块，不随整景安装擅自覆盖世界环境。

全部原始 GLB、纹理、无 LOD 导入配置、对应 CC0 许可及原创组合 MIT 许可随包保存。封装逐文件核对哈希，再用现有格式完整打包和解包验证。

真实 Rust 预检重新覆盖全部 18 包，并重复安装一棵橡树，共 19 次源码安装，位置、实例身份、UID 和完整资源锁通过：`test-results/builtin-install-t0lcBy/report.json`。无执行器的检查状态为 `source-saved-check-blocked`，并不代表游戏画面已经验证。

整景的原生引擎加载、通道和选中验证见 `FOREST_GATEWAY_COMPONENT_2026-09-12.md`。最终 Windows 成品中的组合画面与重开仍需另验。
