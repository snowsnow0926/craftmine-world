# Demo 提交前复盘与修复记录

日期：2026-09-15。基线：96eb7b45，已交付的 preview25 ZIP 保持原样。

本轮目标是提高首次打开、复用素材、创作、试玩、保存与再次打开的可靠性。继续使用 PI Desktop 的现有界面；用户自己录制宣传片，提交格式为 Windows x64 免安装 ZIP。

## 已确认的问题与本轮处理

| 优先级 | 问题与证据 | 修复方向 | 验收要求 |
| --- | --- | --- | --- |
| P0 | 双博美城市的自动检查有导航路径，但实际运行中雪球仍卡在建筑旁，玩家已到荣誉高地。测试模板已明确标为“导航待修”。 | 复现真实碰撞、寻路和跟随，定位原因后修复；保留原失败证据。 | 实际走到目的地，保留碰撞和重力，保存及冷开后状态正确；仅返回路径点不能算通过。 |
| P0 | 素材复用的世界活动范围目前仅由组合方案提示，直接安装可能继续使用兼容默认 ±80，之后才在城市深处触发保存失败。 | 单装、组合装、提案确认共用接收世界配置；绑定源版本；未知世界要求明确有效配置。 | 在写入前拒绝缺失或过时配置；旧 v2 文件不静默改写；大世界内合法宠物位置能保存。 |
| P0 | 组合目录已有 recipe v3，但工具 manifest 只允许 v1/v2。 | 同步公开参数与实际处理器。 | 用真实 manifest 验证 v3 参数，并覆盖拒绝无效参数。 |
| P1 | 结果卡把自动检查称为“愿望检查通过”，部分文案无证据地声称进度已保留，技术错误直接展示。 | 明确检查、应用、试玩和错误状态；给出当前可执行的主要动作；原错误保留在详情。 | 不能把旧请求结果说成本次完成；不能把自动检查等同玩家实际验收。 |
| P1 | 刷新结果时短暂清空状态；世界切换和迟到响应可能使结果难以理解。 | 同一对象保留上次状态并标记刷新；切换对象立即清理；未确认时禁用采用。 | 不闪烁、不跨世界显示或采用结果，不吞掉刷新失败。 |
| P1 | 我的模板空搜索文案不准确，导入后列表不刷新，首次指引的示例入口描述有误。 | 在现有示例页补充导入路径，修正空状态、刷新与指引。 | 实际 React 表单、分页、导入、延迟响应和选中版本验证；保留已有一键示例创建。 |
| P1 | 创建锁定既表示进行中，也表示保留失败操作等待重试，简单禁用会阻断恢复。 | 区分忙碌和可恢复的原操作，仅允许明确复用原输入与 operationId 的重试。 | 不能在重试中悄悄换模板或新建重复世界，也不能堵死恢复入口。 |

## 基线已经修复、本轮回归的事项

- DeepSeek 1M 上下文配置、输出预留后的压缩阈值、最高思考强度映射，以及普通任务默认次数/时长限制。
- 长思考流式更新的重复消息压力、测试观察器的重复采集压力。
- 自动应用与候选构建期间保存冲突、保留的世界绑定、可恢复任务继续入口。
- 工具公开参数与主机处理器、素材固定版本、保存状态身份和源版本检查。

这些不代表所有创作都会成功。既往 500K、内存退出和导航失败记录仍保留，不能用新结果覆盖历史失败。

## 验证与交付边界

1. 每项修复先跑对应逻辑/界面回归，再在集成版本验证主要流程。
2. 浏览器和应用使用独立后台测试档案，禁用 Pointer Lock，不抢占用户鼠标、键盘或窗口。
3. 若需要真实模型请求，使用用户选择的 DeepSeek Flash、1M、max 配置和普通创作入口，不额外设置 token、调用次数或整轮时长限制。
4. 对模型生成内容区分源代码检查、实际运行、玩家动作、保存与冷开；缺失的环节明确记录。
5. 空白世界的完整多轮自由创作矩阵此前尚未执行，不宣称已经全面通过。提交前按时间优先验证新建、可运行内容、保存与重开。
6. 新包从通过集成验证的干净提交构建。原 preview25 包及失败模板不覆盖；新交付写明修复、证据和剩余限制。

## 实施状态

本文件初始状态为执行中。入口/模板、结果反馈、素材复用由三个隔离的 Codex 子 agent 负责；总控负责导航复现、集成与最终验收。具体通过项在完成后补充，不预填成功。

### 导航复现与修复

已确认三处原因：路径起点投影到墙另一侧；粗导航参数把实际不能迈过的低矮构件当作可走；连续转弯的朝向分解积累缩放误差，触发原来的恢复变换检查。修复仅替换这个已识别城市自己的跟随脚本，保留已发布 v2 素材、场景、身份、碰撞和存档校验。

隔离 headless Godot 已通过 23 项检查：从原存档出发，实际绕墙和上坡、到达玩家附近、连续位移、原存档字段保留、另一个进程完整冷恢复。每个物理帧最大位移约 0.0471 米。中间失败证据保留。应用里的 Web 运行、正式采用和发布仍需另外验收，不能据此宣称完成。
