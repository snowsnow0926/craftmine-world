# 四个演示世界的历史耗时与复用对照计划

模型：`gpt-6-astra`；思考强度：`xhigh`。来源为首次交付时封存的 19 轮内容创作记录，逐轮 token 差值求和，没有重复累加线程累计计数。

| 世界 | 创作及修改轮数 | Agent 累计耗时 | 总 token（含缓存输入） |
| --- | ---: | ---: | ---: |
| 主线：树木、博美、怪物、巨怪与 AK47 | 10 | 约 109 分钟 | 48,041,929 |
| 停雨与倒流 | 2 | 约 34 分钟 | 10,433,527 |
| 歼-20 飞行 | 3 | 约 47 分钟 | 14,306,615 |
| 城市漫游 | 4 | 约 58 分钟 | 23,478,637 |
| 合计 | 19 | 约 249 分钟 | 96,260,708 |

总量中缓存输入为 93,312,256，占总 token 约 96.94%；非缓存输入为 2,599,827；输出为 348,625。推理输出是输出字段的子集，不再次加总。此处不估算金额。

耗时包括内容 Agent 自己的工具执行与内容排错；不包括平台功能开发、操作者试玩、人工等待和打包。几个世界曾并行执行，因此累计耗时不是整个项目的起止历时。

这组世界制作记录已经复用了前一轮的博美和歼-20 GLB，未计入那两个模型此前单独制作的消耗，也不构成严格的“全部从零制作”对照组。

已有部分复用例子：在已有歼-20 模型的情况下，首次做出飞行世界的创作轮耗时约 17.56 分钟、3,065,010 token；随后补保存与画面，才达到上表的最终累计值。已有模型不等于已有完整玩法组件。

机器可读汇总：`test-results/codex-promo/world-cost-baseline-20260913.json`。
原始逐轮账本：`test-results/codex-promo/author-ledger.json`，SHA-256 为 `59d020200ca9161c8c06d630cad189552948930d7503f894abe9fad80a392511`。

## 接下来应分别测量的情况

1. **直接恢复既有世界。** 既有封存包演示验收已经记录 0 次模型请求，因此该恢复流程不消耗模型 token。加载、导入、构建和重开时间须独立记录；它不等于 Agent 在新世界中复现。
2. **在全新世界复用素材或可玩组件。** 新会话通过正常前端请求，检索库中固定版本，完成放置、适配、检查和采用。素材库检索/安装的 Codex 接线补齐后实测，当前没有这项完整对照数字。
3. **让全新 Agent 参考原世界后重新创作。** 分清提供源文件/可提取组件和只提供截图的条件。只有参考图并不等于可直接安装内容，不能预设其节省幅度。
4. **匹配需求的新制作对照。** 使用相同目标和验收条件，不提供这四套成果。历史累计值保留为参考，不直接当作严格对照。

每次使用新的独立世界和会话，保持玩家实际选择的 `gpt-6-astra / xhigh`，不新增 token、调用次数或整轮时长上限。先以同款可跟随博美完成小规模对照，再扩展飞行、控雨及完整城市。

记录两条时间线：Agent 创作轮耗时，以及从玩家提交到结果正式可玩的总耗时。拆开报告总输入、缓存输入、非缓存输入、输出、实际模型请求与 Blender 作业（可取得时），同时保留失败和修复成本。新会话不代表服务端缓存被清空，缓存以真实计数为准。

验收必须覆盖相同外观与目标行为、独立实例、保存重开；复用以精确资产版本/哈希和正常安装回执证明。仅少用 token、只出现模型或只通过编译都不能判为复现成功。所有自动验收保持独立后台、可取消，不发送真实键鼠或抢占焦点。

当前状态：历史数字已核对；新的 Agent 复用/参考世界对照尚未运行，本次没有新增模型调用。
