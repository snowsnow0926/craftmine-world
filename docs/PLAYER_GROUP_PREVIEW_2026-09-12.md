# 普通玩家组合素材：真实提案、检查与预览

本轮使用冻结 Windows 成品 `e8e373fce6d0`，只在原森林演示 `qQcut9/profile` 创建一个普通会话，未打开其他玩家档案。原愿望为：

> 在门洞两边加上城墙和塔楼，用现成素材拼成一个完整的城门入口。先一起准备好让我预览。

按既有真实配置使用 `deepseek-v4.1-flash-expires-on-0910 / max`，未新增评测额度或整轮时限。模型自然完成 10 次请求，主机记录约 309.5 秒；这不是人为限制 10 次。完整用量保留在证据 JSON，不能把缓存读取量当新增输出量。

## 实际完成

模型自行检索现代源码素材库。多词 `wall tower castle gate` 返回 0 条后，自行用空搜索读到了 18 包目录，随后读取城墙、塔基、塔中段和塔顶。模型调用了 `godot_view_capture`，但结果是 `GODOT_VIEW_CAPTURE_DIMENSIONS`，仅文本、没有图像；不能称为看图自验。

模型提出一次包含两问的玩家澄清：现有门洞还是准星边界、新入口延伸规模。按原意选择现有石拱门两侧，以及两侧各一段墙和一座完整方塔。两项恰好都为首项，但均按实际语义审阅选择，没有自动首选，也没有提供代码或工具答案。

模型创建了真实 `propose-group` 提案 `source-20a92aaf8b5cb3236836cd5468246c56fee7d910bedc8378`，共 8 件：

| 资源 | 左侧位置 | 右侧位置 |
| --- | --- | --- |
| 城墙直段 | (-3.816, 0, 2.458) | (3.684, 0, 2.458) |
| 方塔基座 | (-6.816, 0, 2.458) | (6.684, 0, 2.458) |
| 方塔中段 | (-6.816, 3.03, 2.458) | (6.684, 3.03, 2.458) |
| 方塔垛口 | (-6.816, 6.06, 2.458) | (6.684, 6.06, 2.458) |

模型本轮没有修改源码或启动检查；停稳后，按总控确认的具体清单，测试通过正规 `installSourceProposal` 完成一次安装。源码从 revision 3 到 4，新增 8 个独立实例，原自然环境和森林两实例仍在。真实 broker 检查 `gjob-a670805c198c8327922bc34e64d0d66bab6418315d59d2e6a0c51430de5388d4` 通过，运行就绪、画面帧、无错误、快照、隔离、恢复六项断言均通过。

候选 `gcan-7677a6b803605a0ca0f4fcf01f081ec171e3cf0925ded160af320170b2057211` 已通过正规 UI 预览、关闭。**没有采用或保存新作品。** 两次成功预览报告均干净退出，原模型报告及 marker 保持字节一致；正式 build 仍为原森林 `gbd-266a02b8…`。

## 视觉与通行边界

候选截图通过新的绑定截图接口获得，scope 为 `candidate`，包含真实候选与实例身份，前后宿主状态和窗口/视图布局一致。没有借正式画面冒充候选画面，也没有在截图时修改尺寸。

原玩家位置已穿过门洞，初图朝向林内。随后单独补验：在正式世界正常后退到 z≈10.834、视角 pitch=0.18，再重新打开同一个已检查候选；没有再安装、再检查或改源码。候选仍从检查快照的原视角开始，第二张图仍朝向林内。此次因此**未得到城门正面图，不能判定完整城门构图、墙树穿插或候选通行通过**。模型自己指出两棵中景橡树与规划墙体占地重叠；该提示仅是源码布局推算，尚未通过正面图验证，也没有擅自移动树。

第一次无写测试导航失败单独保留：测试过早调用普通 play 按钮，当时世界列表未就绪，只进入了选世界页。436 次只读模式查询后精确停止该后台进程；源码仍是 revision 3，没有安装调用。这是 harness 失败，不是模型或 group 安装失败。后续改为在独立主 renderer 等 `data-active-world` 与真实世界匹配后调用普通按钮 handler；没有写私有 store，也没有修改冻结包。

## 复验资料

- 新驱动：`tests/source-proposal-preview.mjs`，仅接受真实已停稳玩家报告和其中的新提案；默认 prepare，`--run` 才执行。
- `--reframe-report <既有成功预览报告>` 只复用既有检查和候选，不再安装或检查；有限正式玩家移动与候选截图分开记录。
- 采用、保存、任意源码写、模型启动与任务恢复均不在本驱动控制合同内；取消文件位于每个补充报告目录。
- 原始报告与截图绝对路径、文件哈希、问答、工具结果、包回执和用量见 [证据摘要](evidence/PLAYER_GROUP_PREVIEW_2026-09-12.json)。原始大报告和 profile 未提交。
- 两项离线合同测试、脚本语法检查通过；产品成功检查与两次预览另有真实回执，不以单元测试代替。
