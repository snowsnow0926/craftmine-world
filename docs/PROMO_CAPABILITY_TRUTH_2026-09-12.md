# 宣传片开发第一轮：能力报告与素材复用通路

日期：2026-09-12。源码基线：82380309。范围：Godot 能力发现 GU1 的第一项修复。

## 完成内容

- 素材库、组件包、内容历史不再固定显示不可达。按照核心实际握手中的 assetCatalog、creationPackages、contentHistory 和会话草稿能力生成报告。
- 三个工具逐模式列出真实调用。历史读取使用 content.history、content.changes、content.operation.read，并与既有适配器共享方法表。
- 检查点、合并和包变更继续是建议；报告明确不执行采用，不能把建议能力误读成安装或写入权限。
- 报告只读查询当前任务的绑定世界，继承会话绑定而非猜测全局选中世界；未绑定、未迁移内容仓库、Git 状态不可读、旧核心缺少能力标记分别保留原因。报告查询不打开工作区、不获得草稿租约。
- 宿主自定义方法名照实显示，但尚未核实的方法保持未知。移除 library.capture 被错误归为没有工具入口的旧描述。
- 修复上一轮控制器与 HUD 调整遗漏指导同步的基线缺陷：生成器固定到经过核对的 82380309 源码，造物指导和目录升至 1.4.0。仅 creation_world.gd 的参考正文与哈希发生变化，第一人称指导完全不变；仍严格匹配源码，不放宽校验。生成器此前也停在旧提交及旧版本号，本次同步修正，避免重新生成时倒退。

## 验证

- capability.test.mjs 与 broker-contract.test.mjs：32 项通过。覆盖所有已声明模式、专用开关关闭/缺失、会话未绑定、未迁移世界、自定义方法、只读讨论、建议不产生宿主写入。
- capability-modes-core.test.mjs：使用基线 release/craftmine-core.exe、独立临时数据目录通过。实际建立空世界和 Godot 项目、迁移内容仓库；验证迁移前历史不可读、迁移后可读，以及真实素材搜索、组件列表、历史版本和差异读取。
- live-and-execution-wiring.test.mjs：9 项通过，包含实际 broker 的执行器和预算供应方接线。
- creation-guidance/guidance.test.mjs：7 项全部通过；原来失败的当前底座接口匹配已随指导同步修复。总计 49 项通过。
- 全部验证无浏览器、无真实输入、无 Pointer Lock、无模型请求。

## 交付边界

本轮解决能力报告误判和工具发现前置条件，不新增宠物专用工具，不扩大包采用权限，也没有证明任意素材可兼容安装。是否存在具体素材、版本是否合法、组件检查是否通过，仍以对应真实调用结果为准。Windows 新包和真人体验由集成阶段完成。
