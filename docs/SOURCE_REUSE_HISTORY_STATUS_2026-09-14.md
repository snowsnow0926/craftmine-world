# 素材复用历史检查状态说明

真实城市创作中的组合安装检查 `gjob-3b3ee11044d88599a665b478bfd4058b4dcf72bb01c165d85e3ff225d4bceb0f` 对应源码版本 6，结果为 `failed`。只读会话记录同时明确包含 `sourceStale: true`。当前状态记录的正式构建由另一条检查确认通过；这些事实不能把原检查改成通过。

旧界面持续显示“检查失败，源码已保留”的原因有两处：主进程把已有的 `sourceStale` 字段丢弃；界面在检查进入终态后不再查询，而且刷新优先保留本地旧结果。

本次只把已有布尔字段传到界面，并在建议刷新、后续创作轮结束时重新读取同一检查编号。明确陈旧的失败现在显示：

> 历史检查失败；不代表当前源码状态

按钮为“查看检查历史”。原记录仍为失败并保持可见，不自动重试、不隐藏、不改数据库，也不把后续成功归到旧检查上。因为 Core 在旧分支不可读时也可能返回 `sourceStale: true`，界面不会仅凭这个字段声称后续修改已经通过。

证据来源只读：`D:/cm-product-agent/test-results/desktop-native-product-5N6HZ2/session.json` 与 `status.json`。本次没有操控正在运行的城市、重建总控产物或发起模型请求。

验证：状态解析和原生投影 5 项测试通过；实际 React 工作流 15 项通过，覆盖同一组件在创作轮状态变化后的刷新、无重复安装、原失败不变，以及缺失字段时不虚构新状态。TypeScript 类型检查通过。
