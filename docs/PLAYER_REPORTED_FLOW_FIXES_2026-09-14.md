# preview.23 玩家真实反馈与修复记录

用户原话：要求“再给我生成一只小狗”，过程出现多次工具错误，结束后看不到第二只狗；刷新后应用报错并黑屏。网页世界中的创作对话也报错。用户随后明确要求修复，覆盖此前“先不开发”的讨论状态。

四张原始截图和该次对话、操作记录、只读 SQLite 在线备份保存在
`D:/Craftmine Archives/player-flow-report-20260914/`。`MANIFEST.json` 记录来源、时间和 SHA256。
没有改写用户当前档案、操作正在使用的窗口，或复制账号密钥。用户仍在继续试玩，因此
文件按各自采集时间记录，不冒充应用暂停后的全档案一致快照。

## 已确认事实

- 应用版本 `0.14.4-preview.23`，源码 `76e8c0a9`；实际模型 `gpt-6-astra / xhigh`，全自动。
- 小狗原始请求会话 `9bf1a417-e9a2-4d40-8352-1b01e9b146b6`，回合 `963bee54-c799-40be-8082-9b10a8d3720f`。
  Agent 当时只创建了安装提案，结尾明确写“目前尚未加入世界”；界面整体结束状态与后续操作仍容易误解。
- 两次分页错误来自 limit=100/60；素材搜索漏填 scope；已交付演示世界观察升级后的精确组合缺少指导兼容项。
- 后续用户安装的检查作业 `gjob-4530dcebcd0325a5e14e128ca11c6f52b36e21ee5160ed0702574f9376dec96e` 通过。
  新构建 `gbd-a126d5f5448517629670ed8ffbf1951d9cecf2d408099143184bb87bd749f24c` 的候选于 06:52:23.290 UTC 实际采用。
  存档包含原 `pet-dog-01` 和新增独立组件；不能将黑屏等同于未写入第二只狗。
- 截图错误为 `GODOT_CANDIDATE_ACTIVE`。源码确认刷新先打开世界、后清理候选的顺序会阻断恢复；真实插件错误只保留 `.code`，旧协调器却只认 `.errorCode`。最初黑屏的全部时序仍需独立原生复测，不能仅凭截图归因。
- 网页世界会话 `f9285320-cb4e-4c93-8fe0-b2923da361d5` 的“生成一个树”于 06:45:41 UTC 报 `CODEX_GODOT_WORLD_REQUIRED`，24ms 内拒绝，尚未请求模型。
  用户之后自行切换 Deepseek 的新尝试是另一轮，不替代 Codex 能力验收。

## 修复与验证边界

候选恢复、网页世界 Codex 工具选择、分页及素材查询和精确指导兼容项由独立分支修复。
素材库增加明确的全自动安装模式，仍使用原创作任务、现有检查和采用机制；普通提案保留确认流程。
聊天素材卡片读取真实采用记录，区分源码准备、检查通过、已加入世界和历史版本。

针对性授权、重复操作、源码保留、撤销授权和状态回读测试已通过；独立 headless 界面测试
确认采用后无需手动刷新即可显示“已加入世界”。真实模型、原生画面与冷开验收已完成：
原话加狗得到两只独立狗实体并自动采用；最终解压包通过候选预览刷新、采用与冷开；网页
空白世界原话生成树通过真实 Codex 创作、评审、普通应用和冷开。详见
[实际验收与证据边界](PLAYER_FLOW_FIX_ACCEPTANCE_2026-09-14.md)。

修复成品为 `0.14.4-preview.24`，程序源码 `c04cef7e13e30e77460ae12f1bd29546173aeea1`。
已解压入口、完整 ZIP 和保留原试玩档案的方法见[当前 Windows 交付](CURRENT_WINDOWS_DELIVERY.md)。

两套旧 catalog 测试有 23 项基线失败：仍调用已淘汰的 `importCatalogSource`，其 UI fixture
也未实现当前 `sourceProposals`。独立分支在未修改相关文件时得到相同失败；没有降低断言或
伪装通过。实际当前提案、直接使用、作者安装和状态回读使用各自现行测试验证。
