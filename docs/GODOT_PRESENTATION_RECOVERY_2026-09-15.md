# 自动应用后世界面板残留错误修复

实测包 Demo-preview26-19a5e153，独立测试输出 desktop-native-product-vJtlTm。玩家请求“我想要一棵树。”自动应用成功，随后普通打开世界操作在读取旧面板错误时遇到 GODOT_CANDIDATE_ACTIVE。

证据：plugin.log 在 2026-09-14T17:37:26.550Z 记录应用成功；003-show-tree.json 在 17:38:19.409Z 因旧错误中止；004 的画面和运行观察确认 tree_1 已存在。总控进一步验证 005 普通打开、006 实际移动与保存成功，且保持同一实例。因此本次是旧界面错误残留，并非树没生成或整个运行实例黑屏。原日志未逐条记录失败 RPC 名称，不能断言两条拒绝分别来自哪个调用。

修复仅针对世界面板的显示、暂停恢复意图：候选事务忙碌时保留最新有效操作，事务解除后重新请求；收到真正成功回执后，才清除它自己留下的忙碌错误。保存失败和未知错误不会被后台重试覆盖或清除。世界切换、页面关闭、手动预览和应用过程会取消过期操作。未修改候选身份、源版本校验或自动应用权限，也不自动关闭玩家预览。

验证使用独立禁用 GPU 的 headless 完整面板和纯调度回归；没有调用模型、启动游戏实例、操作当前玩家数据或抢占输入。真实包的下一轮验证由总控执行。原自动保存及旧世界快照测试保留全部语义断言，只补齐已有函数签名和局部状态的测试夹具。
