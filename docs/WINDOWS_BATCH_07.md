# 第 7 批：额度、缓存和 Windows 客户端收尾

产品：craftmine world / 最中幻想。日期：2026-09-09。

本批承接第 6 批后的剩余工作，并纳入用户截图中的 token 上限和缓存问题。
用户没有独立 Windows 测试机，因此先完成当前环境能开发和验证的部分。
所有浏览器与桌面自动验证使用独立 headless/offscreen 进程及独立数据目录。

本轮开发和同包无窗口验收已完成。新程序在
`D:/Craftmine World/desktop/build/windows-preview-batch-07/`：运行整个目录中的
`Craftmine World.exe`，或使用其中约 155 MB 的 `Craftmine-World-Setup-0.14.3.exe` 安装。
当前打开的旧窗口不会自动获得修改，请先导出备份，关闭旧程序后再启动新版本。
完整步骤见 [Windows 使用说明](../desktop/windows-USER_GUIDE.zh-CN.md)。

## token 上限为什么会出现

截图的“剩余 938k”表示模型单次可容纳的上下文还有空间。
`TOKEN_BUDGET_EXHAUSTED` 来自本地任务的累计预算：旧默认值是 100 万，
同一任务内每步输入、输出、压缩和评审调用都会计入，用过的缓存输入也有真实用量。
界面的“本轮合计”与持久任务账本也不是同一个范围。

本批让新任务默认不限制累计 token；旧任务保留原额度和账目，玩家可以在
世界工作台解除上限或设置新额度，再恢复已保存的任务。实际用量、未知请求的预留量、
草稿和恢复关系都保留。模型自身的上下文与单次输出容量仍然存在，请求次数、
压缩次数和运行时限也单独显示，不能把这些说成模型无限上下文。

目前仍保留每任务 80 次请求、8 次压缩和首次请求起约 30 分钟的绝对时限。
解除 token 上限不会给已经过期的任务续期；界面对此类过期任务的恢复提示仍不够细分，
后续请求会显示准确的时限错误。这是本批保留的独立边界。
有重要的未应用改动时，请先导出备份再结束旧任务；结束后新任务从已应用世界开始，
不会自动继承旧草稿。目前没有普通界面的一键历史草稿重新挂载功能。

本地额度错误有独立说明并停止自动重试，不再误报成 DeepSeek 服务故障。

## 缓存做了什么

旧代码把每步变化的任务编号、草稿版本、回执和用量放在系统提示词中，
导致后面很长的重复历史也失去相同前缀。本批保留稳定的系统提示词和历史，
把最新任务事实放到本次请求的末端。事实仍逐次从宿主重建，不会作为玩家新指令保存。
工具调用与结果的相邻关系、思考内容和图片都保留。

使用现有 `deepseek-v4.1-flash-expires-on-0910`、high 思考配置进行了 6 次真实 API 对照：

| 请求结构 | 第一次 | 第二次 | 第三次 |
| --- | ---: | ---: | ---: |
| 每步改变系统内容 | 0% | 1.35% | 1.35% |
| 稳定系统内容、最新事实放末端 | 0.67% | 98.24% | 98.24% |

这是固定约 1.9 万输入 token 的小规模实验，总用量 114,096 token，
未知用量 0。它证明这处修改改善了前缀复用，不代表所有长期创作都会达到这个比例。
新增内容、发现新工具、压缩与服务端缓存预热仍会影响结果。
原始非敏感证据在 [deepseek-prefix-experiment.json](dispatch-reports/batch07/evidence/deepseek-prefix-experiment.json)。
DeepSeek 缓存默认开启、按相同前缀复用且不保证 100%。
见 [官方缓存说明](https://api-docs.deepseek.com/guides/kv_cache/)。

在最终 Windows 包的真实创作中，我们还强制压缩了三次：19 次创作请求的累计
未缓存输入为 54,293，缓存读取为 93,824，整体输入缓存命中率为 **63.34%**。
这个数字不含另外三次摘要和一次评审，比固定前缀实验更接近一次不断改动的创作过程。
三次压缩会改变前缀；本批没有把 98.24% 包装成每个任务的保证。
证据见 [同包压缩与用量](dispatch-reports/batch07/integration-evidence/packaged-compaction/summary.json)。

## 与本次问题一起完成的范围

- 重要操作保留持久回执；刷新、重启或丢失回复后确认原结果，避免重复安装和保存。
- 作品已装进草稿但检查提交失败时，可以重新检查原草稿，保留任务与预算。
- 游戏可占满工作区，一键回到创作，原会话和其他页面保持。
- 扩展的多个命令先整体校验，再一次发布状态，后一个失败不会留下半次奖励或伤害。
- 正式应用会保存本次验证过的扩展；重启后继续使用相同扩展状态。
- 评审保留全部失败断言；玩家可明确选择“带评审提示应用”，独立机器检查和加载仍须通过。
- 接入实际启动、界面帧间隔和任务耗时数据；缺少样本时不填造假的 0 毫秒。
- 复核并补强预算设置丢回复后的恢复，以及损坏备份的预算字段检查。

## 当前验收状态

基础领域 276 项、Rust 57 项、PI Agent 运行时 330 项、最终接线 30 项和多语言 22 项通过。
完整 TypeScript/JS 构建通过。不同测试有重叠，不能把所有数字相加当作独立功能数量。

最终同一 Windows 包的结果：

| 小白能理解的验证 | 结果 |
| --- | --- |
| 桌面打开、隔离、检查和应用等基础流程 | 40 项通过，使用固定测试回复 |
| 真模型生成树，保存作品，新会话再次使用同一版本 | 20 项通过，含记忆与备份 |
| 真模型做到一半进程中断，再恢复并完成 | 12 项通过，保留原草稿与账本 |
| 真模型连续压缩三次，继续改树、应用并重启 | 10 项通过，另有 13 项持久数据审计 |
| 生命、枪械、近战和扩展组合，保存后完整重启 | 30 项通过；固定作品、真实评审与实际游戏逻辑 |

组合场景的模型评审仍提出未通过的断言，测试明确模拟玩家选择“带评审提示应用”，
保留全部红色结果；独立机器检查与实际战斗、扩展、存档检查通过。它不是模型自主
从零开发这套战斗系统的证明，也没有把评审改成自动放行。

825 个程序文件逐个复制哈希一致。源码固定在 `5ac3f1a`，安装包、对应源码、许可与构建
清单一致。所有真实用量、失败尝试及验收边界均保留。

最终交付包、对应源码提交、安装包哈希和同包验收结果由
[第 7 批集成报告](dispatch-reports/batch07/G_REPORT.md) 统一记录。

干净 Windows 首装、覆盖升级、卸载、跨用户凭据和真实操作手感仍需独立环境或玩家体验。
本批不会把当前机器上的无窗口验证记作这些项目通过。通用任意代码状态迁移也不作承诺；
目前提供兼容状态保留、数据迁移约定和不兼容时拒绝覆盖。

因此，W0–W4 的本机核心创作与固定玩法组合闭环已有证据；W5 的程序包、备份、诊断和
说明已交付，干净系统安装生命周期与物理体验仍待独立环境。不能称整个项目已没有待办。
