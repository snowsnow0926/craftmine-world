# 横版恢复后的镜头定位修复

beb66d76 同包综合客户端 `D:/cm-fb2-20260910/test-results/desktop-native-complete-iwxLmt` 完成四底座首次创作和游玩；重开横版时实际截图只有 3 个采样颜色，人物落在画面边缘，故第 26 项未通过。两次客户端严格退出均为 0，隔离审计无错误。没有将其记为完整通过。

根因是横版房间重新放置玩家时启用了镜头平滑，而宿主在恢复状态时暂停游戏；镜头仍可能留在旧中心，不能靠等待物理帧补齐。修复在玩家传送及目的房间限制设置后立即重置镜头平滑的位置并刷新视图；普通移动保留平滑，不恢复物理推进，也不更改玩家进度。

原生 Godot 对照：`test-results/side-view-camera-Ms7eJ0/report.json` 中未修复中心为 [320,180]，恢复到 [589.4258,439.924] 后应为 [589.4258,300]。修复后 `test-results/side-view-camera-fD7MyW/report.json` 的初始加载、两处暂停恢复定位、两次完整进度不变共 5 项通过。中间一次夹具把房间加载自然补齐的实体字段误当镜头修改，保留 `side-view-camera-UjlV7G`；最终进度断言比较镜头操作前后的完整状态，不要求换房前后实体集合永远相同。

方法语义参考 [Godot Camera2D 官方文档](https://docs.godotengine.org/en/latest/classes/class_camera2d.html)。上述原生测试验证真实镜头变换；最终包仍需完整客户端画面和恢复复验。没有降低原来像素断言来放行问题。
