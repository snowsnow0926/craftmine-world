# P10 集成记录（进行中）

本阶段已整合 P1 的渲染帧无关请求处理、首次加载失败持久化、P2 应用状态解释、P3 分视图快捷键、P4 宿主任务计量，以及 P5 真实 React 统计显示。原主目录、旧 827 包与玩家 profile 保持原状；候选源码位于 D:/cm-fb-20260910。

Main 对实际事件先按 envelope 的 session/turn 身份记录，结束任务前排空计量队列；异常通过同一结束事务写入统计缺口，不把缺失数据当完整。只读指标查询不暴露写 RPC。快捷键回调只改变所属窗口全屏状态，Esc 请求明确退出，不关闭应用或退出世界。

集成审查补了两处接线：首次加载失败私有 RPC 和中文确认阶段；Godot 预览额外保留 100px 状态解释区域，避免被原生游戏子视图覆盖。P2 实际页面夹具更新后 23/23；完整桌面 TypeScript 检查通过。界面夹具不等于真实模型、原生按键或最终包验收。

P1 改动涉及的三个既有源码字节 pin 已逐项对照 Git blob 与工作树刷新，未改变任何作者、许可证、分发范围或待决权利声明。

后续集成：P6 资产导入/预览已接入当前目录授权；已知旧加载组件的显式修复已接入 Core 原子门禁，并拒绝补回已检查项目中被删除的源码。37 项相关受控测试通过。首次重跑中一个旧测试直接导入新增依赖时失败，已让它使用项目既有 TypeScript 测试加载器后重跑通过；没有修改产品模块解析规则。

P7 新 broker 在独立真实引擎测试中确认成功任务回收、失败与历史任务保留，完整原始证据已合入；最终包仍必须从当前源码重编和复验。P8 已接入只在显式隔离验收启用的有限控制器，真实凭据只由 localhost relay 运行时读取；模型固定文件中的 `deepseek-v4.1-flash-expires-on-0910`，累计无 Token 上限、最多 16 次实际生成请求，不能切换模型。

候选版本设为 `0.14.4-preview.1`，只调整本 fork 版本面，不改上游来源声明或刷新模型目录。当前尚未交付新发行物，仍需真实客户端/模型回归、Windows 安装包及便携包同源码构建/核验。P9 内容扩展保持可选，不算本轮基础修复已完成。

P11 本机只读准备检查：未发现 Windows Sandbox/Hyper-V 服务或可用签名证书；没有执行系统安装、卸载、系统配置修改。不能因此声称干净系统生命周期或签名发行已验收；本地受控运行和完整包内容核验继续进行。
