# 候选包与验收边界

## 第一轮冻结包

源码提交：`b9c0c0d5bdf371280d1a9814773d614e02920e16`。版本：`0.14.4-preview.1`。
源码工作树：`D:/cm-fb-20260910`；完整发行目录：`desktop/build/releases/b9c0c0d5bdf3-1b22d534-54c2-4a0f-80a6-1028eb4b0df6`。
安装包和便携包均完成解包逐文件核验，安装包尚未签名；没有执行系统安装或卸载。

- 综合客户端：`test-results/desktop-native-complete-ZceQ3Y/report.json`，55/55，8 次严格正常退出。四种底座真实创建、检查、首次采用、游玩、保存与重开；跨世界 CPZIP 复用、四种世界独立复制、备份和恢复通过。
- 默认参数：`test-results/desktop-native-defaults-XCVZgh/report.json`，28/28，4 次严格退出。
- 单条问题导出：`test-results/desktop-native-issue-export-66nig5/report.json`，11/11，2 次严格退出。
- 旧失败世界：`test-results/desktop-native-lr-wD8aLi/report.json`，6/6；真实旧桥失败、显式重试、新候选采用、完整进度保存重开，详见 P7 已归档证据。
- 引擎任务回收：`test-results/p7-native-UsEoqi/report.json`；两次真实成功检查、一次真实失败保留、重启后保留文件哈希不变。退役成功任务的逻辑文件字节为 1,453,367,360，不表示磁盘净释放。
- 资产面板复验因验收控制器把预期拒绝变成页面异常而中断。P8 两次尝试在窗口就绪和原始世界列表结构检查处停止，实际模型生成请求为 0。这些不是模型服务失败或功能验收通过。

## 第二轮候选

工作树：`D:/cm-fb2-20260910`。保留第一轮源码与证据，继续修正有限验收控制器及驱动：P8 等待真实窗口与引擎就绪；资产面板只将五个已知预期拒绝编码为有限结果；旧桥驱动使用已获准的隔离目录前缀，并在有界就绪轮询中仅容忍精确的“尚无运行时”错误。

第二轮须在干净提交冻结后重建，并按新的提交与 build-manifest 哈希重新运行实际客户端验收。第一轮结果不能代替第二轮包的结果。后续证据及报告必须明确对应哪一轮包。

## 真实模型与外部限制

用户指定运行时读取桌面 deepseek.txt 中的精确模型 `deepseek-v4.1-flash-expires-on-0910`；不使用别名或其他模型。不设累计 Token 上限；此前最多 16 次实际生成请求的授权仍保留，包含 SDK 重试。两个源码目录合计尚未发送生成请求。真实凭据只在限定远端的本地 relay 内存中使用，不进入客户端、命令行或报告。

独立后台测试禁用 Pointer Lock 和真实输入，不激活用户窗口。软件渲染的后台游玩与截图不能替代真实按键、原生 GPU 性能或干净 Windows 安装/升级/卸载验收。基础场景是可运行原型，不能据此宣称内容丰富度、任意生成能力或 L3/L4 全部完成。
