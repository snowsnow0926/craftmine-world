# 第三轮候选：从实际失败继续修复

工作树 `D:/cm-fb3-20260910`。前两轮源码与发行目录保持不变。版本仍为 `0.14.4-preview.1`；最终交付依靠提交、构建清单和文件哈希区分候选。

第二轮 beb66d76 安装包与便携包同源码重建及解包核验通过；完整客户端不能记为通过：四种底座首次创作、游玩及保存均通过，但第 26 项横版重开截图失败。实际引擎确认恢复位置后镜头未立即跟随；本轮已修镜头定位，5 项定向真实引擎检查及 26 项四底座完整进度检查通过。新包仍须重新验证画面、复制、复用和恢复。

第二轮素材表单先暴露空来源信息触发 INVALID_TEXT，再经明确标注的探索驱动发现编译后解码 worker 错误启动整个 Main。这两项已修：留空记录为未知而不冒充许可通过；解码器成为独立构建入口，成功/取消/超时都等待线程退出，无法确认则明确失败。Main 的正式退出流程现接入预览服务清理。旧包默认参数 28/28、问题导出 11/11 通过，不能自动代替新包成绩。

真实模型使用文件指定完整型号，16 次已授权请求已用完（一个早期断连、十五个 HTTP 200）；真实服务器回报型号另记为 deepseek-flash。锤子未生成候选，小狗尚未请求，不能算创作通过。实际用量为 285,458 Token；发现上下文重复计入 UI 镜像，引发三次过早整理及反复读文件，并暴露同任务多张统计卡。预算估算和单张统计卡已修，最终物理请求预算门禁保持；真实修复后复测仍待次数授权更新，不把已取消 Token 上限解释为无限次请求。

第二轮旧桥恢复检查完成实际恢复及完整状态比较，但一次第二次退出发生原生 0x80000003，故整组仍失败。同一合成存档的最小重开/快照/退出通过，暂未稳定复现，不能声称已根治。P7 第二轮真实回收通过。详见 P7 分开的成功、失败与最小复测证据；第三轮仍要求严格进程退出码，不能用空审计数组替代。

本轮仍只在独立 D 盘数据、后台无输入进程中验证。没有执行用户系统上的安装/卸载，没有覆盖原试玩包、个人存档或客户端，也没有推送 GitHub。干净 Windows 生命周期、真实键盘/全屏手感、原生 GPU 性能及正式签名/权利材料单独保留状态。最终包与易读交付报告在验证完成后另行生成。
