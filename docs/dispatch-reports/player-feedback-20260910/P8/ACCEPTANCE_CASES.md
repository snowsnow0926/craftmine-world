# P8 真实模型验收：待授权的有限案例

准备日期：2026-09-10。执行对象为 P10 提供的同一源码、完整客户端候选；此处尚未发出真实模型请求。仅使用 D 盘新建测试世界与合成内容，不读取玩家世界、历史聊天或复制玩家 profile。

1. 第一人称：在测试训练场创建可见的雷神之锤。明确有限行为为拾取、装备、普通攻击，以及有冷却时间的局部闪电视觉效果；保留现有训练目标和存档。让模型走正式工程读写、构建反馈、有限修错、预览，之后由验收驱动执行正式采用、检查对象/行为、保存并重开。
2. 俯视角：在新测试村落创建一只小狗。明确有限行为为可见角色、有限距离跟随、接近交互显示一句话；保留已有角色和地图。采用相同正式创作链，检查正式采用后重开仍存在。

以上为重建案例，不是玩家原提示的逐字复现；不得将固定脚本或预写作品当作模型创作成果。

已授权边界：总计最多 16 次实际模型请求，包括修错、重试和子调用。用户随后明确取消 50,000 的累计 Token 上限，并指定使用桌面 DeepSeek 文本配置、仅允许其完整模型名 `deepseek-v4.1-flash-expires-on-0910`。测试不得自动改型号。全部计量仍记录完整/部分/缺失情况，未知用量不视为零；不把密钥写入文档、日志或聊天。

证据需绑定 source commit、exe/core/runtime/plugin/engine/broker 哈希、独立 profile、world/base/build/candidate/application 身份。逐项保留提示、实际请求/重试、工具调用、构建错误与恢复、预览/采用回执、完整/部分/缺失用量、生成时间和任务墙钟时间。对照 P4/P5 显示和宿主原始记录，分别记录代理型号与实际执行型号。

故障项使用合成世界、有限注入：初始化失败、中断、缺回执、过期候选、采用失败；需要证明已有正式世界/进度保持，错误原因可见，显式恢复后能继续。只有真实模型参与的案例才写“模型通过”；离线驱动只能写相应本地通道/故障通过。

授权状态：用户已明确授权运行时读取 `C:/Users/WINDOWS/Desktop/deepseek.txt`。只读配置检查得到官方 API 根地址与上述完整型号。GET `/models` 成功但未列出该型号；用户确认仍按文件中完整名称调用，不以目录结果替换。该元数据查询不是模型生成请求。实际生成须在 P10 同源客户端冻结后执行；测试配置从这个已授权文件读取，不复制玩家 profile。
