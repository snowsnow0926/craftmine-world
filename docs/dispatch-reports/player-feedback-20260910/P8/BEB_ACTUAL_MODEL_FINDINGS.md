# P8 sealed beb model attempt

Source: beb66d76c00a102c755ec7aeaed59b19cf5c68a9. Original report remains at D:/cm-fb2-20260910/test-results/desktop-native-p8-V35GyJ/report.json. The sealed application was not changed. An independently hashed external driver unwraps the actual product prompt/abort IPC envelope.

The shared authorization journal reached 16 admissions: one earlier disconnected attempt and 15 subsequent HTTP 200 responses. Exact requested model remained deepseek-v4.1-flash-expires-on-0910; the service returned deepseek-flash as a separately recorded response alias. No fallback occurred. The dog case made no request. The hammer task ended in error after budget exhaustion; it did not produce a source mutation or checked candidate. No extra request is authorized by this report.

P4 durable usage reconciliation passed against all 15 response usage reports: total 285458 tokens, output 29593, reasoning 11125 (included in output), input 141305, cache read 114560. Wall time 178511 ms; generation 98748 ms; output TPS 299.68201887633165. Coverage remains partial because six additional locally observed calls have no upstream usage. No inferred usage replaces missing facts. Strict owned shutdown passed.

The original driver then failed its first-card token assertion. Four actual DOM cards shared the same durable turn: 73080/running, 156811/running, 241279/running and 285458/error. Only the final card was current. The product repair assigns the one card to the final compaction segment, retains a real message identity for the host lookup and updates memo equality. Five actual React DOM checks pass with authored read transport; this is not a rerun of sealed native acceptance.

Three automatic compactions occurred at tokensBefore 31989, 34230 and 40191; the model never called new_context. Summaries grew from 8281 to 13727 to 21142 characters. All structured readFiles arrays were empty. There were 26 Godot file reads and no project patch/build. The private facts response was about 20 KB and capability report about 18 KB; both were repeatedly read. The 128000-token model configuration reserves 16384 output tokens; the Craftmine preflight triggers at 85 percent and currently measures full internal message JSON (including mirrored tool details), unlike the final provider wire content. This is a diagnosed preflight inflation risk, not a claim that removing protection or sending more requests fixes the task.

The subsequent estimator repair passes six actual PI/SDK loopback checks and strict TypeScript. Initial authored fixture used generation 0 and was correctly rejected as CRAFTMINE_CONTEXT_INVALID; it was corrected to the existing valid generation 1 contract, with raw failure retained. No extra upstream POST was made. The final sealed-package/model rerun remains pending.
