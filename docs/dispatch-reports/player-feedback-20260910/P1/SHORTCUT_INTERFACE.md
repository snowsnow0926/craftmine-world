# P1/P3 finite child-view shortcut handoff

Dependency: P3 helper e3abc987501c027f4911838cb943c741bee6bfe0 (identical
cherry-pick bf9f442 in this branch). P1 initial-load fix remains 487dc779.

P10 Main integration:

```ts
new GodotWorldViewHost({
  // existing options remain unchanged
  onFullscreenShortcut(action) {
    const window = /* the same owner returned by this host's window option */;
    if (!window || window.isDestroyed()) return;
    window.setFullScreen(action === "exit" ? false : !window.isFullScreen());
  },
});
```

Production files: host, Godot preload, and the shared Godot channel constants.
No main index, App, world HTML, plugin view or global shortcut registry changed.
The bridge refuses stale/noncurrent/hidden/detached/unshown candidate views,
foreign or extra scope fields and non-main-frame Escape messages.

Actual validation: 17/17 tests from
`node --test tests/player-feedback/P1/fullscreen-host.test.mjs tests/player-feedback/P3/fullscreen-shortcuts.test.mjs`;
`node tests/godot-remaining/D/godot-candidate-typecheck.mjs` reports zero
diagnostics with CRAFTMINE_TYPECHECK_DEPENDENCY_ROOT=C:/cm-plan-next-20260910.
Raw logs accompany this report. No real keyboard input, Electron/native rerun,
window activation, fullscreen call or Pointer Lock request was generated by
these tests. Real canvas Escape consumption and user keyboard behavior remain
P3/P10/manual verification; F11 and Escape are not declared player-accepted.
