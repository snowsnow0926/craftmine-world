# 第 7 批集成验收报告

日期：2026-09-09。产品：craftmine world / 最中幻想。

当前阶段：产品代码已通过下列集成检查，准备冻结并构建新的 Windows 包。
安装包和最终同包真实模型结果尚未完成，不能据此记为通过。

## 已完成的修复

累计任务额度与模型上下文分开。新任务默认不限制累计 token，旧任务可由玩家在工作台
明确解除额度或自定义额度；原 owner、草稿、实际与未知用量保留。额度错误停止自动重试，
真实错误结束会登记为可恢复，旧错误记录也按严格条件修复。限额修改丢失回复后，
即使任务已恢复，也能找到原回执而不重复修改。

系统提示词与既有历史保持稳定，每次请求的新任务事实置于请求末端。
真实 DeepSeek 六次固定输入对照中，预热后的命中率从 1.35% 变为 98.24%；
共 114,096 个实际 token，未知用量 0。这是受控实验，不是长期任务的命中率承诺。

其他收尾包括重要操作持久回执、原草稿重新检查、游戏工作区展开、扩展整批状态校验、
应用时保留验证过的扩展、明确的玩家评审提示确认、实际生命周期遥测，以及备份账本验证。

## 冻结前验证

| 验证 | 结果 | 证据与边界 |
| --- | --- | --- |
| 基础领域回归 | 276/276 | G 的 domain-tests.log |
| Rust core | 57/57 | 最新错误结束恢复、预算与备份回归 |
| PI Agent runtime | 330/330 | 包含上下文、缓存结构、错误与重试检查 |
| 最终 Node 集成 | 30/30 | 真实 Rust/宿主、预算回执、原子扩展等 |
| 多语言 | 22/22 | 新错误与未缓存输入用语 |
| TypeScript / JS 完整构建 | 通过 | pnpm run build:js |
| 工作台 UI / React 布局 | 30 / 10 项通过 | 独立 headless；带宿主固定响应，不冒充真实创作 |
| G 独立正式游戏、存档、全进程重启、备份 | 25 项通过 | 固定内容，零模型调用；desktop-native-batch07-NInYtr |

日志位于集成工作树的 `desktop/build/batch07/`，正式非敏感证据随最终交付补录。
定向测试与完整测试有重叠，表中数字不能相加当作独立测试总数。
真实模型作者、固定作者与真实评审三种证据必须分别标注。

## 尚待本轮完成

1. 从冻结提交构建新的 Windows 安装包、对应源码和许可清单。
2. 同一个实际包的离屏验收、真实模型创作与作品库复用、三次压缩、错误/重启恢复、玩法存档。
3. 补齐原始证据、包哈希、项目进度和小白使用报告，再合回主仓库。

## 当前环境无法验收

干净 Windows 首装、覆盖升级、卸载、跨用户凭据与真实操作手感仍未验收。
没有可用签名证书，本批交付为未签名的预览包。兼容状态保留与数据迁移已有约定，
没有实现任意代码的通用状态迁移。扩展验证原子性不等于任意外部图形/音频失败的事务回滚。

全程采用独立数据目录与无窗口进程，禁用 Pointer Lock，无真实鼠标键盘操作。
未运行安装器、未替换用户当前运行的程序、未改动用户现有创作数据。
