# Formal Windows export service — 2026-09-10

Implemented a native-host export service and Godot library UI, plus a standalone bootstrap using each existing managed runtime. The service reads only godotRuntime.exportSource and content.readFile at the returned formal contentOid. No draft source is used. The sourceRevision comes from the formal build owner. Exporting never mutates the managed project or source world progress.

Acceptance: 12/12 passed across first-person, top-down and side-view, with actual ended full-client fixture copies, real Rust exportSource, the pinned LPAC broker, and six independent headless Windows game processes. Full results are native-final.json; raw native logs are adjacent. Initial embedded JSON matches the original formal snapshot exactly. Native-to-native load/save/restart compares every field exactly. Cross-WASM-to-native comparison allows only explicit Vector coordinate paths, using exact float32 bits; every original decimal and normalized value is retained in the report. Health, inventory, quests and all other fields are not normalized. Seven focused service tests pass, and the isolated package/export UI test passes with zero input/focus/Pointer Lock.

Final native temporary root: C:/Users/WINDOWS/AppData/Local/Temp/windows-formal-export-UrPdtC.
Core SHA256: 4cbfd7b4933795613c6996a8983822a50984ef8f99b09e8a4192d1f0e54ee51c.
Broker SHA256: 0a0d671fdf37010de5bb4a8838de4f75e9ccdd318eb05a87fb472680325ff8bf.
The parent must rebuild/re-pin its integrated broker and stage the two new shared files. This report does not certify a final installer or current integrated index wiring.

Prior failures remain unchanged: GDScript signal name/type errors, bit-identical Vector decimal serialization, and Windows LPAC editor cache paths exceeding usable path length. The latter was fixed with short scoped directory and task tokens; full operation identity remains in the checked intent. The strict script/engine log gate was added after observing an export process return zero despite script parse failures. Only five exact existing LPAC OS diagnostic message/location pairs are allowed.

The exported game uses F5/Save, periodic save, and save-before-close. It loads the captured formal progress then local progress on restart. Local save validates full identity/version, size and hash, preserves a previous backup, and refuses bad states or failed writes. Those failure behaviors are implementation coverage, not yet separate native corruption/crash fault-injection acceptance. This is ordinary native game execution, not a security sandbox for the exported EXE.

Scope limits: mining-sandbox is the fourth catalog base and is not yet accepted or enabled by this service. No model calls, visible play/input, whole-product export picker, copied-world runtime or live active-view checkpoint were tested here. The test checkpoint uses already durable ended-client progress. The Town/Ruins source fixture had separately passed build/apply/gameplay/save, but its original client restart attempt failed WORLD_BUSY; these exported Windows restarts passed and do not rewrite that earlier result. Authored module rights remain pending. Outputs include complete transformed source and exact Godot notices, labelled local preview, without any new redistribution permission. Additional third-party package notices/rights need integration review before external distribution.
