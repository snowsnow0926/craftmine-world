# Native package service handoff

New module: `vendor/pi-desktop/apps/desktop/electron/main/craftmine-package-service.ts`.
Root must construct it with the live selected-world function, private plugin
domain caller and native picker, then route the existing package gateway to it.
No root index/main/gateway source was edited by this task.

Five native file-service test groups passed, including actual synthetic
junction refusal. Raw output: `package-native.log`; data:
`C:/Users/WINDOWS/AppData/Local/Temp/package-native-gZpFi9`.

The real integration now includes the native service instead of directly
invoking the installer. Seven checks passed with Rust SHA-256
`41d740d0b14cce18f4980b73c2e1485cba7a30fe18089acde76d0b0a9a5dfc81`
and the verified Godot 4.7.2 engine. Raw receipt: `package-native-core.log`.
Fixture/engine logs: `C:/Users/WINDOWS/AppData/Local/Temp/package-source-vuTsiH`.
The source extractor/UI also now reports the 512-object list bound explicitly.

The native fixture picker is author-controlled, and the engine runs only the
fixed authored door fixture. No real input, focus or Pointer Lock was used.
No registered executor, completed product build/check, candidate apply or model
request is claimed by these tests. Cross-restart native operation/grant recovery
remains a product integration requirement; the lower source intent is durable.
