# Full-client Windows export entry acceptance

Passed 8/8 in D:/Craftmine World/test-results/desktop-native-wx-tspfPV. This closes the earlier direct-service-only gap for first-person. It starts the actual frozen compiled Electron application with its actual installed plugin and fresh profile. Product navigation creates a new training-range world and waits for the real broker build/check/application and rendered active game. Fixed product gameplay switches equipment and changes camera direction. The test then invokes worldPanel('godot.exportWindows',{worldId,operationId}) through the real panel route, native main export service, actual GodotWorldViewHost.checkpoint and private core formal-source read. No service, checkpoint, core receipt or source version is mocked.

The only picker accommodation is the existing headless host branch selecting this task's windows-exports directory. Source/index files are not patched. Initial embedded progress equals the complete actual client snapshot after gameplay. The same operation replays the original receipt. Client shutdown is orderly with exit code zero, no forced kill, no focus/input/Pointer Lock violations and no page errors. Two separate native exported-game processes verify initial progress, actual sword equip, local save and complete second-process restoration. Native-to-native JSON equality is exact; known cross-WASM Vector values retain the existing exact float32 comparison and original values.

Frozen source-built client: C:/Users/WINDOWS/AppData/Local/Temp/windows-client-frozen-d5RVol/frozen-info.json. Its 1636 copied files are fully hashed and verified before launch. The app out/resources/build and packages dist/dist-bundle are copied. Workspace @pi-desktop dependencies resolve to frozen copies; only third-party installed node_modules remain shared read-only. The root was held static during copying and every source copy was rehashed. The root release broker was copied only into this frozen resource tree and its identity regenerated there. Its binary contains the frozen LF Windows preset bytes exactly. Broker SHA256: abd205c5a9ac1fbcde7f4741dfd1195671c1718e646bd029c9fa0dd5f95139a7. Main SHA256: 8c235426bebdebef4703a5bb032912659c14f324d1b1f49713d7b537e5cf81db. The final package must be rebuilt and retested against its own complete resource identities; this is not installer acceptance.

All raw report, logs and six captures are preserved. capture-1.png was visually inspected: actual training range, three targets, pistol, crates and HUD are present. Electron stderr includes a deprecation and one Widget message rejection; zero runtime page errors does not mean zero process diagnostics. The initial test-directory naming error was rejected by the existing headless profile guard before controller readiness, and is retained as first-failure-*; the script was corrected to use test-results/desktop-native-* without changing the guard.

Only the first-person whole-product export entry was tested here. Four-base direct-service export/native restart evidence remains in ../windows-service; this report does not substitute first-person for full-client export of the other three. No model call, visible play input, Pointer Lock or foreground browser was used. Authored module rights remain pending; local preview does not grant redistribution permission.
