# Frozen I provenance refresh, 2026-09-10

Only `FREEZE.lock.json.sources[docs/GODOT_MULTIBASE_DEVELOPMENT_PLAN.md]` changes. The main plan was read from `D:/Craftmine World/docs/GODOT_MULTIBASE_DEVELOPMENT_PLAN.md`; this does not edit, stage or copy the user's uncommitted plan. The task owner authorized refreshing this source provenance while preserving all eight frozen specifications and the full 15-category, 30-round, 141-assertion denominator.

| Identity | Previous | Current |
| --- | --- | --- |
| SHA-256 | `92b0a04f1d6cca624bde8bff2dc6623acd3c6dd710077d2914f426abdcc67592` | `eeb688fbedadc3641aa5d553531bd4c08f8f0cfb2fa236c9f1169e7483fac291` |
| Bytes | 48801 | 49718 |

This is provenance bookkeeping, not a new acceptance freeze or a passing model result. Existing unimplemented product-interface routes, incomplete model accounting, and missing visual/player evidence remain unmet. A later plan edit must be detected again by the source integrity check.

The existing formal product prompt path is `craftmine-acceptance-f-agent.ts` → renderer `piDesktop.invoke(piDesktop.channels.invoke.agentPrompt, {sessionId, viewingSessionId, messageId, content})` → the normal main-process handler and Agent sidecar. The I `pi-plugin` transport constructs its own Agent and cannot substitute for verification of that production entry point.

The first-person adapter already exposes bounded native `equip`, `look`, `attack`, and `interact` through `BaseOps`; `runtime_bridge.gd` delegates unknown managed operations to that adapter. Earlier inspection of the bridge's administrative OPS constant alone incorrectly suggested these operations were absent. No new gameplay operations or expected-state setters are needed for the initial equipment probe.

The first attempted new real-model controller patch was rejected by automatic approval review before any file was written. The stated reason was missing explicit user authorization for credential/project-data egress to DeepSeek. No model call occurred and no alternate execution path was used. Further work on that controller awaits authorization evidence or an explicit user decision coordinated by the task owner.
