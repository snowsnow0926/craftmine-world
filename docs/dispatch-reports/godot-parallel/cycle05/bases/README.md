# Cycle 05 bases delivery

Scope: E/F/G authored base materialization, shared Godot bridge and complete native progress. No Rust, JavaScript host, application UI, plugin, license declarations, master merge or push changed in this worktree.

Implemented: versioned world/base identity; explicit initial load and paused lifecycle; complete native snapshot/save/restore; exact UTF-8 runner receipts and scoped durable acknowledgement; atomic scene restoration; rejection of unsupported custom fields; managed persistence ownership; G target/vital progress; E stable timestamp and paused contact metadata; G editor shutdown leak fix. Generated F examples and hashes are synchronized.

Evidence is authored fixture evidence, with zero model calls. The original physics suites separately exercise actual movement, attacks, shops, quests, ability gates and restart. No real input, foreground window, browser manipulation or Pointer Lock was used.

Managed progress: 20 groups passed in test-results/godot-managed-progress-SJsZxX. The report includes strict import and all process runs. Full state equality retains every field, including fixed historical savedAt and midair onFloor=false. Each base rejects foreign identity, future version, unknown fields, invalid body/player and an oversized body without partial mutation. F additionally rejects a missing saved scene; G rejects invalid vitals, unknown target fields, missing rooms and inconsistent target/reward ledgers. Receipt acknowledgement rejects a foreign instance, wrong snapshot hash and stale confirmation after restore.

Persistence audit: 5 groups passed, including strict full side-view import with no resource leak diagnostics, in test-results/audit-base-save-xEKH1W. Raw logs and saved bodies remain in the isolated worktree evidence directories for root archival.

Parent reported separate actual Godot Web/Electron/Rust native acceptance 9/9 with complete exit/restart equality in godot-runtime-native-mr2Pjt. That evidence belongs to the host agent and is not duplicated or relabeled here.

Limits: the body preserves the native progress schema; transient velocity, animation frames and active hitboxes remain outside that schema. Existing unknown native fields are explicitly rejected if they cannot survive the adapter. Runtime candidate transactions, real-model authoring acceptance, sandbox enforcement and A17 remain parent/core work. Do not treat these fixture checks as product release approval.

Final native regressions: E 52/52 (godot-first-person-base-NYNHiO), F 40/40 and G 80/80 (gd5-native-Cig5lQ). Both committed F examples match regeneration. Compact check reports and original-file hashes are included alongside this file; complete raw reports remain at those paths. E acceptance output now resolves inside its own repository/worktree instead of the parent directory.
