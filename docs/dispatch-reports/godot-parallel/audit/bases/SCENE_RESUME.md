# F 完整重启场景与朝向修复

接续提交 27ecce6，保持同一独立工作树与分支。修改仅限 top-down 底座、生成物与审计报告。

修复 Game 首次绑定入口场景时覆盖存档 sceneId/facing 的缺陷。world.json 增加稳定
sceneId 到 authored 场景路径的 scenes 映射；启动先保留完整存档位置与朝向，临时入口
场景停止处理，目标场景加载并应用真实角色/精灵朝向后才发布绑定。路由期间和失败时
禁止自动保存，避免把入口默认状态写回旧档。旧工程采用本运行时时须同步场景目录映射。

真实固定 Godot headless 全量 **40/40**：保留原 34 项，新增 6 项。

- 店内从出生点真实移动到柜台并购买，再移动到非出生点面朝上保存。
- 完整新进程仅调用 snapshot：sceneId=shop-interior、真实坐标与保存值误差<0.001、
  player/sprite 均朝上，钱物/库存/任务/奖励/flags 保持。
- 回到室外真实左移保存，新进程恢复同一非默认位置与朝左精灵。
- 删除场景映射、映射指向错误场景：两次启动均禁止保存，主存档字节完全不变。

生成一致性 2/2。模板、示例脚本、world.json 与 world-build.json 哈希已同步。
证据摘要见 scene-resume-evidence.json。无真实模型调用、无输入模拟、无前台窗口。
G 编辑器退出资源泄漏继续保留为未修复，未扩展重构。
