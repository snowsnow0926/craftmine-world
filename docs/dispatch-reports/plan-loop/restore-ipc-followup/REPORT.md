# Actual restore acceptance after IPC assertion correction

The same immutable ae32974 packaged application passed 16 focused mining-sandbox steps with three clean shutdown audits. It created, played, saved and reopened the real world, exported and inspected its archive, rejected a stale restore, compared every saved native field, restored through the normal product action and reopened the restored root. The package seal was reverified after execution.

Only the test helper changed (58a93a8; caller tree cb549a7). This does not certify VM2 or the subsequent path-budget patch. The earlier comprehensive run remains failed after 50 passed steps; it is not rewritten as a pass. The Windows long-profile defect remains open in ae32974. No model, input simulation or visible window was used.

Raw evidence and exact file hashes accompany this report. The original private profile is retained at `C:/cm-release-test-20260910/test-results/desktop-native-complete-xijxdS`.
