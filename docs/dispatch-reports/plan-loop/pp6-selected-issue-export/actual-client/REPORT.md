# Actual selected-issue export client acceptance

Development source e2050d273d2466f8d36c6b42083a249065bd7a01 passed 11/11 steps with two strict clean client exits. Its real broker built and checked one authored world. Main captured exact original issue text, two supplements, player retest status and each stored version identity, then wrote the selected JSON under the fixed headless save authorization.

Replay returned the identical receipt and file bytes. Wrong revision, caller-supplied path and an unsupported export-all channel were rejected without overwriting. The raw issue ledger, full progress (including savedAt) and formal build were unchanged by export. A new process preserved the original ledger, exported file and every saved field.

This exercises the actual Main channel and fixed test-owned file grant. The earlier DOM-to-real-service tests separately cover the actual detail form, picker cancellation and write/lost-reply failures. No visible OS picker was opened; this is not clean-OS or packaged acceptance. No model, external transmission, diagnostics or extra records were collected. Cross-process operation replay is deliberately not claimed.
