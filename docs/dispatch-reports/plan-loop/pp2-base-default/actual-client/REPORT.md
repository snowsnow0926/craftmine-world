# Actual default-button client acceptance

Development client source 16134bac6e706ab3cee45936db8d8d352e0b67bf passed 28/28 steps and four strict clean exits. Native core, host and broker source remained identical to the verified 827 package; the reused executable hashes were checked separately. Managed runtime and JavaScript were rebuilt from this source.

The real default form filled independently verified training-range120 while leaving the complete domain state unchanged. The original durable submit created one check; loaded/running evidence bound the exact world, job, build and120. Preview cancellation kept500 and full progress. Actual gameplay advanced after the check, and adoption retained that latest state plus all other target values. A fourth process start preserved every saved field, including savedAt.

No model request, external transmission, OS input, Pointer Lock or visible window. This is development-client evidence, not a newly packaged Windows release. The 827 release does not contain this default button. Asset-library Main routing was still under repair and is not certified by these parameter tests.
