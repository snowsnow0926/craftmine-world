# Integrated guide and finite parameter development client

The actual compiled development client at source/runtime commit
`fbeccb09e8985f289a0c5bbc13ea3c64185ce787` passed **20/20 checks** in the
fresh private run `desktop-native-parameters-808jW7` on 2026-09-10.

The test created a first-person world through its real broker, used the optional
library guide and existing navigation, played eight fixed semantic actions,
saved the full formal progress, adjusted target A from 120 to 500 milliseconds,
checked actual loaded/running runtime values, closed preview without adopting,
restarted, adopted the exact candidate, and restarted again. All original
progress fields, including savedAt, remained equal. Other targets kept their
120-millisecond defaults. Invalid zero and stale source bindings were refused.

All three launches exited with code 0 and empty input violations, page errors
and **shutdownFailures**. This is the strengthened shutdown audit, not the old
exit-code-only inference. The actual six-step guide remained optional, reopened
collapsed, cleared on navigation, and did not change the world header height.

The report records the rebuilt core/host hashes, compiled Main/plugin identity,
runtime source identity, exact job/candidate evidence and full state comparisons.
The separate `pp2-runtime-requirements-chain` record proves a sibling script's
700-millisecond override is rejected by the real core and cannot be adopted.

This covers one parameter contract on supported first-person training targets,
not arbitrary game properties or every PP1/PP2 story. It is not a packaged
Windows acceptance, real-model creation or a new-player study. Earlier failures
remain preserved. A subsequent review found a separate pending-runtime-factory
shutdown window; this run does not exercise that timing or establish an IOCP
root cause. That case requires additional implementation and verification.

`integrated-tests.log` records 54 focused post-merge tests. The earlier
`host-close-tests.log` records the seven new HostProcess checks and eleven
existing RPC checks; these overlap and must not be added as distinct coverage.
Desktop TypeScript compilation also completed with exit 0.

Four completed earlier private test directories were moved from D: to the
owned C: temporary archive to make room for packaging. Every file's size and
SHA-256 was verified after moving. `earlier-test-archive-index.json` records the
original and current paths and all file hashes. No active test, user profile or
previous Windows delivery was moved. Existing committed raw reports remain
unchanged; their original run paths are historical identities.

## Verification after startup-disposal repair

The subsequent fresh run `desktop-native-parameters-6Uvnzp`, using source and
runtime commit `85b1015de6d40d380ae93c255aa73122ca0fdf06`, again passed all
20 checks with three zero exits and empty shutdown/input/page failure arrays.
Its untouched report and stderr/lifecycle logs are in `after-startup-fix/`.
This repeats the same client scenarios and is not twenty additional cases.

That source adds in-flight startup tracking, so disposal cannot return while a
runtime factory or its late cleanup is still pending. The integrated targeted
suite passed 60 checks, including six new startup timing cases and a real local
HTTP factory/cleanup case. TypeScript compilation passed. Those timing tests
are distinct from the native client's ordinary workflow; neither result proves
the cause of any prior intermittent IOCP failure.

After both runs completed, their full private directories were also moved to
the same owned C: archive and verified byte for byte to reserve D: space for
the new Windows release. `final-test-archive-index.json` records these two
additional locations and hashes; both committed reports above remain intact.
