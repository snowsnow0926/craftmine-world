# Demo 素材跨世界可用性复盘

本轮发现：伙伴 v3 已声明必须配置保存范围，但普通素材搜索/读取仅检查底座
源码前提，安装器仍直接采用 ±80 默认值。这样在出生点检查通过，走到大城
更远处仍可能无法保存。另发现工具参数 schema 漏列已有的组合方案版本 3。

修复把配置接入真实共享安装链：自动单装、自动组合装、单提案确认、组合
提案确认都先解析范围，再写每个新实例的两个导出属性。已知沙盒/城市必须
同时匹配控制脚本、工程入口和主场景哈希；自定义世界要求 agent 查看源码，
提交绑定当前 revision/manifestHash 的结构化 positionBounds。缺配置或源码
已变化时，在计划安装、写源码和启动检查之前拒绝。实例 ID、原有对象、
进度和已发布素材字节均保留。

界面仍基于 pi-desktop。新版伙伴缺少所需配置时，素材详情说明要让 AI
查看世界源码并配置。旧版没有同样的强制配置契约，仍允许有限区域使用，
但显示每个坐标只支持 −80 到 80 的保存范围，并建议大世界跟随使用配置后的
新版或明确审查的源码升级；没有擅自禁止所有旧素材，也没有改旧 ZIP 哈希。

验证使用独立真实 Rust Core，实际导入 ZIP、生成场景、执行源码 CAS 并记录
检查任务。四种安装流程覆盖冷重开、两个不同范围实例和幂等重试；缺配置、
源码变更、NaN/Infinity、倒置边界和注入字段均有针对性回归。实际 PI 参数
校验器也验证了版本 3 与新字段。打包插件重复运行同套 Core 测试。

CPU 测试中的引擎执行器不可用，检查真实返回 blocked；不把此结果描述为
游玩/保存成功。大城跟随、保存冷开和视觉体验由总控的真实玩家流程验收。
既有自动保存候选切换的七个回归继续通过，此轮没有凭推测再改保存逻辑。

后续核对又发现两处阶段问题：候选已经写好范围之后，应用阶段不能再次套用
“新安装是否还需要配置”的判断；添加首只伙伴改变主场景字节，也不应迫使
第二只重新找 AI 配置。现在区分结构可用性与新安装配置要求，并允许项目
入口、完整世界控制脚本链及主场景根绑定均不变时继续自动配置。根脚本、
根属性或变换、继承场景、重复声明和未校验文本均不能获得这种认可。

独立遗留问题：position 初始放置参数仍有 ±80 限制，城市 z=-128 处不能
通过该参数直接放置；保存范围修复并不等于放置范围已放开。本轮仅记录，
没有扩大所有素材的坐标许可。后续应把超范围放置限定到接收世界已验证范围，
同时核对父节点坐标变换、地面以及玩家胶囊中心与宠物脚底的差别。

真实新包的首次直用验收还发现 P0：普通空白世界并非未经处理的底座目录。
创建器会给 project.godot 追加运行桥、adapter 和当前 world_id。只读核对
实际 SQLite revision 2 后确认，场景 bac1347d…和脚本 77f10dff…匹配，
只有项目文件 e956ae08…与原始 pin 不同。现在仅识别这项精确生成器转换：
原前缀逐字节哈希不变、追加后缀完全一致、世界 ID 等于当前世界、运行桥和
适配器完整登记脚本组匹配。没有删除任意 autoload 后宽松比较，也没有信任
玩家可改写的 manifest。新增输入映射或其它项目改动仍需显式配置。

CPU 回归改用真实 materializeBase 生成、完整注册的源码，覆盖首只和第二只，
避免再把未物化底座当成普通玩家创建流程。真实故障资料未修改。

修复后的插件使用实际发布包内 Core 完成 76 项回归，全部通过且无跳过；
五个已发布旧包哈希保持不变，原生 Windows 复测交由总控继续。
