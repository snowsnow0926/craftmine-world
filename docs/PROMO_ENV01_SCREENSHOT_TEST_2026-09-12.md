# ENV01 树组实机结果

本轮只提交「我想生成一些树。」一次，独立 profile，DeepSeek high，
最多 40 次请求、模型阶段 10 分钟。实际使用 11 次请求后正常结束，
耗时 78.872 秒；产品统计总 token 362315，其中缓存读取 280832。

受测目录成品为 `43203d8e`（preview.12）。生成源码完成真实导入、
导出和运行检查，随后通过正式预览、采用、保存、冷启动重开。
采用及截图没有新增模型调用，原报告和请求账本保持原字节。

实机观察：世界中出现多棵有阴影的树，树干和椭圆树冠均为简单几何体。
初始落点离树较近；通过正常后退 75 帧、抬头观察取得全景。
没有补绘、修改模型产物或移动物体来改善截图。花草、修改同一棵树、
连续创造均不在这一条愿望的完成范围内，不能据此宣称环境故事全通过。

原始证据（保存在独立开发目录）：

- 模型报告：`D:/cm-promo-loop-0912/test-results/desktop-native-complete-VburCs/report.json`
- 采用报告：同目录 `adoption-ed70068a-dad2-47ad-b0c7-006807f70830/report.json`
- 全景截图：同目录 `exploration-7988b0d7-7b8c-4a3f-a07e-d349f0325f68/view-1.png`
- 世界：`world-5493090c7bec`

本轮也使用同一受测成品重新观察此前已采用的 PET01 小狗：
`D:/cm-promo-loop-0912/test-results/desktop-native-complete-2lwsto/exploration-1e9b8edb-2976-4473-a01e-b4d03c9ec0c2/view-1.png`。
先正常等待 360 帧，小狗从远处走近玩家。该图沿用旧生成结果，
不是新一轮生成，更不是白色博美犬测试。两组截图审计均无输入、焦点、
页面错误和退出违规，冻结成品不变。
