# 造物连续任务的基础工具目录

真实模型复测发现，重开后的下一愿望仍会沿用历史中的工具名，而每轮清空的延迟激活集合令这些基础调用报“工具不存在”。

世界任务将实际由宿主提供的工程事实、能力报告、造物操作、检查开始、检查读取和创作指导作为基础工具，随每轮请求与运行时重建持续公开。高级源码修改仍可按需发现；没有由宿主注册的名称不会凭空加入。该调整只改变模型看到的工具目录，原有世界作用域、任务租约、预算、源码身份和采用权限继续由宿主检查。

验收：真实运行时连续两轮及两次实例重建，基础造物工具始终在请求目录中；未安装的指导和未激活的高级源码工具不出现，Bash等通用工具仍不属于世界任务。
