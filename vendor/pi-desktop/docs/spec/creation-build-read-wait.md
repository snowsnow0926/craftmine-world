# 模型 Godot 作业读取等待

生产 `godot_build_read` 由宿主固定启用最多 30 秒等待，每 500 毫秒读取同一绑定作业。终态立即返回，活动任务和世界变化中止等待，不发任何写入或重复 enqueue。核心单次请求最多 5 秒且不得超出剩余等待窗；单个待决核心请求期间取消检查间隔不超过 100 毫秒，读取之间的暂停不超过 500 毫秒。

返回实际状态及 waitedMs、lastReadAgeMs、waitReason。超时返回上次真实采样并说明时间，首次读取超时则报错。直接 UI 不经过长等待；未配置的测试工具默认即时读取。
