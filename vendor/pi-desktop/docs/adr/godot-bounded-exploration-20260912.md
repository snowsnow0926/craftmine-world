# Headless 有限探索输入

日期：2026-09-12。状态：采用。

现有固定验收故事无法观察新生成的宠物与环境。为受保护的独立 headless 父控制器增加有限序列，复用既有四种玩法操作和截图实现；不新增模型工具、脚本执行器或玩法状态赋值入口。

序列先校验总预算，随后逐动作验证实际实例身份。截图身份必须来自截图自身的观察；无法确认时失败。继续保留玩法与视觉判断由独立证据决定的边界，序列执行完成本身不等于愿望通过。
