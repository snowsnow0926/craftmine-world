// Actual form/controller with the host's idempotent create/explicit retry contract.
import fs from 'node:fs';import path from 'node:path';import assert from 'node:assert/strict';import {createRequire}from'node:module';import{pathToFileURL}from'node:url';import{playwright,browserOptions}from'../app/browser-tools.mjs';
const repo=path.resolve(import.meta.dirname,'..'),desktop=path.join(repo,'vendor/pi-desktop/apps/desktop'),require=createRequire(path.join(desktop,'package.json'));fs.mkdirSync(path.join(repo,'test-results'),{recursive:true});const out=fs.mkdtempSync(path.join(repo,'test-results/godot-create-retry-'));
const code=`import React,{useState}from'react';import{createRoot}from'react-dom/client';import{useCraftmineWorlds}from'./src/hooks/use-craftmine-worlds';import{WorldCreatePanel}from'./src/components/craftmine/WorldCreatePanel';
const f=globalThis.fixture={serial:0,calls:[],selected:'world-old',registered:false,state:'failed',request:null,mode:'failed',entered:[],closed:0,retryFails:false};
const old={id:'world-old',title:'Original world',state:'ready'},draft=()=>({id:'world-new',title:f.request.title,state:f.state,creation:{operationId:'host-init-id',stage:'check',stages:[],progress:0,actions:['retry','details'],error:f.state==='failed'?{code:'CHECK_FAILED',message:'Check failed',recoverable:true}:null}});
globalThis.__craftmineWorldBridge={onChanged:()=>()=>{},invoke:async(_p,channel,payload)=>{
 f.calls.push({channel,payload});
 if(channel==='world.list')return{activeWorldId:f.selected,worlds:[old,...(f.registered?[draft()]:[])]};
 if(channel==='workbench.capabilities')return{};if(channel==='task.current')return null;
 if(channel==='world.createOptions')return{create:true,switch:true,createActions:true,bases:[{id:'creation-sandbox',label:'3D world',delivered:true,starters:[]}]};
 if(channel==='world.create'){
  if(f.mode==='invalid'){f.mode='failed';throw Error('WORLD_BASE_UNAVAILABLE');}
  if(f.request&&JSON.stringify(f.request)!==JSON.stringify(payload))throw Error('WORLD_EXISTS');
  f.request??=payload;f.registered=true;f.selected='world-new';
  if(f.mode==='lost'){f.mode='failed';throw Error('ACK_LOST');}
  return draft();
 }
 if(channel==='world.creationRetry'){if(payload.worldId!=='world-new'||f.selected!=='world-new')throw Error('GODOT_WORLD_CHANGED');if(f.retryFails)throw Error('RETRY_PREPARATION_FAILED');f.state='ready';return{worldId:'world-new',state:'initializing'};}
 if(channel==='world.switch'){if(f.switchFails)throw Error('SAVE_REJECTED');f.selected=payload.id;return{ok:true,activeWorldId:f.selected};}
 if(channel==='world.creationCancel'){if(payload.worldId!=='world-new')throw Error('WRONG_CANCEL_WORLD');f.state='failed';return{worldId:payload.worldId,status:'cancelled'};}
 throw Error('UNEXPECTED:'+channel);
}};
function App(){const controller=useCraftmineWorlds('en'),[open,setOpen]=useState(true),[formKey,setFormKey]=useState(0);globalThis.controller=controller;f.remountForm=()=>setFormKey(value=>value+1);return <section data-open={open}>{open&&<WorldCreatePanel key={formKey} controller={controller} lang='en' onClose={()=>{f.closed++;setOpen(false)}} onCreated={async id=>{f.entered.push(id)}}/>}{controller.error&&<p role='alert'>{controller.error}</p>}</section>;}
const root=createRoot(document.getElementById('root'));f.reset=mode=>{f.serial++;f.calls=[];f.entered=[];f.closed=0;f.selected='world-old';f.registered=false;f.state='failed';f.request=null;f.mode=mode;f.retryFails=false;f.switchFails=false;root.render(<App key={f.serial}/>);};f.reset('failed');`;
await require('esbuild').build({stdin:{contents:code,resolveDir:desktop,loader:'tsx'},outfile:path.join(out,'fixture.js'),bundle:true,platform:'browser',format:'iife',jsx:'automatic',define:{'process.env.NODE_ENV':'"production"'}});fs.writeFileSync(path.join(out,'index.html'),'<div id="root"></div><script src="fixture.js"></script>');
const report={checks:[],errors:[],scope:'Actual React form/controller; finite host idempotency, registration and retry receipts; no native claim'};let browser;const check=(name,value)=>{assert.ok(value,name);report.checks.push(name);console.log(name);};
try{
 browser=await playwright().chromium.launchPersistentContext(path.join(out,'profile'),browserOptions());await browser.addInitScript(()=>{globalThis.violations=[];window.focus=()=>violations.push('focus');Element.prototype.requestPointerLock=()=>{violations.push('pointer');throw Error('disabled');};});const page=await browser.newPage();page.on('pageerror',error=>report.errors.push(String(error)));await page.goto(pathToFileURL(path.join(out,'index.html')).href);
 const wait=fn=>page.waitForFunction(fn,null,{polling:20,timeout:5000});
 const name=async text=>{await wait(()=>controller.capabilities?.bases.length>0);await page.evaluate(text=>{const el=document.querySelector('[data-world-create=name]');if(el.disabled)throw Error('NAME_DISABLED');el[Object.keys(el).find(key=>key.startsWith('__reactProps$'))].onChange({target:{value:text}});},text);await page.waitForFunction(text=>document.querySelector('[data-world-create=name]').value===text,text);};
 const submit=()=>page.evaluate(()=>{const form=document.querySelector('form');if(document.querySelector('[data-world-create=submit]').disabled)throw Error('SUBMIT_DISABLED');form[Object.keys(form).find(key=>key.startsWith('__reactProps$'))].onSubmit({preventDefault(){}});});
 const failed=()=>wait(()=>!controller.busy&&!!controller.error);
 await name('Preserve this world');await submit();await failed();
 check('registered failed world retains its original attributes and exposes explicit preparation retry',await page.evaluate(()=>document.querySelector('[data-world-create=name]').disabled&&[...document.querySelectorAll('fieldset')].every(el=>el.disabled)&&document.querySelector('[data-world-create=submit]').textContent==='Retry preparation and enter'));
 await page.evaluate(()=>fixture.remountForm());await wait(()=>document.querySelector('[data-world-create=name]').value==='Preserve this world');check('reopening the same form restores the retained attributes and retry identity',await page.evaluate(()=>document.querySelector('[data-world-create=name]').disabled&&controller.createAttempt.input.title==='Preserve this world'));
 await submit();await wait(()=>fixture.closed===1);check('retry uses the same registered world and real initialization action, without a second create',await page.evaluate(()=>fixture.calls.filter(c=>c.channel==='world.create').length===1&&fixture.calls.filter(c=>c.channel==='world.creationRetry').length===1&&fixture.entered[0]==='world-new'&&fixture.request.title==='Preserve this world'));
 await page.evaluate(()=>fixture.reset('lost'));await name('Lost acknowledgement');await submit();await failed();
 check('an unknown acknowledgement preserves exact input until host identity is confirmed',await page.evaluate(()=>controller.createAttempt.worldId===null&&document.querySelector('[data-world-create=name]').disabled&&document.querySelector('[data-world-create=submit]').textContent==='Retry and confirm creation'));
 await page.evaluate(()=>{const button=document.querySelector('[data-action=cancel-world-create]');button[Object.keys(button).find(key=>key.startsWith('__reactProps$'))].onClick();});await failed();check('unknown creation cancellation cannot silently close and lose its request identity',await page.evaluate(()=>fixture.closed===0&&controller.createAttempt.worldId===null&&document.querySelector('[data-world-create=name]').value==='Lost acknowledgement'));
 await page.evaluate(()=>{fixture.mode='invalid';});await submit();await failed();check('later validation failure cannot prove an earlier unknown create was unregistered',await page.evaluate(()=>!!controller.createAttempt&&document.querySelector('[data-world-create=name]').disabled&&fixture.registered));
 await submit();await wait(()=>fixture.closed===1);check('lost ACK recovery resends the identical operation then validates and retries that world',await page.evaluate(()=>{const creates=fixture.calls.filter(c=>c.channel==='world.create');return creates.length===3&&creates.every(call=>JSON.stringify(call.payload)===JSON.stringify(creates[0].payload))&&fixture.entered[0]==='world-new'&&fixture.calls.filter(c=>c.channel==='world.creationRetry').length===1}));
 await page.evaluate(()=>fixture.reset('invalid'));await name('Invalid base attempt');await submit();await failed();check('a proven pre-registration validation failure keeps attributes editable',await page.evaluate(()=>!controller.createAttempt&&!document.querySelector('[data-world-create=name]').disabled&&!fixture.registered));
 await name('Corrected unregistered request');await submit();await failed();check('the next valid request uses the corrected attributes',await page.evaluate(()=>fixture.request.title==='Corrected unregistered request'));
 await page.evaluate(()=>{fixture.selected='world-old';fixture.switchFails=true;});await submit();await failed();check('failed original-world save blocks scoped retry and keeps the existing draft',await page.evaluate(()=>!fixture.calls.some(c=>c.channel==='world.creationRetry')&&fixture.registered&&controller.createAttempt.worldId==='world-new'));
 await page.evaluate(()=>{fixture.switchFails=false;fixture.retryFails=true;});await submit();await failed();
 await page.evaluate(()=>{const button=document.querySelector('[data-action=cancel-world-create]');button[Object.keys(button).find(key=>key.startsWith('__reactProps$'))].onClick();});await wait(()=>fixture.closed===1);check('Cancel after failed retry still cancels only the owned draft and restores the original world',await page.evaluate(()=>fixture.selected==='world-old'&&fixture.registered&&fixture.calls.filter(c=>c.channel==='world.creationCancel').every(c=>c.payload.worldId==='world-new')&&fixture.entered.length===0));
 check('no focus, pointer lock or page errors',await page.evaluate(()=>violations.length===0)&&report.errors.length===0);report.passed=true;
}catch(error){report.error=String(error.stack??error);process.exitCode=1;}finally{await browser?.close();fs.writeFileSync(path.join(out,'report.json'),JSON.stringify(report,null,2));console.log(JSON.stringify({out,passed:report.passed,error:report.error}));}
