import test from'node:test';import assert from'node:assert/strict';import{allowedContinuationCase,stoppedOriginal,validateInheritedBudget,compareHarvestRestore}from'./helpers/creation-model-continuation.mjs';
const budget={format:'craftmine.creation-evaluation-budget/1',limit:40,requests:Array.from({length:25},(_,i)=>'private-fixture-'+i)};
const original={format:'craftmine.creation-next-model/1',model:'deepseek-flash',provider:'deepseek',sessionId:'s',worldId:'w',launches:[{exit:{code:0}}],cases:[{id:'CA05',outcome:'failed',error:'WORLD_BUSY',modelRequests:11},{id:'CA06',outcome:'not_run',modelRequests:0}]};
test('only closed original artifact with its exact shared 25 reservation baseline is accepted',()=>{assert.equal(stoppedOriginal(original,budget).id,'CA05');assert.throws(()=>stoppedOriginal({...original,launches:[{exit:{code:null}}]},budget));assert.throws(()=>stoppedOriginal(original,{...budget,requests:[]}));});
test('prior attempted cases cannot be submitted and previously untouched continuation has one attempt',()=>{assert.equal(allowedContinuationCase(original,'CA06',[]),true);assert.equal(allowedContinuationCase(original,'CA06',['CA06']),false);assert.equal(allowedContinuationCase(original,'CA05',[]),false);assert.equal(allowedContinuationCase(original,'CA07',[]),false);});
test('continuation never resets or changes inherited reservation identity and caps the combined ledger at 40',()=>{assert.equal(validateInheritedBudget(budget,{...budget,requests:[...budget.requests,'new']} ).newReserved,1);assert.throws(()=>validateInheritedBudget(budget,{...budget,requests:['changed',...budget.requests.slice(1)]}));assert.throws(()=>validateInheritedBudget(budget,{...budget,requests:Array.from({length:41},(_,i)=>'r'+i)}));});
test('paused restore tolerates only verified countdown progression and rejects reset rewards or unrelated changes',()=>{const saved={body:{inventory:{wood:1},rules:{chop:{remainingFrames:280,chopped:true,chops:1}},player:{position:[0,1,6]}}};const restored=structuredClone(saved);restored.body.rules.chop.remainingFrames=276;assert.equal(compareHarvestRestore(saved,restored,'chop',4).equal,true);restored.body.inventory.wood=2;assert.equal(compareHarvestRestore(saved,restored,'chop',4).equal,false);restored.body.inventory.wood=1;restored.body.rules.chop.remainingFrames=300;assert.equal(compareHarvestRestore(saved,restored,'chop',4).equal,false);});
