# Domain contract fixtures

The garden and extension JSON files are copied unchanged from dispatch D commit
`2f5097f`, `examples/dispatch-d/garden-training@1.json` and
`training-drain@1.extension.json`. They exercise existing package/ABI formats.
These A tests execute the actual compiler and Rust service, but use explicitly
authored verification/review/application proof fixtures. They do not claim a
native render, real model run, or gameplay behavior execution.
